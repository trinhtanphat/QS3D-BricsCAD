# QS3D local-agent inbox

**Updated:** 2026-08-31 (UTC+7)

This file is the **single live queue for LOCAL_ONLY work**. Detailed runbooks remain in the linked local qualification/handoff documents, but a local agent should start here before opening those longer files.

## Mandatory handoff contract

- A remote/hybrid agent that discovers a new LOCAL_ONLY requirement must add or update the matching item in this file **in the same source/docs batch that introduced or exposed the requirement**.
- Do not create a second live queue. Historical `docs/LOCAL-AGENT-*.md` files are supporting detail/evidence; this inbox is the current priority index.
- Every `OPEN`, `IN_PROGRESS`, or `BLOCKED` LOCAL_ONLY item in this inbox has implicit remote disposition **`DO_NOT_RETRY_REMOTE`**. Subsequent remote/non-local agents must skip its execution/re-audit unless current source materially changes the scenario, the owner explicitly asks for a fresh remote source investigation, or the agent actually gains the missing local capability.
- Before adding an item, remote agents must search this inbox and update the existing matching item instead of duplicating the same unavailable work. Lack of local capability is a handoff condition, not a reason to retry from another equivalent remote agent.
- Local agents work `P0` before `P1` before `P2`, always from a clean checkout of the newest intended SHA.
- `LOCAL_PASS` requires real evidence tied to the exact tested SHA. Source review, static preflight, mock tests, `-SkipRuntime`, or a remote build cannot manufacture `LOCAL_PASS`.
- Never commit proprietary BricsCAD DLLs, private/customer DWGs, signing keys, credentials, or unsanitized runtime captures.
- When an item passes, set `Status: PASS`, replace `Evidence: PENDING_LOCAL` with a sanitized evidence summary, and record the exact SHA under `Evidence`.
- When source changes alter a local scenario, update this inbox immediately instead of relying on an older handoff paragraph.

Valid priorities: `P0`, `P1`, `P2`.  
Valid statuses: `OPEN`, `IN_PROGRESS`, `PASS`, `BLOCKED`.

## P0 — #3681 StructuralWall live-BREP concrete-contact/formwork

- Priority: P0
- Status: PASS
- Area: StructuralWall live-BREP concrete-contact / formwork licensed qualification
- Remote disposition: COMPLETED / NO_RERUN
- Exact runtime checkout: `a4f1a53683a9296532a0290fcb79bc49b9d4b892`
- Minimum source-ready ancestor: `c64eb8c1b83761e155da670904a72e64669464b7`
- Runner: `scripts/run-local-v25-wall-contact-3681.ps1`
- Local contract: licensed BricsCAD V25 qualification is complete for this bounded scenario. Keep the committed runner only as a regression reference; do not rerun unless a material source change explicitly reopens qualification. Hosted/static CI cannot manufacture or replace `LOCAL_PASS`.
- Evidence: `LOCAL_PASS` on exact runtime source `a4f1a53683a9296532a0290fcb79bc49b9d4b892`; sanitized evidence PR #3849 merged as `7fec6f36a7c1181d7113f0e7220ea3dafca66e29`. #3681 is CLOSED/completed.

## LOCAL-001 — exact V25 build/load baseline

- Priority: P0
- Status: IN_PROGRESS
- Area: BricsCAD V25 adapter / packaging baseline
- 2026-08-21 exact issue-72 continuation: clean candidate `0ae7fb4369172198d25347b9b0d75bdbceead2bb` on BricsCAD V25.2.10 passed the official qualification runner with manual-CI/generic preflight, all `962/962` aggregate gates, Core Release `0/0`, Core smoke `ALL PASS`, V25 `Release|x64` `0/0`, offline WPF and licensed NETLOAD/Ribbon/Palette. Matching adapter/Core ProductVersion is `0.1.0-preview.10081+0ae7fb4369172198d25347b9b0d75bdbceead2bb`; SHA-256 values are `B725F335AA71E90E9584EA1A6940A6889ACA2E2FDB22D88C2CB3713047268D01` / `2A5DCE45CC74EB9248A7079E02835DA81DEFD5A492AAF318CF21FB001CB44A2A`. The same exact candidate passed schema-3 Project Lifecycle across four disposable documents: SaveComplete/cold identity/canonical bind/detached and multi-DWG isolation, absent/corrupt sidecar fail-closed, nine REGEN/REFRESH/FINISH phases, legacy/native unit boundaries and explicit unbound Meter override resolution. Package/signing were not requested; the full interactive/private-DWG matrix remains `NOT_RUN` and customer-release qualification remains false.
- 2026-08-23 exact #3612 continuation: refreshed `origin/main@bacfc918982570475d3ab1369310dfbf5119d2a0` is represented by the task branch. Repeated-mode source candidate `9a77d329e90809a2006d8e4dc1bafc995c0a8ca2` passed licensed `NETLOAD` and exact-plugin identity on BricsCAD V25.2.10 and V26.2.07. Each host ran independent two-segment Wall and Beam sequences, observed eight hosted `DrawJig.WorldDraw` callbacks per Family, and passed Enter, physical ESC, planar UCS, active-document switch refusal/isolation, native/semantic Undo/Redo, QS3D/DWG save, sidecar persistence and fresh-process Wall/Beam cold reopen. On predecessor candidate `e5725e96eed6dcebb46370c33e6f8a88e2cc2b68`, V26 CLR 8.0.29 identity/native LINE and the V25 eligible CAD -> B4D -> ED2 XLSX -> Excel Locate -> real PICKFIRST -> visible Workspace bridge passed; the Workspace showed one selected semantic Instance/Family and `1 chọn`, while wrong fingerprint, unknown element, stale Handle and partial resolution all failed closed without corrupting selection/state. This closes only the bounded #3612 cells; private/customer DWG, missing historical BRC-proxy parity, signing/package and the remaining full interactive/release matrix stay pending under their parent issues.
- Google Sheet row 1 Windows smoke-executable bounded PASS (2026-08-15): exact committed/pushed candidate `9b25be9df47459f72acecfa96443d94204410f2f` passed the guarded-entrypoint/failure-containment/manual-CI/local-handoff gates and Release build with `0 warnings / 0 errors`. Apphost ProductVersion/SHA-256 were `1.0.0+9b25be9df47459f72acecfa96443d94204410f2f` / `C65D39BAD9BDBB8F61671478EDA47A182C16C1061F110C8E42E0CDD3E2192606`; Core ProductVersion/SHA-256 were `0.1.0-preview.10045+9b25be9df47459f72acecfa96443d94204410f2f` / `82786A401B2315AD18783B92F476D998B55B02690E3A1EE98BC34D1D58D5E377`. The direct Windows `.exe` run exited `0` in about 19.63 seconds with 30 stdout lines, exactly one `ALL PASS`, no failure token and empty stderr; no Application Error/.NET window, WerFault process, matching Application event, timeout or process residue appeared. Negative managed-failure injection is `NOT_RUN` because no repository-approved external trigger exists; no injection/source/test change was made. Logs remain ignored/local and no Actions/private data/BricsCAD operation was involved. This closes only Sheet row 1 and does not change overall LOCAL-001 `IN_PROGRESS` status.
- Why local: Requires licensed BricsCAD V25 x64, installed managed references, Windows desktop, NETLOAD/DemandLoad, and native command execution.
- Scenario: Run `scripts/run-local-v25-qualification.ps1` from a clean exact SHA with the real V25 install directory; prove Core Release build, Core smoke, adapter exact-V25 build, NETLOAD, DemandLoad, command registration, save/reopen, and multi-DWG isolation. Cold-start/reopen a drawing with an existing `.qsdb`: true writes must bind the canonical same-ProjectId project, while one regeneration-based CSV/XLSX export and one modeless Door/Room refresh/export must use detached regenerated state and leave live project dirty/change-version/timestamp/audit state unchanged. Explicitly exercise `QS3DREGEN` and `QS3DREFRESH` after cache forget/reload: `QS3DREGEN` must bind the canonical existing project before regeneration; `QS3DREFRESH` may regenerate only after the same canonical binding. Repeat those commands with the sidecar/project absent: `QS3DREGEN` must refuse without creating/caching a replacement project, while `QS3DREFRESH` must remain a non-creating UI refresh with no semantic mutation. Exercise `QS3DFINISH` with a selected existing Room after cache forget/reload and verify generated finish semantics stay on the canonical project; repeat with the sidecar/project absent and verify the command refuses without creating/caching a replacement project or finish Family/Element state. Exercise automatic legacy unit binding from a unit-dependent command such as `QS3DBQ`: on a valid legacy project with semantic elements it must bind/update/save the canonical project; on a drawing with supported INSUNITS but no QS3D project it must not create/cache a project merely while resolving units. Separately verify explicit `QS3DUNITS` still intentionally creates/saves project unit state when the user confirms a project override. Repeat ownership-dependent writes with the sidecar absent and verify refusal without leaving a replacement project. For `QS3DINTERCHANGEIMPORT`, review a policy plan, then forget/reload the project cache or replace/remove the sidecar before confirmation; freshness confirmation must refuse the stale plan without creating/caching a replacement project and without applying any import mutation. Repeat the same post-preview cache/reload/sidecar-replacement test with standalone `QS3DINTERCHANGEAPPEND`: its initial target bootstrap before preview is allowed, but after the Yes/No review it must refuse a stale/replaced target through the non-creating freshness guard and must not append any semantic state.
- Additional scenario: Exercise `QS3DLINKHOST` with an empty/cancelled selection before any semantic lookup; it must return without binding, creating, or caching a QS3D project. With one valid opening plus wall source after cache forget/reload, it must bind the same canonical project; with the sidecar absent after selection, it must fail closed without CAD or semantic mutation.
- Additional evidence required: `QS3DLINKHOST` empty/cancelled-selection proof of no project bind/create/cache, valid-selection canonical project continuity, and absent-sidecar refusal with no host/CAD/semantic mutation.
- Additional scenario: Run `scripts/test-bricscad-v25-sidecar-revision.ps1` on the repository-generated disposable fixture. With the canonical project cache warm, make the `.bak` appear, replace the `.qsdb` bytes, and remove the `.qsdb` in separate restored phases. Every read/bind/existing-mutation/Interchange-confirmation/Save boundary must fail closed; the semantic project and DWG hashes must remain unchanged, and byte restoration must recover the same canonical session.
- Additional evidence required: Exact tested SHA, BricsCAD V25/plugin identity, PASS booleans for all three `.qsdb/.bak` warm-cache changes and all five authority boundaries, unchanged disposable/reference DWG SHA-256, restored-session continuity, sanitized marker, environment/process cleanup, and no private path/ProjectId/Handle/fingerprint output.
- Additional scenario: Exercise the rebar selection boundary on the same exact candidate SHA. `QS3DREBAR3D` and `QS3DREBARTIES3D` are PICKFIRST-only: no implied selection must return without opening a new selection prompt and without binding, creating, or caching a project. For `QS3DBEAMREBAR3D`, `QS3DREBARSTIRRUP3D`, `QS3DSLABREBAR3D`, `QS3DFOUNDATIONREBAR3D`, `QS3DWALLREBAR3D`, and `QS3DREBAR3DSHAPE`, cancel/empty interactive selection must return before project binding/creation/cache. Repeat valid selected runs after cache forget/reload and verify canonical same-ProjectId binding plus unchanged generated ownership, exact replacement, rollback and UI finalization.
- Additional evidence required: Rebar selection matrix proving no project bind/create/cache on empty/cancel for all eight guarded 3D commands; proof that `QS3DREBAR3D`/`QS3DREBARTIES3D` remain PICKFIRST-only; canonical ProjectId continuity on successful selected runs; and unchanged generated ownership/replacement/rollback behavior.
- Additional scenario: Exercise `QS3DSAVE` on a cold-cache drawing with a valid existing `.qsdb`; it must bind and persist the canonical same-ProjectId project. On a drawing without a QS3D project/sidecar, Save must fail closed without creating, caching, or persisting a replacement project. After cache forget with a valid sidecar, it must rebind the existing project rather than creating a default one.
- Additional evidence required: `QS3DSAVE` cold-cache canonical same-ProjectId persistence plus absent-project refusal proving no replacement project/cache/sidecar creation.
- Evidence required: Exact QS3D SHA, Windows build, BricsCAD V25 build, .NET/MSBuild version, command/load results, cold-cache ProjectId continuity for true writes, `QS3DREGEN`/`QS3DREFRESH` existing-project and absent-sidecar lifecycle results with proof of no replacement project, `QS3DFINISH` canonical-project success plus absent-sidecar refusal/no-new-project/no-finish-mutation result, automatic legacy unit-binding existing-project persistence plus no-project/no-cache result, explicit `QS3DUNITS` bootstrap/persistence result, before/after live-state invariants for detached refresh/export, absent-sidecar refusal/no-new-project result, generic Interchange stale-confirmation refusal with proof of no replacement project/import mutation, standalone Append initial-bootstrap identity plus stale-confirmation refusal with proof of no second/replacement project and no appended mutation, sanitized failure log if any.
- Evidence: Automated baseline PASS at exact SHA `3a8ae9fc5165fda588ac1377545ad9b31c85982e`: clean-tree/manual-CI/source preflights, all 365 aggregate feature gates, Core Release build and deterministic smoke, exact-V25 adapter Release build with zero warnings/errors, offline WPF theme/Workspace/RightPanel smoke, and licensed V25 NETLOAD/Ribbon/Palette runtime probe. Sanitized runtime identity recorded BricsCAD `25.2.10`, x64 CLR `4.0.30319.42000`, with Ribbon and both palettes ready. The screenshot runner captured only the BricsCAD HWND through `PrintWindow(hwnd)`, cleaned its process environment, left no BricsCAD process/root dump, and the locally inspected image contained only the target host window. The same exact adapter binary (`A78FDC7F9E5300EEBA3E553D9C571F15E38075D8570FB00AE70C3679DCA3A991`) was packaged locally, installed with `OnCommand` registration, and passed a clean BricsCAD start plus `QS3DRUNTIMEPROBE` with `load_mode=DemandLoad`; its generated script contained no `NETLOAD`. Scope remains `source-build+runtime-smoke`; the full interactive matrix, save/reopen, multi-DWG isolation, `QS3DREGEN`/`QS3DREFRESH`, `QS3DFINISH`, legacy/explicit unit-binding lifecycle and generic/standalone-Append Interchange stale-confirmation scenarios were **NOT RUN**, so this item remains `IN_PROGRESS` and customer-release qualification remains false.
  Additional exact-SHA lifecycle evidence: `scripts/test-bricscad-v25-project-lifecycle.ps1` PASS at `604982a506aa07ba3eae047d282847333d529314` on BricsCAD `25.2.10`. Four repository-generated disposable DWG copies proved DWG `SaveComplete` sidecar persistence, cold-cache identity continuity, canonical existing-project binding, detached-snapshot immutability, distinct A/B project identity, isolated per-DWG mutations, a second persisted cold reload, absent-sidecar non-creation, and corrupt-sidecar fail-closed behavior with the corrupt file unchanged. The fixture SHA-256 was unchanged, final evidence contained no raw paths/ProjectIds/Handles/fingerprints, the state file was removed, and no BricsCAD process or probe environment remained. This narrows but does not close LOCAL-001: `QS3DREGEN`/`QS3DREFRESH`, `QS3DFINISH`, legacy/explicit unit binding, modeless refresh/export invariants, Interchange confirmation freshness, save/reopen UI flows and the full interactive/private-DWG matrix remain **NOT RUN**.
  Superseding the earlier **NOT RUN** status for these three commands: the expanded lifecycle runner PASSed at exact SHA `f627e9667e58c036df0be5b094a6f9cc494abaeb` on BricsCAD `25.2.10`. It executed the real `QS3DREGEN`, `QS3DREFRESH`, and `QS3DFINISH` commands in six phases: cold existing project plus absent-sidecar drawing for each command. Existing-project phases preserved canonical project identity and pending semantic mutation; REGEN/REFRESH cleared the probe Room's semantic dirty state; FINISH created every canonical Room Finish category with its Room dependency. All absent-sidecar phases remained non-creating with no cached/pending project state. The first run exposed passive Workspace refresh calling `GetOrCreate` on the absent-sidecar path; the follow-up lifecycle review also caught an intermediate read-only snapshot that could detach modeless edits on a cold cache. The final fix binds only an existing canonical project through `ExistingProjectMutationContext`, and the clean exact-SHA rerun passed all six phases, kept the generated fixture SHA-256 unchanged, removed state/environment/process residue, and emitted no raw path, ProjectId, Handle, fingerprint, or error text in marker/JSON evidence. LOCAL-001 remains `IN_PROGRESS` for unit binding, modeless refresh/export invariants, Interchange/Host Link confirmation paths, Save As UI coverage, and the full interactive/private-DWG matrix.
  Superseding the unit-binding **NOT RUN** status: the nine-phase lifecycle runner PASSed at exact SHA `1dfbf35fbc9745b880133f7ac0cdfb50887391b1` on BricsCAD `25.2.10`. Real `QS3DBQ` on a cold legacy project bound the native unit to the same canonical project, persisted it and left no pending state; real `QS3DBQ` on a supported-INSUNITS drawing without a sidecar resolved units but created neither cache nor QSDB; real `QS3DUNITS` on unresolved INSUNITS consumed a scope-validated one-shot automation confirmation, persisted a Meter project override and intentionally bootstrapped exactly one empty project without semantic elements. The run exposed and drove fixes for unit inspection calling `GetOrCreate` and for BQ XAML checkbox events mutating live column preferences during `InitializeComponent`. All nine phases passed, the generated fixture SHA-256 remained unchanged, cleanup/privacy checks passed, and no GitHub Actions ran. Normal user calls still use `Editor.GetKeywords`; physical keyboard/prompt UX remains **NOT RUN** in the interactive matrix. LOCAL-001 remains `IN_PROGRESS` for modeless refresh/export runtime invariants, Interchange/Host Link confirmation paths, Save As UI coverage, the physical `QS3DUNITS` prompt and the full interactive/private-DWG matrix.
  Warm-cache sidecar revision evidence (2026-08-15): the automation-only matrix PASSed on clean, committed and pushed exact SHA `cfc80fe80f1bf866fdec27111eb5fdf1977a3305` in BricsCAD `25.2.10` x64. Focused sidecar/Save/static/manual-CI/handoff gates, PowerShell AST, full Core smoke and the installed-reference V25 `Release|x64` build passed first with `0 warnings / 0 errors`; plugin/Core ProductVersion was `0.1.0-preview.10040+cfc80fe80f1bf866fdec27111eb5fdf1977a3305`, adapter SHA-256 `E06C477F04DC18546D89B8DC0C291783D4FFDF82820F2E8D0F68D8EC9C68CA68`, and Core SHA-256 `6F7220CF5318A1E70A1F09B59E449E64A16FF744A4DAF82A35CECB66CAFF0685`. With one canonical project cache kept warm, backup appearance, primary byte replacement and primary removal each made read-only access, canonical bind, existing-project mutation, Interchange confirmation and Save fail closed. Semantic state stayed unchanged, restoring the original bytes recovered the same canonical session, and no DWG write was requested. Fixture and disposable-copy SHA-256 stayed `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; zero process, environment, script, QSDB, backup, project-lock and drawing-lock residue was verified. The bounded runner/probe corrections only grouped cleanup paths atomically, finalized the exact host before DWG hashing, shortened private scratch names below net48 MAX_PATH, and emitted allowlisted failure stage/kind tokens; production persistence/Core behavior was not changed. This closes only the warm-cache revision row. LOCAL-001 remains `IN_PROGRESS` for the other current-candidate scenarios and customer-release qualification remains false.
- Current evidence reading rule: the later exact-SHA lifecycle paragraphs supersede only the baseline `NOT RUN` statements they name. They do not promote LOCAL-001 to `PASS`; all remaining scenarios in the final current-candidate block below stay `PENDING_LOCAL`.
- Related docs: `docs/LOCAL-V25-QUALIFICATION.md`; `docs/EXISTING-PROJECT-MUTATION-CONTEXT.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`; `scripts/test-bricscad-v25-project-lifecycle.ps1`; `scripts/test-bricscad-v25-sidecar-revision.ps1`; `scripts/preflight-save-project-lifecycle.py`
- Historical extension snapshot (superseded by the consolidated current-candidate scenario below):
- Scenario: Run `scripts/run-local-v25-qualification.ps1` from a clean exact SHA with the real V25 install directory; prove Core Release build, Core smoke, adapter exact-V25 build, NETLOAD, DemandLoad, command registration, save/reopen, and multi-DWG isolation. Cold-start/reopen a drawing with an existing `.qsdb`: true writes must bind the canonical same-ProjectId project, while one regeneration-based CSV/XLSX export and one modeless Door/Room refresh/export must use detached regenerated state and leave live project dirty/change-version/timestamp/audit state unchanged. Explicitly exercise `QS3DREGEN` and `QS3DREFRESH` after cache forget/reload: `QS3DREGEN` must bind the canonical existing project before regeneration; `QS3DREFRESH` may regenerate only after the same canonical binding. Repeat those commands with the sidecar/project absent: `QS3DREGEN` must refuse without creating/caching a replacement project, while `QS3DREFRESH` must remain a non-creating UI refresh with no semantic mutation. For `QS3DFINISH`, cancel/empty selection must return before canonical project bind/cache and leave project, finish, audit and CAD state unchanged. Then exercise `QS3DFINISH` with a selected existing Room after cache forget/reload and verify generated finish semantics stay on the canonical project; repeat with the sidecar/project absent and verify the command refuses without creating/caching a replacement project or finish Family/Element state. Exercise automatic legacy unit binding from a unit-dependent command such as `QS3DBQ`: on a valid legacy project with semantic elements it must bind/update/save the canonical project; on a drawing with supported INSUNITS but no QS3D project it must not create/cache a project merely while resolving units. Separately verify explicit `QS3DUNITS` still intentionally creates/saves project unit state when the user confirms a project override. Repeat ownership-dependent writes with the sidecar absent and verify refusal without leaving a replacement project. For `QS3DINTERCHANGEIMPORT`, review a policy plan, then forget/reload the project cache or replace/remove the sidecar before confirmation; freshness confirmation must refuse the stale plan without creating/caching a replacement project and without applying any import mutation. Exercise `QS3DLINKHOST` with an empty/cancelled selection before any semantic lookup: it must return without binding, creating, or caching a QS3D project. Then repeat with exactly one Door/WallOpening plus one valid wall/vách source after cache forget/reload: selection acquisition must complete first, the mutation must bind the canonical same-ProjectId project, and host-link regeneration/rollback behavior must remain intact; with the sidecar/project absent after selection, the command must fail closed without creating a replacement project or mutating CAD/semantic state. Also exercise the rebar selection boundary on the same exact candidate SHA. `QS3DREBAR3D` and `QS3DREBARTIES3D` are PICKFIRST-only: with no implied selection they must return without opening a new selection prompt and without binding, creating, or caching a QS3D project. `QS3DBEAMREBAR3D`, `QS3DREBARSTIRRUP3D`, `QS3DSLABREBAR3D`, `QS3DFOUNDATIONREBAR3D`, `QS3DWALLREBAR3D`, and `QS3DREBAR3DSHAPE` retain interactive selection fallback: cancel/empty selection must return before project binding/creation/cache. For each family, repeat a valid selected run after cache forget/reload and verify the mutation binds the canonical same-ProjectId project while existing generated-ownership, exact-replacement, rollback and UI-finalization behavior remains intact. Also qualify `QS3DGRIDNUMBERAUTO`: after selecting valid semantic Grid LINE sources, cancel independently at ordering-axis start/end, naming mode/start/padding/prefix/suffix, and decline/cancel the final confirmation. Read-only preview may inspect the detached existing sidecar, but every such exit must occur before canonical project bind/cache and must leave Grid names, `ChangeVersion`, audit and CAD state unchanged. On a cold-cache valid `.qsdb`, complete the preview and confirm: the command must then bind the canonical same-ProjectId project, re-resolve the selected semantic ownership, re-read the authoritative live LINE geometry, re-order using the confirmed axis, verify the preview ElementId/projected-coordinate plan is still current, and only then renumber. Change Grid ownership/project identity or move a selected Grid LINE across the ordering axis between preview and commit and verify freshness rejection occurs before any renumber/audit mutation.
- Evidence required: Exact QS3D SHA, Windows build, BricsCAD V25 build, .NET/MSBuild version, command/load results, cold-cache ProjectId continuity for true writes, `QS3DREGEN`/`QS3DREFRESH` existing-project and absent-sidecar lifecycle results with proof of no replacement project, `QS3DFINISH` cancel/empty proof of no canonical project bind/cache and no project/finish/audit/CAD mutation plus canonical-project success and absent-sidecar refusal/no-new-project/no-finish-mutation result, automatic legacy unit-binding existing-project persistence plus no-project/no-cache result, explicit `QS3DUNITS` bootstrap/persistence result, before/after live-state invariants for detached refresh/export, absent-sidecar refusal/no-new-project result, Interchange stale-confirmation refusal with proof of no replacement project/import mutation, `QS3DLINKHOST` empty/cancelled selection proof of no project bind/create/cache plus valid-selection canonical ProjectId continuity and absent-sidecar refusal with no host/CAD/semantic mutation, rebar selection matrix proving no-project-bind/create/cache on empty/cancel for all eight guarded 3D commands, proof that `QS3DREBAR3D`/`QS3DREBARTIES3D` do not gain an interactive prompt, canonical ProjectId continuity on successful selected runs, unchanged generated ownership/replacement/rollback behavior, `QS3DGRIDNUMBERAUTO` per-prompt/confirm cancel matrix proving no canonical bind/cache or Grid/audit mutation after detached preview, successful cold-cache canonical ProjectId continuity plus live authoritative LINE re-read, stale ProjectId/ownership/order-coordinate rejection before renumber, sanitized failure log if any.
- Related docs: `docs/LOCAL-V25-QUALIFICATION.md`; `docs/EXISTING-PROJECT-MUTATION-CONTEXT.md`; `scripts/preflight-room-finish-project-lifecycle.py`; `scripts/preflight-grid-auto-number-project-lifecycle.py`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Historical Room Auto extension snapshot (superseded by the consolidated current-candidate scenario below):
- Scenario: Run `scripts/run-local-v25-qualification.ps1` from a clean exact SHA with the real V25 install directory; prove Core Release build, Core smoke, adapter exact-V25 build, NETLOAD, DemandLoad, command registration, save/reopen, and multi-DWG isolation. Cold-start/reopen a drawing with an existing `.qsdb`: true writes must bind the canonical same-ProjectId project, while one regeneration-based CSV/XLSX export and one modeless Door/Room refresh/export must use detached regenerated state and leave live project dirty/change-version/timestamp/audit state unchanged. Explicitly exercise `QS3DREGEN` and `QS3DREFRESH` after cache forget/reload: `QS3DREGEN` must bind the canonical existing project before regeneration; `QS3DREFRESH` may regenerate only after the same canonical binding. Repeat those commands with the sidecar/project absent: `QS3DREGEN` must refuse without creating/caching a replacement project, while `QS3DREFRESH` must remain a non-creating UI refresh with no semantic mutation. For `QS3DFINISH`, cancel/empty selection must return before canonical project bind/cache and leave project, finish, audit and CAD state unchanged. Then exercise `QS3DFINISH` with a selected existing Room after cache forget/reload and verify generated finish semantics stay on the canonical project; repeat with the sidecar/project absent and verify the command refuses without creating/caching a replacement project or finish Family/Element state. Exercise automatic legacy unit binding from a unit-dependent command such as `QS3DBQ`: on a valid legacy project with semantic elements it must bind/update/save the canonical project; on a drawing with supported INSUNITS but no QS3D project it must not create/cache a project merely while resolving units. Separately verify explicit `QS3DUNITS` still intentionally creates/saves project unit state when the user confirms a project override. Repeat ownership-dependent writes with the sidecar absent and verify refusal without leaving a replacement project. For `QS3DINTERCHANGEIMPORT`, review a policy plan, then forget/reload the project cache or replace/remove the sidecar before confirmation; freshness confirmation must refuse the stale plan without creating/caching a replacement project and without applying any import mutation. Exercise `QS3DLINKHOST` with an empty/cancelled selection before any semantic lookup: it must return without binding, creating, or caching a QS3D project. Then repeat with exactly one Door/WallOpening plus one valid wall/vách source after cache forget/reload: selection acquisition must complete first, the mutation must bind the canonical same-ProjectId project, and host-link regeneration/rollback behavior must remain intact; with the sidecar/project absent after selection, the command must fail closed without creating a replacement project or mutating CAD/semantic state. Also exercise the rebar selection boundary on the same exact candidate SHA. `QS3DREBAR3D` and `QS3DREBARTIES3D` are PICKFIRST-only: with no implied selection they must return without opening a new selection prompt and without binding, creating, or caching a QS3D project. `QS3DBEAMREBAR3D`, `QS3DREBARSTIRRUP3D`, `QS3DSLABREBAR3D`, `QS3DFOUNDATIONREBAR3D`, `QS3DWALLREBAR3D`, and `QS3DREBAR3DSHAPE` retain interactive selection fallback: cancel/empty selection must return before project binding/creation/cache. For each family, repeat a valid selected run after cache forget/reload and verify the mutation binds the canonical same-ProjectId project while existing generated-ownership, exact-replacement, rollback and UI-finalization behavior remains intact. Also qualify `QS3DGRIDNUMBERAUTO`: after selecting valid semantic Grid LINE sources, cancel independently at ordering-axis start/end, naming mode/start/padding/prefix/suffix, and decline/cancel the final confirmation. Read-only preview may inspect the detached existing sidecar, but every such exit must occur before canonical project bind/cache and must leave Grid names, `ChangeVersion`, audit and CAD state unchanged. On a cold-cache valid `.qsdb`, complete the preview and confirm: the command must then bind the canonical same-ProjectId project, re-resolve the selected semantic ownership, re-read the authoritative live LINE geometry, re-order using the confirmed axis, verify the preview ElementId/projected-coordinate plan is still current, and only then renumber. Change Grid ownership/project identity or move a selected Grid LINE across the ordering axis between preview and commit and verify freshness rejection occurs before any renumber/audit mutation. Also qualify `QS3DROOMAUTO` preview-to-commit freshness. On an initially projectless drawing, prepare valid accepted Room boundary geometry, then make a valid QS3D project/sidecar visible before the command reaches commit: the command must refuse and require rerun before `GetOrCreate`, `ProjectStateSnapshot`, Room/audit mutation, regeneration or CAD-side follow-up. With an existing project, change `RoomBoundaryToleranceM`, `RoomBoundaryArcSagittaM`, `RoomBoundarySplineChordM`, `RoomBoundaryMinimumAreaM2`, or the effective drawing-unit policy after boundary selection/diagnostics but before mutation and verify fail-closed refusal. The successful no-project path must remain creation-capable only when accepted topology exists and no project appears before commit.
- Evidence required: Exact QS3D SHA, Windows build, BricsCAD V25 build, .NET/MSBuild version, command/load results, cold-cache ProjectId continuity for true writes, `QS3DREGEN`/`QS3DREFRESH` existing-project and absent-sidecar lifecycle results with proof of no replacement project, `QS3DFINISH` cancel/empty proof of no canonical project bind/cache and no project/finish/audit/CAD mutation plus canonical-project success and absent-sidecar refusal/no-new-project/no-finish-mutation result, automatic legacy unit-binding existing-project persistence plus no-project/no-cache result, explicit `QS3DUNITS` bootstrap/persistence result, before/after live-state invariants for detached refresh/export, absent-sidecar refusal/no-new-project result, Interchange stale-confirmation refusal with proof of no replacement project/import mutation, `QS3DLINKHOST` empty/cancelled selection proof of no project bind/create/cache plus valid-selection canonical ProjectId continuity and absent-sidecar refusal with no host/CAD/semantic mutation, rebar selection matrix proving no-project-bind/create/cache on empty/cancel for all eight guarded 3D commands, proof that `QS3DREBAR3D`/`QS3DREBARTIES3D` do not gain an interactive prompt, canonical ProjectId continuity on successful selected runs, unchanged generated ownership/replacement/rollback behavior, `QS3DGRIDNUMBERAUTO` per-prompt/confirm cancel matrix proving no canonical bind/cache or Grid/audit mutation after detached preview, successful cold-cache canonical ProjectId continuity plus live authoritative LINE re-read, stale ProjectId/ownership/order-coordinate rejection before renumber, `QS3DROOMAUTO` no-project-preview/project-appears refusal with proof of no project creation/cache replacement and no Room/audit/CAD mutation, per-setting and effective-unit freshness rejection before snapshot/mutation, successful accepted-topology no-project creation path, sanitized failure log if any.
- Historical disposition: `PENDING_LOCAL`; the authoritative current disposition is recorded after the consolidated FieldMerge/current-candidate scenario below.
- Related docs: `docs/LOCAL-V25-QUALIFICATION.md`; `docs/EXISTING-PROJECT-MUTATION-CONTEXT.md`; `scripts/preflight-room-finish-project-lifecycle.py`; `scripts/preflight-grid-auto-number-project-lifecycle.py`; `scripts/preflight-room-auto-project-lifecycle.py`; `docs/ROOM-AUTO-PREVIEW-COMMIT-FRESHNESS.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Authoritative current-candidate extension:
- Source-side status: REMOTE_DONE for the dedicated reviewed native FieldMerge source path: `QS3DINTERCHANGEFIELDMERGE` previews the existing target through `TryGetReadOnly`, completes all 11 precedence prompts and the final confirmation before canonical mutation binding, rechecks ProjectId/drawing fingerprint/ChangeVersion, then `InterchangeFieldMergeImportService` performs generated-dependent native prepare -> exact authorized Core field apply -> ownership metadata sweep -> CAD commit with semantic snapshot rollback on pre-commit failure. `scripts/preflight-interchange-field-merge-execution.py` guards this source contract. Exact licensed V25 transaction/failure/Undo/save-reopen/multi-DWG proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`.
- Scenario: Run `scripts/run-local-v25-qualification.ps1` from a clean exact SHA with the real V25 install directory; prove Core Release build, Core smoke, adapter exact-V25 build, NETLOAD, DemandLoad, command registration, save/reopen, and multi-DWG isolation. Cold-start/reopen a drawing with an existing `.qsdb`: true writes must bind the canonical same-ProjectId project, while one regeneration-based CSV/XLSX export and one modeless Door/Room refresh/export must use detached regenerated state and leave live project dirty/change-version/timestamp/audit state unchanged. Explicitly exercise `QS3DREGEN` and `QS3DREFRESH` after cache forget/reload: `QS3DREGEN` must bind the canonical existing project before regeneration; `QS3DREFRESH` may regenerate only after the same canonical binding. Repeat those commands with the sidecar/project absent: `QS3DREGEN` must refuse without creating/caching a replacement project, while `QS3DREFRESH` must remain a non-creating UI refresh with no semantic mutation. For `QS3DFINISH`, cancel/empty selection must return before canonical project bind/cache and leave project, finish, audit and CAD state unchanged. Then exercise `QS3DFINISH` with a selected existing Room after cache forget/reload and verify generated finish semantics stay on the canonical project; repeat with the sidecar/project absent and verify the command refuses without creating/caching a replacement project or finish Family/Element state. Exercise automatic legacy unit binding from a unit-dependent command such as `QS3DBQ`: on a valid legacy project with semantic elements it must bind/update/save the canonical project; on a drawing with supported INSUNITS but no QS3D project it must not create/cache a project merely while resolving units. Separately verify explicit `QS3DUNITS` still intentionally creates/saves project unit state when the user confirms a project override. Repeat ownership-dependent writes with the sidecar absent and verify refusal without leaving a replacement project. For `QS3DINTERCHANGEIMPORT`, review a policy plan, then forget/reload the project cache or replace/remove the sidecar before confirmation; freshness confirmation must refuse the stale plan without creating/caching a replacement project and without applying any import mutation. For `QS3DINTERCHANGEFIELDMERGE`, use a valid snapshot against a cold-cache existing project and cancel independently at each of the 11 field-group precedence prompts and at the final confirmation: every cancel must remain read-only, must not canonical-bind/cache a project, and must leave semantic/native/audit state unchanged. Then confirm a reviewed UseSource-containing plan and verify the command canonical-binds the same ProjectId/drawing fingerprint/ChangeVersion before native prepare. Replace/remove/reload the sidecar/project or change target fingerprint/version after review but before confirmation and verify fail-closed refusal before native erase or semantic mutation. Inject failures after generated-dependent native Prepare, during Core authorized apply, after metadata sweep and before native commit; prove the CAD transaction aborts and `ProjectStateSnapshot` restores the semantic target with no cross-DWG or foreign-object deletion. A successful FieldMerge must leave affected generated outputs explicitly invalidated for rebuild; it must not silently auto-build3D/cut/rebar/curtain/grid/save. Exercise `QS3DLINKHOST` with an empty/cancelled selection before any semantic lookup: it must return without binding, creating, or caching a QS3D project. Then repeat with exactly one Door/WallOpening plus one valid wall/vách source after cache forget/reload: selection acquisition must complete first, the mutation must bind the canonical same-ProjectId project, and host-link regeneration/rollback behavior must remain intact; with the sidecar/project absent after selection, the command must fail closed without creating a replacement project or mutating CAD/semantic state. Also exercise the rebar selection boundary on the same exact candidate SHA. `QS3DREBAR3D` and `QS3DREBARTIES3D` are PICKFIRST-only: with no implied selection they must return without opening a new selection prompt and without binding, creating, or caching a QS3D project. `QS3DBEAMREBAR3D`, `QS3DREBARSTIRRUP3D`, `QS3DSLABREBAR3D`, `QS3DFOUNDATIONREBAR3D`, `QS3DWALLREBAR3D`, and `QS3DREBAR3DSHAPE` retain interactive selection fallback: cancel/empty selection must return before project binding/creation/cache. For each family, repeat a valid selected run after cache forget/reload and verify the mutation binds the canonical same-ProjectId project while existing generated-ownership, exact-replacement, rollback and UI-finalization behavior remains intact. Also qualify `QS3DGRIDNUMBERAUTO`: after selecting valid semantic Grid LINE sources, cancel independently at ordering-axis start/end, naming mode/start/padding/prefix/suffix, and decline/cancel the final confirmation. Read-only preview may inspect the detached existing sidecar, but every such exit must occur before canonical project bind/cache and must leave Grid names, `ChangeVersion`, audit and CAD state unchanged. On a cold-cache valid `.qsdb`, complete the preview and confirm: the command must then bind the canonical same-ProjectId project, re-resolve the selected semantic ownership, re-read the authoritative live LINE geometry, re-order using the confirmed axis, verify the preview ElementId/projected-coordinate plan is still current, and only then renumber. Change Grid ownership/project identity or move a selected Grid LINE across the ordering axis between preview and commit and verify freshness rejection occurs before any renumber/audit mutation. Also qualify `QS3DROOMAUTO` preview-to-commit freshness. On an initially projectless drawing, prepare valid accepted Room boundary geometry, then make a valid QS3D project/sidecar visible before the command reaches commit: the command must refuse and require rerun before `GetOrCreate`, `ProjectStateSnapshot`, Room/audit mutation, regeneration or CAD-side follow-up. With an existing project, change `RoomBoundaryToleranceM`, `RoomBoundaryArcSagittaM`, `RoomBoundarySplineChordM`, `RoomBoundaryMinimumAreaM2`, or the effective drawing-unit policy after boundary selection/diagnostics but before mutation and verify fail-closed refusal. The successful no-project path must remain creation-capable only when accepted topology exists and no project appears before commit. Also exercise `QS3DSAVE`: on a cold-cache drawing with a valid existing `.qsdb`, Save must bind and persist the canonical same-ProjectId project; on a drawing with no QS3D project/sidecar, Save must fail closed without creating, caching, or persisting a replacement project; after cache forget with a valid sidecar it must rebind the existing project rather than creating a default one.
- Evidence required: Exact QS3D SHA, Windows build, BricsCAD V25 build, .NET/MSBuild version, command/load results, cold-cache ProjectId continuity for true writes, `QS3DREGEN`/`QS3DREFRESH` existing-project and absent-sidecar lifecycle results with proof of no replacement project, `QS3DFINISH` cancel/empty proof of no canonical project bind/cache and no project/finish/audit/CAD mutation plus canonical-project success and absent-sidecar refusal/no-new-project/no-finish-mutation result, automatic legacy unit-binding existing-project persistence plus no-project/no-cache result, explicit `QS3DUNITS` bootstrap/persistence result, before/after live-state invariants for detached refresh/export, absent-sidecar refusal/no-new-project result, Interchange stale-confirmation refusal with proof of no replacement project/import mutation, `QS3DINTERCHANGEFIELDMERGE` per-prompt/final-confirm cancel proof of no canonical bind/cache or semantic/native/audit mutation, successful cold-cache canonical ProjectId/fingerprint/ChangeVersion continuity, stale/replaced-target refusal before erase/mutation, native Prepare/Core apply/metadata sweep/pre-commit failure-injection evidence proving CAD abort plus semantic snapshot restoration, successful explicit invalidation/rebuild-required result and no foreign/cross-DWG deletion, `QS3DLINKHOST` empty/cancelled selection proof of no project bind/create/cache plus valid-selection canonical ProjectId continuity and absent-sidecar refusal with no host/CAD/semantic mutation, rebar selection matrix proving no-project-bind/create/cache on empty/cancel for all eight guarded 3D commands, proof that `QS3DREBAR3D`/`QS3DREBARTIES3D` do not gain an interactive prompt, canonical ProjectId continuity on successful selected runs, unchanged generated ownership/replacement/rollback behavior, `QS3DGRIDNUMBERAUTO` per-prompt/confirm cancel matrix proving no canonical bind/cache or Grid/audit mutation after detached preview, successful cold-cache canonical ProjectId continuity plus live authoritative LINE re-read, stale ProjectId/ownership/order-coordinate rejection before renumber, `QS3DROOMAUTO` no-project-preview/project-appears refusal with proof of no project creation/cache replacement and no Room/audit/CAD mutation, per-setting and effective-unit freshness rejection before snapshot/mutation, successful accepted-topology no-project creation path, `QS3DSAVE` cold-cache canonical same-ProjectId persistence plus absent-project refusal proving no replacement project/cache/sidecar creation, sanitized failure log if any.
- Evidence: PENDING_LOCAL. Exact current-main-derived candidate
  `8b5ece70d1aaf489e14ac68d7606053def1d08ba` (parent
  `6aa3270fb71b68d3039e19569d2d89e74e294712`) passed manual-CI/generic
  preflight, all `960/960` aggregate gates, Core Release build and deterministic
  smoke, installed-reference V25 `Release|x64` build with `0 warnings / 0
  errors`, and offline WPF qualification on 2026-08-21. Adapter/Core
  ProductVersion matched
  `0.1.0-preview.10081+8b5ece70d1aaf489e14ac68d7606053def1d08ba`;
  adapter/Core SHA-256 values were
  `292BBFFF4903A4C596165C4EECAB7BCFED4BA177E01085343F94124A074D5AB9` /
  `BE49748E84C58C61B02CD6F096A74C9E760470AC4A1E78F14264E3C51FD4D27A`.
  The official NETLOAD/Ribbon/Palette step is `NO_RESULT`, not a product
  verdict: its `CodexSandboxOffline` host timed out after 120 seconds before
  `QS3DRUNTIMEPROBE`, emitted no runtime metadata, cleaned its launched process,
  and a post-run scan found zero BricsCAD processes. The earlier licensed PASS
  at `7820b72f894534443b53e315608f6a2228533248` is precursor-only and cannot
  qualify this candidate. All current-candidate native/interactive scenarios
  in this block therefore remain `PENDING_LOCAL`.
  The publication carrier was subsequently refreshed onto then-current `main`
  `e4bfb1fb59c61f03a47ae99a196dfeed3b2b1ad6` while retaining the prior remote
  carrier as second parent. That combined source tree passed `git diff --check`,
  all `960/960` aggregate gates, registration of all 838 runnable smoke classes,
  and the Core deterministic smoke suite with `ALL PASS`. This does not replace
  the exact licensed evidence boundary above; native/interactive acceptance
  remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`.
  A final remote audit observed `main`
  `ccf3c8e182415aab2dca5a7d7f363fb56d0bf97a`, ten commits beyond that
  validated carrier. The five changed semantic documentation/health
  source/test/preflight paths do not overlap this lane's two documentation
  paths. Git HTTPS and the managed GitHub write API were both unavailable, so
  the local carrier remains unpushed and must be refreshed by the next
  write-capable session before publication.
  Continuation host activation reached a responsive `BricsCAD Launcher` in the
  interactive Windows session and successfully dismissed it through the real
  focused 2D Drafting action. The test-owned process then remained responsive
  without creating a CAD/document HWND under the managed
  `CodexSandboxOffline` token. No QS3D DLL/command/DWG/customer assertion ran;
  all owned processes were removed and the final BricsCAD count was zero. This
  is a narrower post-launcher/pre-CAD-UI `NO_RESULT`, not product-failure or
  `LOCAL_PASS` evidence. Git HTTPS and GitHub write-API retries remained blocked,
  At the capability retry, remote `main` and the canonical remote branch were
  respectively `ccf3c8e182415aab2dca5a7d7f363fb56d0bf97a` and
  `7820b72f894534443b53e315608f6a2228533248` with no open PR. Before closeout,
  `main` advanced six more commits to
  `afff082096998fa404f08a5e29bcfd9fbc3830dd`; a future write-capable
  continuation must repin that then-current branch rather than publish this
  stale local carrier unchanged.
  On 2026-08-25 the unchanged canonical runner was invoked exactly once from a
  clean detached checkout of the post-#3985 source-ready SHA
  `d52a0065a3f63575885761bc59fab2c08a32f4a4`. Exact-SHA/manual-CI/generic
  source checks, all `1043/1043` aggregate gates, Core Release and deterministic
  smoke, the V25 `Release|x64` adapter build with `0 warnings / 0 errors`, and
  offline WPF qualification passed. The licensed NETLOAD/Ribbon/Palette row is
  `NO_RESULT`: one concurrently opened BricsCAD V25 process appeared before the
  hosted boundary, so the canonical dedicated-runner precondition stopped the
  attempt before launching its own host. The worker performed one blocker audit,
  did not touch that process, and did not rerun. DemandLoad and the installed
  loader hash were restored, the exact-SHA tree remained clean, and no source or
  runner file changed. This is environmental interference, not
  `SOURCE_FIX_REQUIRED`; post-#3985 Interchange continuation and the remaining
  current-candidate matrix remain `PENDING_LOCAL` under #3924/#72.
- Related docs: `docs/LOCAL-V25-QUALIFICATION.md`; `docs/EXISTING-PROJECT-MUTATION-CONTEXT.md`; `docs/INTERCHANGE-FIELD-PRECEDENCE.md`; `scripts/preflight-interchange-field-merge-execution.py`; `scripts/preflight-room-finish-project-lifecycle.py`; `scripts/preflight-grid-auto-number-project-lifecycle.py`; `scripts/preflight-room-auto-project-lifecycle.py`; `docs/ROOM-AUTO-PREVIEW-COMMIT-FRESHNESS.md`; `scripts/preflight-save-project-lifecycle.py`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-25

## LOCAL-002 — Curtain whole-command atomicity and native panels

- Priority: P0
- Status: OPEN
- Area: Curtain Wall
- Why local: Final proof needs native V25 Solid3d/nested-transaction behavior, injected failures across host/frame phases, modeless Curtain Hub/regeneration behavior, panel materialization, and save/reopen ownership verification.
- Source-side status: REMOTE_DONE for canonical Curtain Hub Family-property writes at source commit `ea9a15e02057425099f8f774b73448bde8e21fbc`. REMOTE_DONE in current source for `QS3DCURTAIN3D` whole-command source atomicity: selection remains ahead of canonical project binding, `ProjectStateSnapshot` is captured before regeneration, and six ordered LINE/path host/frame/panel phases execute under one outer native transaction. Failures before the outer commit restore semantic state instead of reporting a partial commit. Native panels use bounded opening-clipped pieces, independent `GeneratedCurtainPanelHandles`, complete-build/XData/fingerprint/stale/Health/Release/Locate wiring, and metadata-orphan fail-closed replacement. `scripts/preflight-curtain-orchestration-boundary.py` and `scripts/preflight-curtain-native-panels.py` guard this static source contract. Bounded exact-SHA licensed evidence now exists for every P01-P12 cell, including LINE/opening, straight/bulged path, stale/rebuild, ownership/budget refusal, all seven pre-commit rollback boundaries, both post-commit warning boundaries, Workspace/Health/Release selection, Undo/Redo/save-reopen/rebuild and the two-DWG modeless Curtain Hub lifecycle. P10 is bounded `LOCAL_PASS` at `6c56a10add09a8ab4a844178c0f264d0f109cf73`; P11 is bounded `LOCAL_PASS` at `58dbc1b80d45774593303856285dd69f81c7e1a2`. The Family-editor row is bounded `LOCAL_PASS` at `cf5f4a1938fcff403ffe8e2cd30184a6f1f7ce9d`; broad H.1 proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`. Local execution must use an exact merged `main` SHA and must not promote these bounded cells to broad LOCAL-002 parity.
- Scenario: Qualify atomic rollback for `QS3DCURTAIN3D` by injecting failures during semantic regeneration and every LINE/path host, frame and panel phase present in the exact tested SHA. Before the outer native commit, every injected failure must leave no partial host/frame/panel CAD commit and must restore the command semantic snapshot; after the outer commit, fingerprint/UI failures are warnings and must not pretend geometry was rolled back. Qualify panel-by-panel glass from `CurtainWallDetailPlanner.Panels` using the exact P01-P12 source-shape, opening-clipping, bounds, ownership-conflict, stale/rebuild, selection/health/release, Undo and save/reopen matrix in `docs/CURTAIN-NATIVE-PANELS.md`. Also qualify the modeless `QS3DCURTAIN` Family Save path on a clean current project: use a GlassWall Family with at least one inherited instance and one explicit instance override, change numeric values plus `Material`/`CurtainFrameMaterial`, and verify inherited values follow the Family while explicit overrides remain unchanged and are not dirtied solely by the Family change. Attempt a material/frame-material value longer than the canonical 1000-character Family-property limit and verify Save fails/rolls back without semantic/native partial mutation. With the project clean and all form values already equal to the Family, Save again and verify no `ChangeVersion`/`UpdatedUtc` advance attributable solely to a no-op Family write; any regeneration must correspond only to genuinely pre-existing dirty work. Then save/reopen and verify the accepted Family values and generated Curtain output remain consistent.
- Evidence required: Exact final tested `main` SHA containing the whole-command atomicity diff plus source commit-equivalent `ea9a15e02057425099f8f774b73448bde8e21fbc`; Windows/BricsCAD V25 build; injected-failure matrix for semantic regeneration and every LINE/path host/frame/panel phase present in that SHA; before/after native object counts and semantic snapshot/version evidence proving no pre-commit partial state; post-commit fingerprint/UI warning behavior; panel ownership/count and opening/door clipping checks; inherited-instance versus explicit-override before/after property and dirty-state evidence; >1000-character Material/FrameMaterial rejection with before/after semantic/native state; clean no-op Save before/after `ChangeVersion` and `UpdatedUtc`; save/reopen result; no foreign-object deletion.
- Partial local evidence: P01 basic LINE/no-opening was requalified at exact clean SHA `53a4490f245774e9253d24ba70799b4311ff7e12` on BricsCAD `25.2.10` x64 with plugin SHA-256 `644E24154161ADF0DE31A49E17FBB0FF65BB1B9A6251EC11594EA1E2E4924EAF`. The exact adapter produced one backing host, ten frame solids and fifteen panel solids; panel metadata count matched, ownership sets were disjoint, panel Health had zero blocking issues, and Locate resolved a selected panel to one canonical GlassWall. The repository-generated disposable DWG SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`, no sidecar remained, the private `.scr` was deleted and launched-process cleanup was verified. This is a bounded P01 result only; later bullets supersede P02-P08. P09-P12, Undo, save-reopen, multi-DWG and Family editor cases remain `PENDING_LOCAL`.
- P02 local evidence: the runner uses synthetic legacy/no-Level LINE partial and complete-empty cases. Its first exact attempt at `af0aec7f` produced the old coarse marker; the later V2 `failure_phase`/`failure_code` result at `7c160de66de68c811282f4cd460e927370e454cd` isolated the duplicate negative half-extent placement at `DOOR_NATIVE_GEOMETRY / STATE_REJECTED`. The centered-box correction was then rerun on a fresh ordinary copy at clean exact SHA `7b4a379da15c8c0bed60536bc0ccca7334eb4712`. BricsCAD `25.2.10` loaded exact x64 Release adapter SHA-256 `25B6A40F120028CED160F5F04362FFAE1FBEA25E0A850CEE45860E761559B53F`. The Door case had 15 source cells, 16 positive output fragments, one fully removed cell, five partially clipped cells, 16/16 unique native/Core-plan matches and zero positive native/opening intersections. The WallOpening case removed all 15 cells with healthy `Complete`, opening-aware zero-output/zero-handle state. Ownership remained disjoint, panel Health had zero issues, Locate resolved one panel to one canonical owner, the source geometry remained unchanged, and the disposable DWG hash stayed `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. Process/script/sidecar/backup cleanup passed. P02 is therefore bounded `LOCAL_PASS`; P03-P12 and overall LOCAL-002 remain `PENDING_LOCAL`.
- P06 local evidence: the bounded ownership-failure matrix passed at clean exact SHA `7c41ff875813a7773868240e8f79d22060eca196` on BricsCAD `25.2.10` with x64 Release adapter SHA-256 `309E934D12581DCDD97D104BA46F13F2F5A03595B2C005DBE3B22FF5FB0E6DB6`. Six synthetic legacy/no-Level LINE GlassWalls produced 90 baseline panels. Missing expected panel, duplicate canonical alias, foreign/unmarked live solid and cross-owner claim each refused before erase/append while preserving semantic metadata, surviving panels, unrelated owners and the native entity set. A clean control then removed its complete old set and produced 21 valid marked panels with zero panel Health issues. The generated disposable DWG SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; process, script, drawing-lock, sidecar and backup cleanup passed. This is bounded `LOCAL_PASS` for P06 only; later bullets supersede P07-P08. P09-P12, Undo/save-reopen/multi-DWG and overall LOCAL-002 remain `PENDING_LOCAL`.
- P07 local evidence: the bounded later-element budget/provenance rollback matrix passed at clean exact SHA `ef8656362aed5f5eb060bd62383d102f6302b125` on BricsCAD `25.2.10` with x64 Release adapter SHA-256 `5805F8E441D3EBE522E9E9ECC0A8480CD0F3B18E31F447D21B5F57CA4339770B`. Two synthetic legacy/no-Level LINE GlassWalls produced 30 baseline panels. A later owner independently exceeded the 4096-panel ceiling, linked a Door whose semantic source set was empty and linked an off-host live Door. Each production attempt refused while the whole semantic project, Current Space Handle/type/extents state and both old panel sets were unchanged. A valid two-owner control then removed the old sets and produced 36 healthy marked panels. The generated disposable DWG SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; process, script, drawing-lock, sidecar and backup cleanup passed. This is bounded `LOCAL_PASS` for P07 only; the next bullet supersedes P08. P09-P12, post-commit warnings, Undo/save-reopen/multi-DWG and overall LOCAL-002 remain `PENDING_LOCAL`.
- P08 local evidence: the seven-boundary mixed LINE/open-POLYLINE matrix passed at clean exact SHA `a025b9aa20a919ae585ddab2700055389e38eb1c` on BricsCAD `25.2.10` with x64 Release adapter SHA-256 `E18A82EE66631D091EC78CD8E65E864DAD03BD6E8A4031C1BD572AAB268651A1`. The baseline contained 63 generated source/host/frame/panel objects. One-shot failures after semantic regeneration and each of the six LINE/path host/frame/panel phases independently preserved the exact semantic project, Current Space Handle/type/extents state, source geometry and complete old generated sets. The final valid control removed the old sets and created 87 healthy objects with zero panel Health issues. The disposable DWG SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; process, private script, drawing-lock, sidecar and backup cleanup passed. This is bounded `LOCAL_PASS` for P08 only; P09-P12, post-commit warning behavior, Undo/save-reopen/multi-DWG and overall LOCAL-002 remain `PENDING_LOCAL`.
- P09 local evidence: both post-commit warning boundaries passed at clean exact SHA `76a1e760c78f1146fa528dcf11e906fecaa532e0` on BricsCAD `25.2.10` with strict x64 Release build `0 warnings / 0 errors` and adapter SHA-256 `C61A27C78B3248D70BA3DA4E57854AF8F3B3F4B41CA0E31DF93A82653F0AE708`. The 30-object baseline was replaced by 34 committed objects when live-fingerprint stamping was injected; the complete old set disappeared and Health reported the exact frame/panel missing-live-fingerprint review state. Clean recovery produced 34 healthy objects. The injected UI-refresh failure then retained a complete 38-object committed replacement, valid fingerprints and zero Health issues. Source geometry and disposable DWG SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E` were preserved; process, private script, drawing-lock, sidecar and backup cleanup passed. This is bounded `LOCAL_PASS` for P09 only; P10-P12, Undo/save-reopen/multi-DWG and overall LOCAL-002 remain `PENDING_LOCAL`.
- P10 source-ready handoff: `WorkspacePanel.TryResolveSemanticSelection(...)` now delegates all selected source/generated-host/physical-cut/frame/panel Handles to `SemanticHandleOwnershipResolver.Resolve(...)`, so `GeneratedCurtainPanelHandles` and `GeneratedCurtainFrameHandles` use the same dynamic owner-slot and ambiguity contract as Locate and the runtime probes. Workspace still rejects blank/duplicate Handles, unknown or mixed native objects, ambiguous ownership, and multiple CAD references owned by the same semantic element. The additive `QS3DCURTAINP10*` probe plus `scripts/test-bricscad-v25-curtain-panel-workspace-review.ps1` now drive one generated panel through production `QS3DINSPECT`, the visible Workspace canonical GlassWall/Family/Instance review, `QS3DHEALTHALL` and `QS3DRELEASECHECK`; the runner binds a clean exact SHA, synthetic copy, process/script/sidecar/DWG/UI-layout cleanup and sanitized marker. A clean attempt on exact SHA `337e97d32b6642b3a3d013596ffa1545df168999` and BricsCAD `25.2.10` reached the 900-second startup timeout without any marker or launcher handoff; it is `NO_RESULT`, not a product verdict. Process/script/private-state/DWG/UI-layout cleanup all passed and the disposable DWG returned to repository SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. P10 remains `SOURCE_READY / PENDING_LOCAL` until a fresh initialized-profile run reaches the sanitized production marker, and one P10 result cannot promote overall LOCAL-002.
- P10 diagnostic refinement: exact SHA `4de61a97362003072e0918b7a35e12f53671f2e6` and adapter SHA-256 `BC8C4F9B513DCE615832E687FE4294A0716C76A1C7A8BCFA6E62D8401A897A1E` passed the generic V25 runtime probe, but both the responsive-host P10 run and a P01 diagnostic sharing Direct Draw GlassWall -> prepare -> Curtain3D produced no marker before their guards. Cleanup and repository drawing-hash preservation passed, so this is still `NO_RESULT`, not a product verdict. The P10 probe/runner now records only an allowlisted coarse progress phase after each production command boundary to isolate the retained command on the next clean run; P10 and overall LOCAL-002 remain `PENDING_LOCAL`.
- P10 checkpoint diagnosis: exact SHA `7735c3cb756fb13b9ad1fbbf3c845316c35f18db` reached `direct_draw_complete`, isolating a runner-only environment mismatch before `QS3DCURTAINPANELPREPARE`. P10 now supplies and restores the shared prepare command's required `QS3D_CURTAIN_PANEL_RESULT/NONCE` values and cleans its private result path; production Curtain code is unchanged. Drawing/script/private-file restoration passed, but a separate BricsCAD process appeared during cleanup, so the run is diagnostic `NO_RESULT` and P10/overall LOCAL-002 remain `PENDING_LOCAL` until a fresh exact-SHA rerun.
- P10 empty-partition diagnosis/source correction: a clean run on exact SHA `2a79e5d165373d34e56a6532a9931cce238bc786` and adapter SHA-256 `774E7A961D7781346A5C1C2FDBA857ED465C79012FC62A48364C08758E9F99CE` advanced to `source_selection_prepared` and timed out before `curtain_build_complete`; process/script/private-state/DWG/UI-layout cleanup all passed and the generated disposable DWG returned to SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. Audit proved `QS3DCURTAIN3D` invoked absent LINE/path partitions and could fall back to an unrelated selection prompt. Issue `#1106` / PR `#1109` merged the bounded per-partition guard at `50d074eb579a3e9503b623fd37fd50a4e82c5b9b`, preserving all six phase hooks, the outer transaction, rollback/Undo and post-commit boundaries. Exact current `main` `16d47ef46c4a955587f398d5597fb84ebce32c2e` builds V25 Release|x64 with zero warnings/errors; focused P10/empty-partition gates pass. This remains `SOURCE_READY / PENDING_LOCAL`, not a runtime PASS, until a fresh zero-process exact-SHA run completes Workspace/Health/Release and cleanup.
- P10 post-fix zero-process rerun: clean exact SHA `82901db277eba9e685ebc356e97375138d0d538d`, BricsCAD `25.2.10` x64 and adapter SHA-256 `1DB2A88E7769BF53398652811CC1EFBBB54A975A286A374B095E03BB6C971DA1` passed the installed-reference V25 Release|x64 build, Core smoke and focused P10/empty-partition/native-panel gates. The guarded licensed runner still timed out after 900 seconds at final allowlisted phase `source_selection_prepared`, with a null marker and without reaching `curtain_build_complete`. Process/script/private-state/DWG/UI-layout cleanup passed, launcher handoffs and sidecars were zero, and the disposable DWG restored to SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. Issue `#1106` is reopened for a remote claim-first audit of current nested selection/UI retention after canonical selection. This is `NO_RESULT`; P10 and overall LOCAL-002 remain `PENDING_LOCAL`.
- P11 source-ready handoff: the guarded two-process Undo/save-reopen/rebuild probe ran on clean exact SHA `484a9c686a10edd4f04d7edf135031b2328c8c29`, BricsCAD `25.2.10` and x64 Release adapter SHA-256 `4EDB6F3B06B0AC7744CEE760AEE576CE4EC5AF689400C6940CE5BDB721050F9B`. Native Undo produced the sanitized verdict `native_undo / SEMANTIC_NATIVE_DIVERGENCE`; it restored CAD without the matching in-memory semantic owner state, so that run remains historical FAIL evidence. Redo, cold reopen and ownership-scoped rebuild were coherent; reopened/rebuilt counts were both 1 host / 10 frames / 15 panels; source and unrelated sentinel were preserved; old output was removed, replacement ownership was disjoint and Health issues were zero. Process, private-script, QSDB, native-backup and drawing-lock cleanup passed and the disposable DWG was restored byte-for-byte to SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. PR `#1035` merged the bounded correction at `884f2059fa520439640d95c8c8782cf5f160cdfa`: known Curtain revision transitions now restore the exact selected-owner/project persistence stamp without `Touch()` or audit mutation, finalize the committed after-state after live fingerprinting, and clear history on project reload/forget. Focused gate, Core smoke and installed-reference V25 compile passed remotely; no BricsCAD runtime was run. Rerun the unchanged guarded P11 probe/runner on the final exact merged `main` SHA. Keep issue `#987`, P11 and overall LOCAL-002 open/`PENDING_LOCAL` until that complete run passes; the next bullet supersedes earlier P12-pending wording.
- P10 final local evidence: the guarded Workspace/Health/Release flow passed at clean exact SHA `6c56a10add09a8ab4a844178c0f264d0f109cf73` on BricsCAD `25.2.10`. Exact generated-panel reselection survived every modal boundary; Workspace resolved the canonical GlassWall/Family/Instance, aggregate Health was clean and Release Check was ready. Process, script, private-state, drawing and UI-layout cleanup passed. This is bounded `LOCAL_PASS` for P10 only.
- P11 final local evidence: the corrected grouped Undo/Redo runner passed at clean exact SHA `58dbc1b80d45774593303856285dd69f81c7e1a2` on BricsCAD `25.2.10` with adapter SHA-256 `201AF99477BD4AA96111A69BF89F883F5870DDFFE26F5621C5AEB476B0D455C1`. Undo removed the complete generated after-set, restored the semantic before-state and preserved source/sentinel state; Redo restored coherent after-state. Cold reopen and ownership-scoped rebuild were coherent at 1 host / 10 frames / 15 panels; rebuilt output replaced the old set disjointly and Health issues were zero. Process, script, sidecar, drawing-lock, native-backup and restoration cleanup passed; the disposable DWG returned to SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. This is bounded `LOCAL_PASS` for P11; historical failures above remain diagnostic provenance only.
- P12 local evidence: the guarded multi-DWG/modeless probe passed at clean exact `main` SHA `67426427d67490b246eb035af1bc19c930789686`, BricsCAD `25.2.10` and x64 Release adapter SHA-256 `85D36541D52A119DCCD031D0555EAB3EE097CB0E9D409A24B5F4A3B81A48DC51`. With the real A-bound Curtain Hub open, routed Refresh and Frame Health command events both refused while B was active; A and B project stamps stayed unchanged and no wrong-document health window opened. Reactivating A made Refresh succeed against A. Native destruction of A closed the exact bound window and raised Closed; B remained active and its project remained unchanged, with one document left. Both disposable copies remained byte-identical to repository sample SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; PID/script cleanup passed and an independent post-run scan found zero sidecar, backup, lock, drawing-lock or script files. This is bounded `LOCAL_PASS` for P12. P10/P11 are now also bounded `LOCAL_PASS`; broad H.1, Family-editor proof and overall LOCAL-002 remain `PENDING_LOCAL`.
- 2026-08-21 current-candidate requalification: exact candidate `0ae7fb4369172198d25347b9b0d75bdbceead2bb`, BricsCAD V25.2.10 and adapter SHA-256 `B725F335AA71E90E9584EA1A6940A6889ACA2E2FDB22D88C2CB3713047268D01` passed P10 production Workspace review, P11 Undo/Redo/save/cold-reopen/rebuild and P12 two-DWG/modeless ownership with full process/script/private-state/DWG/UI-layout cleanup and byte-for-byte fixture restoration. P10 verified one live generated panel, one canonical GlassWall owner, matching Family review, active Instance scope, ready Health All/Release Check, unchanged project/source and zero health issues. Issue `#3397` is closed after PR `#3398` landed the review-preservation fix and static guard on `main`; this carrier merged that source fix and introduced no competing production UI change. Broad H.1/Family-editor coverage and overall LOCAL-002 remain `PENDING_LOCAL`.
- 2026-08-21 final fingerprint refresh: exact candidate `9130c70582f4f67b83d4d63c6a59d3a30f7c0ed8` on `main@805bc1dc63c007b562dc5d65529c76b25a7336ab`, BricsCAD V25.2.10 and adapter SHA-256 `2E31BCF12D3770E62097671CC683628B83A72F6158CC26C302BA6193D965EB1E` passed the official `962/962` gate set and the affected P04/P10 native review paths after the latest frame/panel fingerprint snapshot hardening. P04 verified 50 curved tessellation segments, 24 source panel cells, 168 authoritative/native pieces, 215 frames, 168 panels, one canonical owner/Locate and zero health issues. P10 verified one live selected panel, its canonical GlassWall/Family/Instance review, ready Health All/Release Check, unchanged project/source, zero health issues and complete DWG/UI restoration. Both probes intentionally retain `production_local002_qualified=false`; P04 also retains `exact_swept_curve_qualified=false`. Process/script/sidecar/backup cleanup passed, while two ignored BricsCAD `.dwl/.dwl2` files remain beside the disposable P04 copy. Broad H.1/Family-editor coverage and overall LOCAL-002 remain `PENDING_LOCAL`.
- Family-editor local evidence: the guarded modeless Family Save matrix passed at exact clean and pushed candidate `cf5f4a1938fcff403ffe8e2cd30184a6f1f7ce9d`, containing `origin/main@425738e01c42cbaae02943939092463733fa1f51`, on licensed BricsCAD V25.2.10. Exact SourceLink identity, seven focused Family/QSDB guards, Core smoke `ALL PASS` and V25 Release x64 build with zero warnings/errors passed. Plugin/Core SHA-256 values were `B34D6203B9F78452DED365B8D784DECE1A6112EDCB0C7D6B96B82E5035076C75` / `9A16E992924412B5B9CC7293F0499F0B6C3AA45F7C8997BD77B8D175BDA19643`. A production-created project and GlassWall Family used two real LINE sources captured through production semantic capture. Real `QS3DCURTAIN` Save propagated ten accepted numeric/material values to one inherited instance, preserved ten explicit overrides and its timestamp, rolled back over-limit glass/frame material attempts, and made a clean no-op Save with unchanged version/timestamp and regen zero. Production rebuild/save plus fresh-process cold reopen retained two hosts, 21 frames, 35 panels, disjoint native ownership and zero Health issues. Both sessions exited gracefully; canonical public DWG/QSDB bytes, fixed DemandLoad loader/`LoadCtrls=2`, process/script/sidecar/temp cleanup all passed. The runner applied no sample-sidecar workaround and retains `production_local002_qualified=false`; broad H.1 and overall LOCAL-002 remain `PENDING_LOCAL`.
- H.1 P01 current licensed blocker evidence: exact clean, pushed and current-at-start runtime candidate `9af52940bd0c9e65e4e7fba948a62391ea11bc62`, BricsCAD V25.2.10 and exact SourceLink-bound Release x64 adapter SHA-256 `A5E5C4C144D97B4DE6774F814AC1DE4370FE0CB37B6DCDE6E00EDA107CEB2F09` passed twelve focused lifetime guards, Core smoke `ALL PASS` and V25/private-probe builds with zero warnings/errors. The complete A/B/C functional marker passed: all 13 A-bound windows and both B-bound Family/BBS windows closed exactly once and detached, B managed-wrapper drift was observed and matched by native database identity without path identity, C-bound windows stayed alive, and both dynamic hubs/project-isolation/repeat-cycle checks passed. A diagnostic exact-PID/owner-HWND snapshot proved that the earlier 90-second symptom was an ignored-runner miss of BricsCAD's single-document `DirectUIHWND` save dialog, not a source hang. With the private detector safely constrained to the exact PID, exact disabled host owner and known Yes/No/Cancel plus ListBox/DirectUI variants, one normal WM_CLOSE discarded the disposable C-copy once and BricsCAD exited with Process exit code `0`; real teardown nevertheless produced one exact-PID Windows Application Error for `ucrtbase.dll`, exception `0xc0000409`. Issue #3621 remains open for this production final-host teardown correction; #3593 must rerun only after a follow-up lands on exact current `main`. Public fixture, protected user drawing, installed loader/bytes and `LoadCtrls=2` were preserved; cleanup left zero processes and a clean tracked tree. This remains `BLOCKED_SOURCE_FIX`, not a PASS; broad H.1 and overall LOCAL-002 remain `PENDING_LOCAL`.
- H.1 P05 current licensed blocker evidence (supersedes the preceding H.1 P01 target): exact clean current-at-start `main@3d5b77066c30e0f1e7d11065c3ec5feb8f1b87c5`, containing #3651 source head `92947101915141bbda2bbc4548e5fb6cae65cc76`, passed 14 focused lifetime/dispatch/host-quit guards, V25/private-helper builds with zero warnings/errors and Core smoke `ALL PASS`. The unchanged licensed V25.2.10 runner passed the complete functional marker: A 13/13 and B 2/2 close/detach exactly once, wrapper drift/native database identity, C windows, both dynamic hubs, project isolation and repeat cycle all passed. Final-host shutdown then failed: exact host matched and closed, one disposable save dialog was discarded, BricsCAD exited `0xC0000374` with `graceful_exit=false`, and exact-PID evidence contained one `ntdll.dll` heap-corruption Application Error plus WER `APPCRASH`. The BricsCAD report remained `ACCESS_VIOLATION (C0000005)` in `brx25.dll`; normalized `AcRxProtocolReactorManagerImp`/`AcRxObject` and concurrent `MilContent_DetachFromHwnd` frames matched p04 line-for-line. Public fixture, protected user drawing, installed loader path/bytes and `LoadCtrls=2` were preserved; zero BricsCAD processes remained and the tracked tree was clean. #3621 is again `SOURCE_FIX_REQUIRED`; rerun #3593 only after a new exact merged source SHA. This is `BLOCKED_SOURCE_FIX`, not `LOCAL_PASS`; broad H.1 and overall LOCAL-002 remain `PENDING_LOCAL`.
- H.1 P06 current licensed blocker evidence (supersedes the P05 rerun target): exact clean current-at-start `main@ec4384eb6a12ff6763dfdd19d4e4b84747ab60f3`, containing #3654 source head `3958572813a833d5e8dca945b1841acf955e6849`, passed 16 focused lifetime/dispatch/global-quiescence/race guards, exact plugin/Core SourceLink checks, V25/private-helper builds with zero warnings/errors and Core smoke `ALL PASS`. The unchanged licensed V25.2.10 runner again passed the complete functional marker: A 13/13 and B 2/2 close/detach exactly once, wrapper drift/native database identity, C windows, both dynamic hubs, project isolation and repeat cycle all passed. Final-host shutdown still failed: exact host matched and closed, one disposable save dialog was discarded, process exit was `0` with `graceful_exit=true`, and exact-PID evidence contained one `ucrtbase.dll / c0000409` Application Error plus one WER `BEX64`; Application Hang and .NET Runtime counts were zero. The BricsCAD report remained `ACCESS_VIOLATION (C0000005)` in `brx25.dll`; normalized `AcRxProtocolReactorManagerImp`/`AcRxObject` and concurrent `MilContent_DetachFromHwnd` frames matched P05 line-for-line. Public fixture, protected user drawing, installed loader path/bytes and `LoadCtrls=2` were preserved; zero BricsCAD processes remained, the tracked tree was clean and raw evidence stayed ignored. #3621 remains `SOURCE_FIX_REQUIRED`; do not rerun the unchanged P06 binary. This is `BLOCKED_SOURCE_FIX`, not `LOCAL_PASS`; broad H.1 and overall LOCAL-002 remain `PENDING_LOCAL`.
- Evidence: Bounded Family-editor `LOCAL_PASS`; broad H.1 and overall LOCAL-002 remain `PENDING_LOCAL`.
- Related source/docs: `src/QS3D.BricsCAD.V25/CurtainWallBuildCommands.cs`; `src/QS3D.BricsCAD.V25/CurtainPanelOpeningRuntimeProbeCommands.cs`; `src/QS3D.BricsCAD.V25/CurtainPanelWorkspaceReviewRuntimeProbeCommands.cs`; `src/QS3D.BricsCAD.V25/CurtainPanelMultiDwgRuntimeProbeCommands.cs`; `scripts/test-bricscad-v25-curtain-panel-openings.ps1`; `scripts/test-bricscad-v25-curtain-panel-workspace-review.ps1`; `scripts/test-bricscad-v25-curtain-panel-multidwg.ps1`; `scripts/preflight-curtain-panel-opening-runtime-probe.py`; `scripts/preflight-curtain-panel-workspace-review-runtime-probe.py`; `scripts/preflight-curtain-panel-multidwg-runtime-probe.py`; `scripts/preflight-curtain-orchestration-boundary.py`; `scripts/preflight-curtain-native-panels.py`; `src/QS3D.BricsCAD.V25/UI/CurtainWallWindow.xaml.cs`; `src/QS3D.Core/Domain/ProjectFamilyService.cs`; `scripts/preflight-curtain-wall-ui-export.py`; `docs/CURTAIN-NATIVE-PANELS.md`; `docs/LOCAL-AGENT-REMAINING-GATES-2026-08-10.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-24

## LOCAL-003 — shared Level Z-chain in native geometry

- Priority: P0
- Status: IN_PROGRESS
- Area: Structural / Wall / Opening / Rebar vertical placement
- 2026-08-21 current-main curved/round refresh: exact candidate `9130c70582f4f67b83d4d63c6a59d3a30f7c0ed8` on `main@805bc1dc63c007b562dc5d65529c76b25a7336ab`, matching ProductVersion and adapter/Core SHA-256 `2E31BCF12D3770E62097671CC683628B83A72F6158CC26C302BA6193D965EB1E` / `8344EB5859583770EEA98B0AB7DCB2F216E26018420E88AF2E658F26D6169B92`, passed the official `962/962` preflights, 847-class smoke registration, Core smoke `ALL PASS`, zero-warning Core/V25 builds, offline WPF and licensed NETLOAD/Ribbon/Palette. The current structural Undo contract exposed an automation-only probe drift: the curved probe used a detached project and failed closed at `positive_cases / beam_line`. The Issue #72 correction binds a pristine canonical project for the unchanged production build/rebuild calls, then clears selection and forgets that probe project in `finally` before Close; the focused guard now forbids detached regression. Fresh Millimeter and Meter runs each passed all seven positive capture-to-build cases, seven replacements, expected length/area/Z/positive volume and both fail-closed controls; 66 common fields had zero tolerance mismatches with a maximum absolute numeric difference of approximately `9.99e-15`, and both guarded copies remained/restored at SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. This closes only the dedicated curved/round capture-to-build row on the current candidate. Broader curved Undo/Redo, save/cold reopen, complete-family dual-unit lifecycle, multi-DWG and authorized private-DWG coverage remain `PENDING_LOCAL`; overall LOCAL-003 stays `IN_PROGRESS` and customer-release qualification remains false.
- 2026-08-21 exact current-candidate evidence: exact SHA `0ae7fb4369172198d25347b9b0d75bdbceead2bb`, matching ProductVersion and adapter/Core SHA-256 `B725F335AA71E90E9584EA1A6940A6889ACA2E2FDB22D88C2CB3713047268D01` / `2A5DCE45CC74EB9248A7079E02835DA81DEFD5A492AAF318CF21FB001CB44A2A` passed fresh guarded Millimeter (`INSUNITS=4`) and Meter (`INSUNITS=6`) runs on BricsCAD V25.2.10. Both retained legacy/Bottom-only/Bottom+Top ranges, physical opening, Curtain `16/14`, Beam bars/stirrups `4/6`, zero pre-edit Level health issues, seven stale outputs and Top-only refusal, then exited gracefully with exact process/script/private-state/DWG restoration. The representative P11-backed lifecycle also passed native Undo/Redo, save/close, cold reopen and disjoint stable `1/10/15` rebuild at `3.1..6.8 m` with zero health issues. `production_local003_qualified=false` remains intentional; complete-family dual-unit lifecycle, broader multi-DWG and explicitly authorized private-DWG evidence remain pending.
- 2026-08-25 #3811 bounded Meter lifecycle receipt: exact clean pushed source `2b30b81f3dfbaa389e50d1bc2058bcd530572ed4` passed licensed BricsCAD V25.2.10 with native `INSUNITS=6`, the representative GlassWall/Curtain Level range `3.1..6.8 m`, coherent native/semantic Undo and Redo, explicit project/DWG save, graceful close, fresh-process cold reopen, disjoint stable `1/10/15` ownership-scoped rebuild, and zero Level/P11 Health issues. Cleanup restored DemandLoad, the repository fixture and zero process residue. This closes only #3811's representative Meter lifecycle; complete-family dual-unit, broader multi-DWG and authorized private-DWG evidence remain pending. Do not rerun this bounded cell without a material source change.
- Curved/round structural bounded runtime row (2026-08-15; #1443 capture-to-build harness only): committed and pushed exact SHA `f3640ebbb35ecdda7a68c85e12d57f4fa717612d` passed the focused curved-structural gate, PowerShell AST, strict policy/handoff guards, full Core smoke `ALL PASS` and the installed-reference V25 `Release|x64` build with zero warnings/errors. Matching adapter/Core ProductVersion `0.1.0-preview.10041+f3640ebbb35ecdda7a68c85e12d57f4fa717612d` loaded in licensed BricsCAD V25.2.10 x64; adapter/Core SHA-256 values were `FE2201BEF17D08EC79FC791546E7AA8FB1C0E56256F285BADE65523FFA52D910` / `26024B4F7D4B839ED89A4742674C679029C0402871952DB0160FF55473352431`. Fresh Millimeter and Meter runs each passed Beam LINE/ARC/CIRCLE/straight-open-POLYLINE/curved-open-POLYLINE plus CIRCLE Slab/Column with expected Length/Area, positive owned `Solid3d` volume, expected Z spans, seven stale-rebuild replacements, and closed-Beam-polyline/non-WCS-CIRCLE fail-closed controls; 66 common marker fields matched across units within tolerance. Both hosts exited gracefully, process/script/private-state cleanup passed, and both guarded synthetic copies remained unwritten/restored at SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E` with no sidecar/backup/lock/script residue. This closes only the dedicated harness row: curved/round Undo/Redo, explicit save/cold reopen, broader lifecycle, multi-DWG and authorized private-DWG evidence remain `PENDING_LOCAL`, so overall LOCAL-003 stays `IN_PROGRESS` and customer-release qualification remains false.
- Bounded Level lifecycle follow-up (2026-08-15; representative GlassWall/Curtain scope only): clean exact branch SHA `189bd34eb2e5795cf90acbc275b0d43b642cd1be` passed full Core smoke `ALL PASS`, all focused lifecycle/P11/Level gates and the installed-reference V25 `Release|x64` build with zero warnings/errors. Matching plugin/Core ProductVersion loaded in BricsCAD V25.2.10 x64; adapter SHA-256 was `C947C814DD124744FBF140361BFB67C44C1B49583BD2824FC77E34BBD44818FA` and Core SHA-256 was `B3FCF2BFE2D2380E936BE2A7D41D7F4E0F967F8290F8484AD537BCD5C6F4F352`. The native Millimeter run preserved Bottom Level `3 m + 0.1 m` and Top Level `7 m - 0.2 m`, restored the pre-build host and Level config on Undo, removed the generated set, restored coherent `1/10/15` host/frame/panel output on Redo, preserved the same `3.1..6.8 m` Level span through explicit save/close and cold reopen, then removed the old generated set and produced a disjoint stable `1/10/15` rebuild with zero Level/P11 Health issues. Process/script/sidecar/lock/backup cleanup passed and the disposable DWG was restored to SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. This closes only the bounded representative lifecycle row; complete-family dual-unit lifecycle coverage, multi-DWG isolation and authorized private-DWG evidence remain `PENDING_LOCAL`, `production_local003_qualified=false`, and no GitHub Actions were run.
- Current exact evidence note (2026-08-14; supersedes every older blocker/reactivation/runtime note below): the representative guarded runtime matrix is `AUTOMATED_RUNTIME_PROBE_PASS` on clean exact SHA `2a1967d66f005cbdef20bb024a7b92a7f44077cc`, matching plugin/Core ProductVersion and plugin SHA-256 `86D3938C89BB42BBE4F4854F7C8C027736426B5B20648363798ED634D811516F`, inside BricsCAD V25.2.10 x64. Full Core smoke, all focused Level gates and the installed-reference V25 `Release|x64` build passed first. The probe verified legacy, Bottom-only, Bottom+Top, Top-only refusal, physical Door volume reduction, bounded Curtain frame/panel output, Beam bar/stirrup containment, vertical snapshots and Level-edit stale propagation; BricsCAD exited gracefully and process/script/private-state cleanup plus DWG restoration all verified true. This is scoped `LOCAL_PASS` for the automated representative probe only. The mm/m complete-family, Undo, save/reopen, multi-DWG and representative private-DWG matrix remains `PENDING_LOCAL`, so LOCAL-003 stays `IN_PROGRESS` and customer-release qualification remains false.
- Native unit-parity follow-up (2026-08-15; representative scope only): clean exact branch SHA `f00a948521d911aef314e7d8dd5b4bf1f7613bb3`, matching plugin/Core ProductVersion and plugin SHA-256 `F6D3113F6336FC3E945898F72B239646361F7524A3CEE0080E0D3246D08EB6AD`, passed the same guarded licensed probe separately with native `INSUNITS=4 / Millimeter` and `INSUNITS=6 / Meter` on BricsCAD V25.2.10 x64. Both markers verified the expected legacy/Bottom-only/Bottom+Top ranges, physical opening, Curtain `16/14`, Beam bars/stirrups `4/6`, Health, seven stale outputs and Top-only refusal. Both hosts exited gracefully inside the default 15-second bound after scripted no-save close; zero process, script/private-state cleanup, read-only-through-exit, unchanged drawing and attribute restoration all passed. Before/after/current synthetic-copy SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E` with no sidecar. This closes only representative mm/m unit parity; complete-family coverage in both units plus Undo, save/reopen, multi-DWG and representative private-DWG evidence remains `PENDING_LOCAL`.
- Integrated-main unit-parity confirmation (2026-08-15): after restoring the concurrently lost PR `#1432` diff through PR `#1466`, exact merged SHA `44195df7acf5db6ea06605090384410367cf95f2` passed the focused Level-Z gate, full Core smoke and installed-reference V25 `Release|x64` build with zero warnings/errors. Matching plugin/Core ProductVersion and adapter SHA-256 `52669B6120A99422C357E25AF1D6343C324B598853C76D0D088FC3D6A30DE747` then passed fresh guarded Millimeter and Meter runs with the same expected ranges, Curtain `16/14`, Beam bars/stirrups `4/6`, zero pre-edit Level health issues, seven stale outputs, Level-edit invalidation and Top-only refusal. Both runs exited gracefully, left zero BricsCAD processes, kept the synthetic drawing read-only through host exit and unwritten, restored attributes/private state, and preserved SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. Representative mm/m parity is therefore confirmed on an integrated merged-main SHA; broader LOCAL-003 scope remains `PENDING_LOCAL`.
- Current status note (2026-08-14; supersedes the older blocker paragraphs below): Issues `#990` and `#1092` are `CLOSED / COMPLETED`, and no BricsCAD process is running. The LOCAL-003 claim is being reactivated from synchronized `origin/main@604b8662be656e5a87fd8b43769526b1debec7b5`; newly added active claims are non-overlapping. After the reactivation commit is visible on current `origin/main`, rerun full Core smoke, all focused Level/static gates, the installed-reference V25 `Release|x64` build and matching plugin/Core ProductVersion, then execute a fresh disposable exact-SHA Level probe. Earlier SHA `2dc87bf0` passed Core Release, nine focused gates and the V25 build but did not run the native probe; no `LOCAL_PASS` is inferred.
- Current blocker note (2026-08-14; supersedes the reactivation note immediately above): Exact candidate `f38cc3464a11c62df31d50c186012a654b192e1f` passed Core Release with `0 warnings / 0 errors`, then full Core smoke failed because `RoomFinishFamilyCategorySmoke.MissingFamilyPreservesFallbackBehavior` expects a missing Family fallback while the canonical shared reporting identity guard fails closed for that reference. Handoff issue `#1101` owns the CAD-independent reconciliation; local003 did not edit Core/test or run the native probe after this prerequisite failure. Keep `BLOCKED` until a non-local fix makes full Core smoke pass on current `main`, then reactivate/push before rebuilding all exact-current gates and rerunning the disposable Level probe.
- Source-side status: `SOURCE_INTEGRATED / AUTOMATED_RUNTIME_PROBE_PASS / FULL_LOCAL_MATRIX_PENDING`. One branch-lazy CAD adapter covers qualified wall/structural hosts, Door/WallOpening cutters and Auto Host matching, Curtain LINE/path frames/panels/live state, generated rebar/ties/stirrups/mesh/shape families, semantic effective spans, vertical snapshots and guarded Bottom/Top/Clear Level UI. Unsupported categories remain policy-blocked. Exact SHA `2a1967d66f005cbdef20bb024a7b92a7f44077cc` passed the representative licensed Level probe; the complete interactive matrix is still required before production/customer-release qualification.
- Why local: Correctness depends on native V25 geometry, cutters, generated rebar alignment, and save/reopen behavior after Level edits.
- Current qualification baseline: Resumed at synchronized `main@b3495e36dace537cd8de823b5138c0899b4a68db` after non-local commit `bf671a90` corrected the `MeasurementTrace` nullable contract and its claim was completed; the user-owned BricsCAD `Drawing1` process was saved/closed without agent termination. Exact-SHA installed-reference build, ProductVersion verification and the fresh guarded runtime probe are now in progress; no runtime PASS is inferred yet.
- Current runtime observation: Exact SHA `1ebfd929e05db6c441b0a62fbc8632fcfcfe1f69` passed the installed-reference build and focused gates, then the licensed probe preserved drawing/sidecar/process/environment safety but failed inside the Curtain phase after proving legacy wall Z. The existing generic marker could not isolate frame/panel/range/mode, and its private generated script survived FAIL cleanup. Granular sanitized diagnostics plus exact-script cleanup are being hardened before a fresh exact-SHA rerun; this is not `LOCAL_PASS`.
- Current startup observation: Two subsequent launches remained responsive but never created the main window or executed the script. The dedicated test profile retained one Drawing Recovery entry for the first disposable failed-run copy because failure rethrew and runner cleanup force-stopped BricsCAD. Harness graceful no-save exit and exact disposable-recovery cleanup are in progress before the next exact-SHA run; no owner/private drawing or unrelated profile value is in scope.
- Current blocker: The native-startup machine blocker cleared after Windows restart, but three consecutive goal turns still found a responsive operator-owned BricsCAD session holding a private drawing; the guarded runner must not close or overlap it. On synchronized `origin/main@3752d589634d5a909fb58691150f6fac2905c225`, all nine focused Level/static gates passed and Core Release built with `0 warnings / 0 errors`, while the required full Core smoke compile failed with remote-safe `CS8602` at `MeasurementTraceContractSmoke.cs:98`. That unrelated source defect is handed to issue `#990`; this local worker did not absorb it. Resume only after the non-local fix lands and the operator saves/closes every BricsCAD process, then reactivate/push the claim before rebuilding and running a fresh disposable probe. This does not promote `LOCAL_PASS`.
- Scenario: First run the guarded automation-only `scripts/test-bricscad-v25-level-z.ps1` on a clean exact SHA and disposable `*.level-z-probe-copy.dwg`; it must prove exact assembly revision, unchanged drawing hash/no sidecar, legacy wall `[1.2, 3.7] m`, Bottom+Top wall `[3.1, 6.8] m`, Bottom-only Beam `[3.25, 3.85] m`, Top-only pre-mutation refusal, physical Door volume reduction, bounded Curtain frame/panel output, Beam bar/stirrup containment, vertical snapshots and Level-edit stale propagation. Then execute the wider shared `ElementVerticalPlacementService` matrix across wall families, Beam/Column/Slab/Foundation/Stair/Railing, Door/WallOpening, Curtain LINE/path output and every generated reinforcement family. Cover mm and m drawings; legacy/no-Level; Bottom-only; Bottom+Top; Top-only; missing/deleted/ambiguous Level; non-finite offsets; source reconcile; Level edit; Health/Release; Undo; save/reopen; and multi-DWG isolation.
- Bounded lifecycle scenario: `scripts/test-bricscad-v25-level-z-lifecycle.ps1` reuses the production Curtain P11 native Undo/save-reopen/rebuild path while adding Level-config and vertical-snapshot assertions. It is qualified only for the representative single-GlassWall row above and must not be cited as complete-family or production LOCAL-003 qualification.
- Evidence required: Exact tested SHA and matching plugin/Core `ProductVersion`; focused runner metadata and sanitized aggregate marker; before/after Z measurements; host-opening-Curtain-rebar alignment; health/release blocker behavior; unchanged disposable-DWG hash/no sidecar for the probe; Undo, save/reopen, Level-edit and multi-DWG results. Do not commit raw/private drawings or machine-specific evidence.
- Evidence: Representative automated probe `LOCAL_PASS` at exact clean SHA `2a1967d66f005cbdef20bb024a7b92a7f44077cc` on BricsCAD V25.2.10 x64, plugin SHA-256 `86D3938C89BB42BBE4F4854F7C8C027736426B5B20648363798ED634D811516F`. Matching plugin/Core ProductVersion, full Core smoke `ALL PASS`, all focused Level gates and installed-reference V25 `Release|x64` build `0 warnings / 0 errors` preceded the run. Sanitized marker results: legacy wall `1.2..3.7 m`; Bottom+Top wall `3.1..6.8 m`; Bottom-only Beam `3.25..3.85 m`; physical-opening reduction true; Curtain frames/panels `16/14`; Beam longitudinal/stirrup counts `4/6`; pre-edit Level health issues `0`; stale outputs after edit `7`; Level-edit invalidation and Top-only fail-closed true. Graceful exit, zero BricsCAD process, script/private-state cleanup and drawing restoration verified true; disposable DWG SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. Complete mm/m family coverage, Undo, save/reopen, multi-DWG and representative private-DWG evidence remains `PENDING_LOCAL`.
- Related docs: `docs/LEVEL-REFERENCES.md`; `docs/LOCAL-LEVEL-Z-QUALIFICATION-2026-08-11.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`; `scripts/preflight-level-native-host-placement.py`; `scripts/preflight-level-opening-placement.py`; `scripts/preflight-level-curtain-placement.py`; `scripts/preflight-level-z-lifecycle-runtime-probe.py`; `scripts/test-bricscad-v25-level-z.ps1`; `scripts/test-bricscad-v25-level-z-lifecycle.ps1`
- Current source wave: hosted Door/WallOpening placement is shared across straight/curved cutters, physical-cut live fingerprints and Auto Host matching. Curtain LINE/open-POLYLINE frame/panel builders, opening clipping, live fingerprints and frame config health consume the same placement contract. Beam/Column longitudinal bars, stirrups/ties, Slab/Foundation/StructuralWall meshes, Level-configured BBS shape origins and Railing LINE height resolve the same host bottom/effective height before native replacement. Guarded Bottom/Top/Clear assignment is visible in the Floor/Level UI, legacy arithmetic remains explicit, and unsupported categories remain policy-blocked. The coherent source chain is an integrated candidate only: the expanded exact-SHA V25 probe and the full interactive/private matrix remain `PENDING_LOCAL`, and source/static evidence must not be promoted to full `LOCAL_PASS` or customer-release readiness.
- Partial local probe evidence: the earlier bounded `QS3DLEVELZPROBE` ran in BricsCAD V25.2.10 x64 on a disposable ordinary copy and exposed a real legacy Beam placement bug: `Solid3d.CreateBox` was centered by V25 while `StructuralSolidBuilder` applied an additional negative half-extent translation, producing `-1.3 m .. 1.7 m` instead of `0.2 m .. 3.2 m`. After removing that duplicate translation, the sanitized boundary run passed with Beam extents `0.2 m .. 3.2 m`, unchanged copy SHA-256, one retained owned solid, one pending Level-health blocker, no sidecar and `production_level_qualified=false`. This is historical `LOCAL_PASS` only for legacy LINE-prism Z plus configured-Level fail-closed retention; it does not satisfy the expanded host/opening/Curtain/rebar/Level-edit matrix above.
- Updated: 2026-08-13

## LOCAL-004 — source reconcile native atomicity

- Priority: P0
- Status: IN_PROGRESS
- Area: Source Reconcile / Modify
- 2026-08-21 exact current base-matrix evidence: exact candidate `0ae7fb4369172198d25347b9b0d75bdbceead2bb`, BricsCAD V25.2.10 and adapter SHA-256 `B725F335AA71E90E9584EA1A6940A6889ACA2E2FDB22D88C2CB3713047268D01` returned `production_local004_qualified=true`. Three production reconciles, generated/ambiguous/multi-DWG refusal, forced rollback, source preservation, generated replacement, native Undo marker `BEFORE`, Redo marker `AFTER`, save and cold reopen all passed; `error_code=NONE`, every process/script/private-state/drawing-restoration flag was true, and the repository fixture stayed SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. This requalifies only the base synthetic matrix. PR `#3387` has since merged and its Beam STRETCH dependent P04 is exact-current `LOCAL_PASS`; broader topology/category/dependent/failure coverage under issue `#80` remains pending.
- Why local: Requires real LINE/POLYLINE edits, native generated-object cleanup, transaction/undo behavior, and document switching.
- Scenario: On a clean exact SHA, run `scripts/test-bricscad-v25-source-reconcile.ps1` with the repository-generated sample and initialized V25 profile. The runner creates two outside-repository disposable copies, authors tracked LINE/open-POLYLINE walls through production Direct Draw, edits both live sources, and drives production `QS3DSYNCSOURCE`/`QS3DBUILD3D`. It must verify ownership-scoped invalidation/rebuild, generated and intentionally ambiguous selection refusal, `INSUNITS` mismatch rollback after invalidation preparation, native Undo/Redo, B-document refusal/no cross-project mutation, `QS3DSAVE`, native save and cold reopen.
- Evidence required: Exact SHA plus matching plugin/Core ProductVersion; sanitized V1 aggregate marker; three successful reconciles; generated/ambiguous/multi-DWG refusal; exact semantic/native rollback; Undo/Redo coherence; save/reopen; process/script/private-sidecar cleanup; both disposable copies restored to the repository fixture hash and removed. Issue `#1005` required an exact fixed-SHA rerun; any repeated `NATIVE_UNDO_SEMANTIC_DIVERGENCE` or other production failure would remain PENDING and return to a non-local source-fix lane.
- Evidence: `NATIVE_UNDO_SEMANTIC_DIVERGENCE` reproduced at exact clean SHA `ca0710c1122c413291fe0cba4ef34f89ca584b76` on BricsCAD V25.2.10 x64: native Undo restored CAD while semantic state remained post-reconcile; Redo and cold reopen were coherent. The first production candidate `1c957ae2dc022db8cafbfcf0de91d9d47a53e68f` then reached the second reconcile but failed before Undo with `failure_phase=verify_final_reconcile` / `failure_code=SEMANTIC_SOURCE_MISMATCH` after the intentional unit-policy failure, refusal and multi-DWG sequence. The later clean exact-SHA rerun at `f42171d3f9dab336fa8874547a3016271977546d` with adapter SHA-256 `E36D98EC5097339F0D76EBB78CF6C38C7C1F9D78A0C43590E99B50666583F4B8` failed at the same final verification boundary: both sources remained selected, generated output was retained, project/revision/native marker stayed unchanged, and Source Reconcile history was already `DESYNCHRONIZED` before the final command (`MULTIPLE` entries). The requested source candidate `032cb6e3923772cf40755fb62fe891ea2a239010` was then rerun with adapter SHA-256 `DBF90108F6304714A7E0B297D0601E3927BF432B9D7264346C8DE68BA58978B7` and returned the identical tuple, proving its global command-depth/detach suppression did not remove the earlier desynchronization. Process/script/private-state cleanup and drawing restoration passed on both runs. Issue `#1005` remains open and LOCAL-004 remains `PENDING_LOCAL` until the entire guarded matrix passes on a corrected exact SHA.
- 2026-08-15 database Undo lifecycle diagnostic partial evidence: exact current-main SHA `2d101786403bd7526aa47715db325d941a7bcd88`, matching adapter/Core ProductVersion, focused gates/PowerShell AST/manual-policy PASS and V25 adapter build `0 warnings / 0 errors`. The only recovered `OBJECT_ONLY` tuple was diagnostic-only (`production_local004_qualified=false`) and restored both the existing-object mutation (`BEFORE`) and topology sentinel (`UNDONE`). The wrapper then failed with `System.ArgumentException: Argument types do not match`; aggregate metadata was absent and the three database lifecycle variants were not run. Windows PowerShell 5.1 deterministically reproduces the runner's failing `List[object]` to `@($results)` conversion, so non-local issue `#1515` owns the source-safe repair and original-error preservation. Cleanup and fixture restoration passed. LOCAL-004 remains `IN_PROGRESS / PENDING_LOCAL`; rerun all four variants on exact current `main` after `#1515` merges, and do not infer a lifecycle winner or production qualification from the partial tuple.
- 2026-08-15 post-`#1515` exact rerun and native close-sequence correction: exact pushed SHA `d976f547c425291f214dab5e05cb74f0d363c03b` passed all seven Source Reconcile gates, PowerShell AST, local handoff/manual-policy guards, full Core smoke `ALL PASS` and the installed-reference V25 `Release|x64` build with zero warnings/errors. Windows PowerShell 5.1 now preserved the original qualification error after the same diagnostic-only `OBJECT_ONLY` tuple (`existing_after_undo=BEFORE`, `topology_after_undo=UNDONE`): the host had saved the disposable DWG despite the runner's bare `QUIT N`, so the remaining variants did not run. Licensed stage isolation proved open/close and `NETLOAD` alone preserved the fixture, while `QS3DSRULPREPARE` followed by bare `QUIT N` saved the DWG and created `.bak`; explicit `CLOSE N` then `QUIT N` preserved SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E` with no backup/lock residue. The runner and focused guard now pin that explicit close-before-quit sequence; a fresh committed/pushed exact-SHA four-variant rerun is required before any database-lifecycle or production conclusion.
- 2026-08-15 close-fix exact rerun and process-finalization correction: exact pushed SHA `3856fe92b361180fb944eac9075fe7e4673c69f4` passed the same static gates, full Core smoke and V25 build; adapter/Core SHA-256 values were `1A3F09B9A82C0F62AE3553B2C767089D2856CC94DD444C2BC80F3619837FB1F8` and `C722634B14F728BC93C94D0D7F2C8E9BF6570F3A765174E05E1C2773B19C8799`. The licensed runner preserved the DWG and the same diagnostic-only `OBJECT_ONLY` PASS tuple, then failed closed before variant two because the just-exited host process record had not settled. Independent execution of the identical full sequence with exact-PID `WaitForExit` produced the same tuple, unchanged fixture hash and zero processes immediately afterward. `Wait-Qs3dExit` and its guard now finalize the exact test-owned process before the next zero-process preflight; unrelated/operator processes still cause fail-closed refusal. A new committed/pushed exact-SHA four-variant rerun remains required.
- 2026-08-15 complete database Undo lifecycle diagnostic PASS: exact pushed SHA `745fb3649463a43d577e5042d8595a4e6f09238f`, matching adapter/Core ProductVersion and SHA-256 `C9052C3A16AFC7D863021B60735A2BBE6F3C36ED4AFB07EB2B118DE7377DD5DF` / `43FB00EB67C23F92B769615C357C6221FE873F1F62A46BD966BE93A16688F284`, passed all static gates, full Core smoke and the V25 `Release|x64` build before licensed V25.2.10 execution. All four fresh-process tuples restored the existing marker to `BEFORE` and removed the topology sentinel (`UNDONE`). Database Undo recording was already `ON` at entry; it remained `ON` after optional database enable and/or `StartUndoRecord`, so `OBJECT_ONLY`, `DB_ENABLE_OBJECT`, `DB_START_OBJECT` and `DB_ENABLE_DB_START_OBJECT` were behaviorally identical. Every process/script/private-state/drawing cleanup flag passed and the fixture stayed at SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. This closes only the diagnostic matrix with `production_local004_qualified=false`: database-wide enable/start is not an evidence-backed production fix, and issue `#1005` returns to a non-local production-specific transaction/command-grouping investigation. LOCAL-004 remains `IN_PROGRESS / PENDING_LOCAL` until the complete production matrix passes on a corrected exact SHA.
- 2026-08-15 exact integrated LOCAL_PASS: PR `#1646` landed the automation-only native command-boundary correction at current-main SHA `b1845fef5565d086a1c69ab63ab086cea52b1678`. Exact ProductVersion `0.1.0-preview.10045+b1845fef5565d086a1c69ab63ab086cea52b1678`, Core/Smoke `0/0` plus `ALL PASS`, and installed-reference V25 `0/0` preceded the licensed run. The complete matrix returned `production_local004_qualified=true`: three successful reconciles, post-Undo `BEFORE`, post-Redo `AFTER`, Undo/Redo coherent, all refusal/rollback/source/rebuild/save/cold-reopen checks true, and `error_code=NONE`. Process/script/private-state/drawing cleanup passed, the fixture hash remained exact, and zero BricsCAD processes remained. Issue `#1005` is closed.
- 2026-08-20 native LINE edit P01 PASS: exact clean pushed SHA `2a6aa84a41daa68f35160bfc78c4330b78bc0f97`, matching adapter/Core ProductVersion and installed-reference BricsCAD V25 `Release|x64` build `0 warnings / 0 errors`, passed the additive `LOCAL_004_P01_LINE_ONLY` matrix on V25.2.10. Production Direct Draw authored one tracked LINE Wall and generated solid. Native `MOVE` displaced the source while retaining 5 m length; native `ROTATE` changed placement/orientation while retaining 5 m; top-level native crossing-window `STRETCH` changed one endpoint and the source/semantic length to 8 m. Each production `QS3DSYNCSOURCE` preserved source ownership and removed stale generated output; both explicit `QS3DBUILD3D` boundaries created new live ownership. `QS3DSAVE`/native save plus a fresh-process cold reopen retained the final LINE, 8 m semantic metric and live generated solid. All sanitized phase booleans were true with `error_code=NONE`; drawing persistence and sidecar persistence were observed before reopen, while process/script/private-state cleanup and drawing restoration all passed, zero BricsCAD processes remained, and the repository fixture stayed at SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. This qualifies only deterministic LINE command/batch `MOVE`/`ROTATE`/`STRETCH`; grip/jig/manual ESC, POLYLINE topology and the remaining category/dependent matrix stay pending, so LOCAL-004 remains `IN_PROGRESS` and parent issue `#80` remains open.
- 2026-08-20 native closed-POLYLINE vertex P02 PASS: exact clean pushed SHA `d389fc11a6d9599735180adb34a40a04089e5494`, matching adapter/Core ProductVersion and installed-reference BricsCAD V25 `Release|x64` build `0 warnings / 0 errors`, passed `LOCAL_004_P02_CLOSED_POLYLINE_VERTEX` on V25.2.10. Production `QS3DDRAWSLAB` authored and built a closed 4 m by 3 m Slab. Native top-level crossing-window `STRETCH`, with the last-created overlapping generated solid explicitly removed from the native selection, changed only `(4,3)` to `(5,3)` m; the old solid remained unchanged before reconcile. Production `QS3DSYNCSOURCE` refreshed area `12 -> 13.5 m2`, perimeter `14 -> 12 + sqrt(10) m`, gross/net volume `1.44 -> 1.62 m3` and formwork, erased stale ownership, and `QS3DBUILD3D` created distinct live output with expected `0..5 x 0..3 x 0..0.12 m` bounds. Scoped Core/runtime Health, save, sidecar persistence and fresh-process cold reopen passed. All sanitized booleans were true with `error_code=NONE`; process/script/private-state cleanup, drawing restoration and repository fixture SHA-256 preservation passed with zero BricsCAD processes remaining. This qualifies only one closed Slab POLYLINE vertex edit; grip/jig/manual ESC, closed/open transitions and the remaining category/dependent matrix stay pending, so LOCAL-004 remains `IN_PROGRESS` and parent issue `#80` remains open.
- 2026-08-20 native Beam dependent-output P03 PASS: exact clean pushed SHA `a49342145020b154479eaa780ef3a1af597a2b3f`, matching adapter/Core ProductVersion and installed-reference BricsCAD V25 `Release|x64` build `0 warnings / 0 errors`, passed `LOCAL_004_P03_BEAM_DEPENDENT_MOVE` on V25.2.10. Production `QS3DDRAWBEAM` authored one 5 m Beam with the projectless `0.3 m x 0.5 m` profile; the bounded fixture then built one host solid, four `4D16` longitudinal bars and six `D8@1000` stirrups. A real top-level native `MOVE` translated only the authoritative source LINE by `+1 m` WCS Y while every old generated handle, bound and volume remained unchanged before reconcile. Production `QS3DSYNCSOURCE` erased all old host/rebar/stirrup ownership and metadata; explicit `QS3DBUILD3D`, `QS3DBEAMREBAR3D` and `QS3DBEAMSTIRRUP3D` created distinct complete translated replacements, and scoped Core/runtime Health, save, sidecar persistence and fresh-process cold reopen passed. The first licensed candidate `87ea546b484fb690fde300be26eb12f662c4d937` had failed at `dependent_baseline / OUTPUT_HOST_CONTAINMENT_REJECTED`, isolating centered `CreateFrustum` bars translated to the covered start rather than their longitudinal midpoint; the bounded `BeamRebarSolidBuilder` correction now places each full-length bar at `covered start + axis * usableLength/2` without changing count, cover, layout, ownership, transaction or semantic metadata behavior. All final sanitized booleans were true with `error_code=NONE`; process/script/private-state cleanup, drawing restoration and repository fixture SHA-256 preservation passed with zero BricsCAD processes remaining. This qualifies only one Beam LINE `MOVE` with host/longitudinal/stirrup dependents; Beam STRETCH/count redistribution, grip/jig/manual ESC and the remaining category/dependent matrix stay pending, so LOCAL-004 remains `IN_PROGRESS` and parent issue `#80` remains open.
- 2026-08-23 native manual Beam endpoint-grip P05 PASS: exact clean pushed SHA `239938e676e632ad34d8c48c11674bf18e8c087c`, exact adapter/Core SourceLink, focused preflight, full Core smoke `ALL PASS`, and the installed-reference V25 Release build `0 warnings / 0 errors` preceded licensed V25.2.10 execution. The exact adapter ProductVersion/SHA-256 were `0.1.0-preview.10081` / `14B00F9355BB4CA77DFD413A43FEBD20F8328F800E8FEEEFCDDEB731A9A3807E`. A real endpoint hot-grip entered native `STRETCH`; physical ESC preserved the 5 m source/semantic/generated baseline. A second real hot-grip committed an 8 m source while semantic/generated state remained 5 m until production `QS3DSYNCSOURCE`; reconcile invalidated baseline output, production `QS3DBUILD3D` created the replacement, and SAVE plus fresh-process reopen retained coherent 8 m state with `prior_session_phases_replayed=false`. All six ordered P05 phase markers passed; sidecar persistence, graceful process cleanup and DemandLoad restoration passed with zero V25 processes. This closes only issue #3532's P05 boundary; LOCAL-004 and parent #80 remain open for the broader topology/category/dependent and failure/multi-DWG matrix.
- 2026-08-23 consolidated current-candidate P01-P05 PASS: all five bounded native cells were rerun from clean source SHA `2985f13b0f0d680284e915fb81728bbb26a42ffe` against BricsCAD V25.2.10 with exact x64 plugin SHA-256 `C94A5ED5C8EA4CC039EE364B1DA021005AD07514F93BA60D091DFC88519C14E6`. P01 LINE MOVE/ROTATE/STRETCH, P02 closed-POLYLINE vertex STRETCH, P03 Beam MOVE dependents, merged P04 Beam 5 m -> 8 m STRETCH with stirrups `6 -> 9`, and P05 real hot-grip physical-ESC/commit all passed production reconcile/rebuild, save/sidecar and fresh-process cold reopen. The enhanced V26 LINE lifecycle also passed Direct Draw, native MOVE/ROTATE/STRETCH, Undo/Redo, two-DWG refusal/isolation/reactivation/close and cold reopen on the same source SHA. Parent #80 remains open only because its requested matrix is broader than these P01-P05 cells.
- 2026-08-25 #3779 P06 bounded PASS: after source defect #3787 was fixed by PR #3791, exact clean pushed source `849339a0f7c5f1ef9659320117c5134f53740d11` passed licensed BricsCAD V25.2.10. Production Wall + linked Door cut, native `STRETCH` 5 m -> 8 m, pre-sync isolation/stale Health, `QS3DSYNCSOURCE`, dependent invalidation/rebuild and recut all passed. Native Undo restored uncut geometry plus `NO_CUT_STATE`; directly adjacent grouped recut plus native U/Redo restored cut geometry plus `CUT_STATE_RESTORED`. Explicit save, graceful close, fresh-process cold reopen and secondary-DWG no-project/no-sidecar refusal also passed with complete cleanup. Historical predecessor `3c5367e9408c5f93b58163e493ae95c0bbaf206c` remains failure chronology only. P06 is complete; broader #80 topology/category/dependent/failure coverage remains pending. Do not rerun P06 without a material source change.
- Related docs: `docs/SOURCE-RECONCILE-GENERATED-OUTPUTS.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`; `src/QS3D.BricsCAD.V25/SourceReconcileUndoCoordinator.cs`; `scripts/test-bricscad-v25-source-reconcile.ps1`; `scripts/preflight-source-reconcile-runtime-probe.py`; `scripts/preflight-source-reconcile-undo-coherence.py`
- Source-preparation note: Production Source Reconcile keeps failed-command marker/history state private, publishes semantic history only after native commit, and observes only unambiguous native Undo/Redo completion. The final runner targets the reconcile command explicitly with Mark/Back and Begin/End, and reseeds exact PICKFIRST sources after modal Undo setup. Exact-main licensed evidence now qualifies this synthetic matrix; other scenarios remain separately gated. No private/customer drawing was required.
- Updated: 2026-08-23

## LOCAL-005 — polygon Slab/Foundation native reinforcement

- Priority: P1
- Status: OPEN
- Area: Rebar 3D / Slab / Foundation
- Why local: Core outer+holes+disconnected multi-region topology/planning is REMOTE_DONE. Remaining proof requires native source-loop/RegionId association, native rebar materialization/ownership, straight/bulged extraction and OCS/WCS behavior, limits, Undo/save-reopen, multi-DWG, and exact V25 geometry.
- Scenario: Qualify the current Core region plans through native Slab/Foundation extraction/materialization. Cover convex/concave/disconnected regions and holes, straight/bulged source loops, impossible-cover rejection, bounded bar/object counts, rectangle compatibility, per-region ownership/stale/health, cross-layer rollback, Undo/Redo, and multi-DWG. Do not concatenate islands or treat islands as holes.
- Evidence required: Exact SHA; representative region/hole/bulge matrix; source-loop ↔ RegionId ↔ native-owner checks; cover/spacing/count measurements; limit rejection; rollback/Undo result; save-reopen/multi-DWG result.
- Evidence: PENDING_LOCAL
- Related docs: `docs/POLYGON-REGION-HOLES.md`; `docs/POLYGONAL-SLAB-MESH.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-11

## LOCAL-006 — native documentation objects

- Priority: P1
- Status: OPEN
- Area: Documentation / Tags / Tables / Sheets
- Source-side status: `SOURCE_COMPLETE / REMOTE_DONE` after PR #3643 (`b3212c11ba1dfed04f4a7e1f1e0fd8670e3561a5`). Existing source now covers semantic MText plus owned MLeader create/refresh/remove/runtime-health and bounded batch/associative refresh; native generic/authoritative/custom-schedule Tables; and Semantic Sheet/Layout/PaperSpace lifecycle with deterministic Viewport create/update/lock/scale/placement and title-block materialization. V25 source and V26 linked-source guards are integrated. Do not reimplement these paths from this local row; only a newly evidenced runtime defect should return to a bounded remote/source lane.
- 2026-08-25 post-#3721 bounded Semantic Tag Undo/Redo PASS: exact clean pushed source `a572ab0a350f54f8e994ac1e91f825907646af9c` passed licensed BricsCAD V25.2.10 after PR #3728. Source-selection cancel and placement cancel left project/native/semantic/audit state unchanged; cold-cache placement preserved canonical ProjectId continuity, created exactly one owned MText, advanced audit/ChangeVersion once and reported Health 0. Native Undo restored the complete native/property/audit/version baseline with Health 0, and Redo restored the accepted native geometry/property/audit/version snapshot with Health 0. DemandLoad/loader bytes and disposable state were restored. The host required exact-owned-PID force close after the 30-second graceful-exit window, so save/close/cold-reopen is not claimed. Do not rerun this bounded cell without a material source change; broader MLeader/Table/custom-schedule/Sheet/Layout/Viewport/Unicode/HiDPI/save-reopen/multi-DWG/V26 coverage remains pending.
- Why local: No known LOCAL-006 source implementation gap remains. Licensed BricsCAD is required to qualify the already-implemented MText/MLeader, Table/custom schedule and Sheet/Layout/PaperSpace/Viewport/title-block lifecycles, ownership, rendering and modeless/native behavior on an exact intended SHA.
- Scenario: Follow `docs/LOCAL-006-NATIVE-DOCUMENTATION-QUALIFICATION.md`. Qualify existing Semantic Tag/MText and MLeader placement, batch, refresh, removal and runtime-health behavior; native generic/BQ/Door-Opening/Room-Finish/Material/BBS/custom schedule Table lifecycles; and the implemented Semantic Sheet build/refresh/remove/health path including Layout/PaperSpace/Viewports/title blocks. On cold-cache valid `.qsdb`, true ownership writes must bind canonical state and persist through save/reopen. For Semantic Tag lifecycle specifically, run `QS3DTAG` with source-selection cancel and with a valid authoritative source followed by placement-point cancel; both cancel paths must return without binding, creating, or caching a replacement project and without creating MText or changing semantic/audit state. Then select a valid source, allow the read-only preview to resolve, replace/remove the sidecar or otherwise invalidate the project before confirming placement, and verify the final canonical rebind fails closed on ProjectId/source-owner mismatch with no CAD/semantic mutation. Repeat a successful cold-cache placement and prove the final write binds the canonical same-ProjectId project and exact-replacement ownership remains intact. For `QS3DTAGREFRESH`, cancel source selection before bind, then verify a valid selected source rebinds canonical state and replaces only the complete owned tag set. For `QS3DTAGREMOVE`, cancel tag/source selection before bind; with a valid owned tag/source verify canonical removal, and with the sidecar/project absent or replaced after selection verify fail-closed behavior with no documentation erase or semantic metadata mutation. Separately open BQ, Door/Opening, Room Finish and BBS review windows: BQ column preference writes must bind canonical state, while Door/Room refresh/export and BBS XLSX refresh/export must regenerate detached snapshots and leave live dirty/change-version/timestamp/audit state unchanged. Read-only load/filter/Locate paths remain non-creating. Qualify `QS3DSCHEDULETABLE` create/update, `QS3DSCHEDULETABLEREFRESH`, `QS3DSCHEDULETABLEHEALTH` and `QS3DSCHEDULETABLEREMOVE` across multiple coexisting schedules; a header-only zero-match schedule; prompt cancel before canonical bind; `ProjectId`/`ChangeVersion` change while a prompt is open; schedule definition edit/delete; partial/corrupt metadata and foreign Table ownership; saved WCS position/content drift; then exercise MLeader and Sheet/Layout/PaperSpace/Viewport/title-block ownership, atomicity, Undo/Redo, save/cold-reopen, Unicode/HiDPI, multi-DWG and representative V26 parity.
- Evidence required: Exact SHA; Semantic Tag source-cancel and placement-cancel proof of no project bind/create/cache and no documentation/semantic/audit mutation; stale/replaced-project placement refusal with before/after ProjectId/source-owner/CAD/semantic evidence; successful cold-cache placement canonical ProjectId continuity; `QS3DTAGREFRESH` cancel-before-bind plus complete-owned-set exact replacement; `QS3DTAGREMOVE` cancel-before-bind, canonical successful removal, and absent/replaced-sidecar refusal with no CAD/metadata deletion; MText/MLeader ownership/batch/refresh/remove/health checks; Table/custom-schedule ownership/refresh/health checks; BQ preference persistence; before/after live-state invariants for Door/Room/BBS detached regeneration/export; two or more coexisting schedule owner slots; header-only zero-match native Table; prompt-cancel no-bind/no-mutation; stale `ProjectId`/`ChangeVersion` refusal; definition-delete orphan-slot removal or safe fail-closed partial-metadata behavior; project/schedule/fingerprint XData ownership; stored-position/content health drift; Sheet/Layout/PaperSpace/Viewport/title-block create/update/remove/health, scale/lock and mixed/unowned-content protection; cancellation/freshness and atomic rollback; Unicode/HiDPI; Undo/Redo; save/cold-reopen; multi-DWG; representative V26 parity; sanitized cleanup evidence.
- Evidence: bounded `QS3DTAG -> native Undo -> native Redo` `LOCAL_PASS` at exact source `a572ab0a350f54f8e994ac1e91f825907646af9c`; broader LOCAL-006 remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`. Source/static/hosted CI is not `LOCAL_PASS`.
- Related docs: `docs/LOCAL-006-NATIVE-DOCUMENTATION-QUALIFICATION.md`; `docs/SEMANTIC-TAGS.md`; `docs/SEMANTIC-SCHEDULES.md`; `docs/COMMANDS-NATIVE-DOCUMENTATION-TABLES.md`; `docs/DOCUMENTATION-LAYER.md`; `docs/EXISTING-PROJECT-MUTATION-CONTEXT.md`; `scripts/preflight-semantic-tag-project-lifecycle.py`; `scripts/preflight-native-custom-semantic-schedule-table.py`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-24

## LOCAL-007 — physical L/T/X wall junction output

- Priority: P1
- Status: OPEN
- Area: Wall junctions
- 2026-08-23 P01 local evidence: exact clean pushed runtime candidate `8c928e1b17e1e161ef8437b56ad08aa9ef0e9b66` passed the bounded `QS3DWALLJUNCTIONS` selection/read-only analysis cell on BricsCAD V25.2.10. Exact-PID ESC from no-PICKFIRST selection left no project cache/sidecar/CAD mutation. Projectless LINE/open-POLYLINE analysis used defaults, created no project state and emitted the independently planned `L:1,T:1,X:1,Straight:1,End:16,Multi:1,SnapPlan:0`. Cold-cache valid-project analysis consumed custom junction/sagitta/planarity tolerances, preserved canonical ProjectId plus metadata/version/audit/sidecar/lock/CAD state, and emitted `L:27,T:0,X:0,Straight:0,End:4,Multi:0,SnapPlan:1`; both production logfile summaries matched independent fixture plans. V25/private-probe builds were clean, full Core smoke returned `ALL PASS`, three focused wall-junction guards passed, disposable/source/user bytes and fixed loader state were preserved, raw logs/private copies were removed, host exit code was `0` and zero BricsCAD processes remained. This is `LOCAL_PASS` for P01 analysis only; Wall Snap Preview/Apply, physical multi-owner output, replacement/rebuild, save/reopen and Undo/Redo remain pending under parent #73.
- 2026-08-23 P02 local evidence: exact clean pushed current-main candidate `ddbe528157a29656647ee7da0fcb8b441f512016` passed `LOCAL_PASS / BOUNDED_P02_WALL_SNAP` on licensed BricsCAD V25.2.10 after #3600/#3601. All 24 allowlisted markers across four graceful exit-code-0 sessions passed. Preview advanced exactly two revisions, recorded one audit and stamped the final revision while preserving CAD/generated ownership/sidecar state. Unchanged Apply succeeded, snapped only the intended endpoint, synchronized semantic `LengthM`, invalidated only the touched generated owner, preserved the untouched owner, advanced one audit-owned revision and cleared Preview metadata. Cancel and absent-sidecar cells left no cache/project/metadata/audit/CAD residue. Atomic replacement refused without CAD/semantic/sidecar mutation and left no replacement project cached. V25/private-probe builds were clean, full Core smoke returned `ALL PASS`, eight focused guards passed, fixed loader/security/user/source bytes were preserved and zero BricsCAD processes remained. No production source changed in the rerun.
- Why local: Core now defines deterministic multi-owner identity/dependency/rebuild plans, but safe native Solid3d materialization, booleans, replacement and ownership verification still require V25.
- Scenario: First qualify the analysis lifecycle on the exact candidate SHA. Run `QS3DWALLJUNCTIONS` with no PICKFIRST and cancel the interactive selection: it must return before any project lookup/bind/create/cache. On a disposable DWG with no QS3D sidecar/project, select valid coplanar LINE/open-POLYLINE wall centerlines and verify analysis still classifies L/T/X/Straight/End/Multi using source defaults without creating/caching/persisting a QS3D project or audit state. On a cold-cache DWG with a valid existing `.qsdb`, repeat with custom WallJunctionToleranceM/WallArcSagittaM/WallJunctionPlanarityToleranceM and prove selection completes first, the existing canonical same-ProjectId project is then bound for project-specific tolerance/audit use, and no replacement project appears. Then qualify the existing Wall Snap lifecycle. With a valid `.qsdb`, forget/reload the cache and run `QS3DWALLSNAPPREVIEW` then `QS3DWALLSNAPAPPLY`: both commands must bind the canonical same-ProjectId project before metadata/audit or CAD/semantic mutation, and Apply must preserve the current source-fingerprint, generated-dependent invalidation, native transaction and semantic rollback boundaries. Repeat with the project/sidecar absent or replaced after the source selection is prepared: both commands must fail closed and must not create/cache a replacement project, write preview metadata/audit, move LINE/POLYLINE vertices, invalidate generated ownership, or mutate another DWG. Then materialize only from current `WallJunctionOwnershipPlanner` output. Treat all plans sharing one `GroupToken` (`WJP1:`) as one replacement/rebuild unit; never assume an individual occurrence index remains long-lived when group topology/membership changes. Persist/verify dedicated `OwnerToken` (`WJX1:`) plus `InputFingerprint` (`WJF1:`), never reuse one wall's generated-solid owner. Cover L/T/X/Multi, 2/3/4+ owners, multiple occurrences, mixed thicknesses, incompatible vertical ranges, source/profile/elevation changes, owner add/remove, stale-extra output cleanup, foreign/corrupt ownership refusal, and Door/Opening host retention.
- Evidence required: Exact SHA; `QS3DWALLJUNCTIONS` cancelled-selection proof of no project lookup/bind/create/cache; no-project successful analysis with proof of no sidecar/cache/audit creation; cold-cache valid-project analysis with custom-tolerance use and canonical ProjectId continuity after selection; Wall Snap valid-sidecar canonical ProjectId continuity; absent/replaced-sidecar Preview/Apply refusal with proof of no replacement project, no preview metadata/audit change and no native/semantic mutation; successful Preview→Apply proof that source-fingerprint validation, generated invalidation, native transaction and semantic rollback behavior remain intact; whole-`GroupToken` membership/replacement check; `OwnerToken`/dependency/fingerprint persistence checks; L/T/X/Multi geometry matrix; invalidation/rebuild after fingerprint or group-membership changes; stale-extra cleanup; no cross-DWG/project mutation; opening host-retention result; save/reopen and Undo/Redo result.
- Evidence: P01 analysis `LOCAL_PASS`; P02 Wall Snap `LOCAL_PASS`; physical-output P03 is separately qualified on PR #3616 but not yet integrated; overall `OPEN / PENDING_INTEGRATION`
- Related docs: `docs/WALL-JUNCTION-OWNERSHIP.md`; `docs/agent-work-claims/2026-08-23-codex-issue3596-wall-junction-analysis-p01.md`; `docs/agent-work-claims/2026-08-23-codex-issue3599-wall-snap-p02.md`; `scripts/preflight-wall-junctions.py`; `docs/LOCAL-AGENT-REMAINING-GATES-2026-08-10.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-23

## LOCAL-008 — Direct Draw transient preview and repeated mode

- Priority: P1
- Status: OPEN
- Area: Direct Draw UX
- Source-side status: REMOTE_DONE for the quick/advanced split across the high-frequency Direct Draw commands, family-wide preview-project freshness at `98a9a78f643d3434597091b4d3be7fb8b41e00a8`, and P0 Wall/Beam/Slab/Column plus Door/Opening geometry-prompt context freshness at `6203817538f108b274001d834e7213f12dee7f70`. `DirectDrawProjectPreviewContext` binds prompt-derived Family defaults to the same canonical ProjectId, or requires the operation to remain projectless, before snapshot/source CAD. P0 and Door/Opening now capture active drawing unit/UCS before the first geometry point and revalidate active DWG, Model Space, drawing-unit policy and UCS before geometry-derived width/plan validation or project preview; `scripts/preflight-direct-draw-p0-opening-prompt-freshness.py` guards that source order. Window additionally rechecks ChangeVersion, active DWG, ModelSpace, UCS and unit policy before exact-project Auto Host and opening/host-scoped regeneration. Exact V25 editor/cancel/Auto-Host/reference/repeated-mode proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`; Plan-to-3D runtime proof remains separately owned by LOCAL-014.
- 2026-08-23 P01 local evidence: exact clean pushed runtime candidate `ea85175b4dbc601047d0b6945032836dea4765bd` passed the bounded Direct Draw Beam view/consecutive/cancel cell on BricsCAD V25.2.10. Two consecutive production `QS3DDRAWBEAM` invocations each added one LINE source and one selected owned 3DSOLID; both owners stayed in the same project and used distinct semantic elements. A third invocation was cancelled after its first point by exact-PID ESC, returned to `CMDACTIVE=0` with no active command, and added no source or solid. View center, size, twist, direction and target remained invariant after both commits and after cancellation. The exact adapter/Core PDBs contained the candidate SHA; V25 Release|x64 built with zero warnings/errors, full Core smoke returned `ALL PASS`, seven focused policy/Direct Draw guards passed, the dirty disposable project exercised the discard path, helper/host exit was clean, the fixture copy stayed byte-identical, no sidecar was created and zero BricsCAD processes remained. This is `LOCAL_PASS` for P01/Sheet row 2 only; internal repeated mode, quick/ADV cancellation and drift, Auto Host/reference, Ribbon, planar-UCS and document-switch coverage remain pending under parent #74.
- 2026-08-23 P03 repeated-mode local evidence: exact source candidate SHA `9a77d329e90809a2006d8e4dc1bafc995c0a8ca2` passed the production `QS3DDRAWWALLREPEAT` / `QS3DDRAWBEAMREPEAT` matrix on both licensed BricsCAD V25.2.10 and V26.2.07. Exact plugin SHA-256 values were `7FFE58DBC9390D535F1488730255853880FC2E3215E9FF2C43997C08A12B1F88` and `F063EC199DD50A25FBF619D54D9F8492B94065E3C7E5E4AF0454B54252BD3AFC`. Each host independently accepted two canonical Wall segments and two canonical Beam segments through a database-free DrawJig; the hosted `WorldDraw` callback was observed eight times for each Family. Both hosts preserved committed segments on Enter and exact-process physical ESC, honored planar UCS, refused a pending commit after document switch without cross-DWG mutation, grouped native/semantic Undo/Redo coherently, saved DWG/sidecar, and passed fresh-process Wall/Beam cold reopen. Every cleanup/restoration flag passed with zero BricsCAD processes.
- 2026-08-25 #3789 P04 bounded prompt-cancellation PASS: exact clean pushed source `e945f0f88d56449443ef73be2e6f0cb35d0dd823` passed all 14 Quick/ADV Door/WallOpening physical-ESC prompt cases on licensed BricsCAD V25.2.10. Every command returned to idle with no project/cache/sidecar/source/semantic/native/selection/DBMOD residue and byte-identical disposable DWGs. The production commands did not close their hosts inside the bounded post-result window, so only exact owned PIDs were force-stopped after evidence capture; no graceful-close, save/reopen or cold-reopen claim is made. Do not rerun P04 without a material source change.
- 2026-08-25 #3830 P05 executable context-drift submatrix PASS: exact clean pushed source `6cab2d33d6b19f9d8ce00a884bc1bdd4e9980259` passed 12/12 fresh-process Wall/Slab/Column control plus Model/Paper Space, `INSUNITS 4 -> 6` and planar-UCS drift cases on licensed BricsCAD V25.2.10. The three unchanged controls completed with canonical source/semantic/generated output; all nine executable drift cases were applied after the final point response but before production resumed and refused before project bind/bootstrap or source/native/semantic mutation. The attempted active-DWG switch at the modal `GetPoint` boundary was refused by BricsCAD itself with `BRX_Error__334_eDocumentSwitchDisabled`, so that one subcell remains `HOST_REFUSAL / NO PRODUCT VERDICT`. Cleanup and fixture restoration passed. Do not rerun the bounded executable P05 submatrix without a material source change.
- Why local: DrawJig/transient/editor/UCS lifecycle, ESC cleanup, document switches, native palette behavior, Auto Host ambiguity and proof that cancellation or preview/project drift never crosses the project-creation boundary require interactive V25.
- Scenario: On disposable DWGs, cancel every geometry/reference stage of each primary quick command and every numeric prompt of the corresponding `*ADV` commands; every cancel must leave no new project/cache/sidecar/source/semantic/native residue. For each prompt-bearing family, replace/remove the preview project or make a project appear while a prompt is open and prove refusal before `ProjectStateSnapshot` or command-owned mutation. For P0 Wall/Beam/Slab/Column quick+ADV and Door/Opening quick+ADV, change active DWG, switch Model/Paper Space, change drawing-unit policy, and rotate/move the supported planar UCS while a geometry point/path prompt is open; each drift case must refuse before geometry-derived width/plan validation, project preview/bind, source CAD, semantic mutation or native output. Repeat unchanged-context runs and prove the current quick/project-preview behavior succeeds with the same intended UCS/unit interpretation. Successful runs must rebind the same ProjectId and use the expected Family defaults. For `QS3DDRAWWINDOW`, additionally prove ChangeVersion/DWG/ModelSpace/UCS/unit drift rejection, canonical `WallOpening` + `OpeningUsage=Window`, exact unique Auto Host, unchanged unrelated dirty state, rollback for no-host/ambiguous-host, and no physical cut until `QS3DCUTSELECTEDOPENINGS`. Verify the reference LINE remains read-only and qualify Ribbon idempotence, repeated mode, planar UCS behavior and safe document switching.
- Evidence required: Exact SHA including `98a9a78f643d3434597091b4d3be7fb8b41e00a8` and `6203817538f108b274001d834e7213f12dee7f70`; quick cancel/default matrix; per-command `*ADV` prompt cancellation; P0/Opening geometry-prompt DWG/ModelSpace/unit/UCS drift matrix with refusal before width/plan validation, project bind/source/semantic/native mutation plus unchanged-context success; same-ProjectId success plus replaced/removed/project-appears refusal; no-residue proof; malformed/non-finite default refusal; Door/Opening/Window host outcomes; Window ChangeVersion/DWG/ModelSpace/UCS/unit freshness, `OpeningUsage=Window`, scoped-regeneration and explicit-cut results; reference LINE before/after proof; Ribbon/repeated/document-switch results. Evidence must be sanitized and exclude private paths and raw Handle lists.
- Evidence: P01, P03, P04 and the executable P05 submatrix are bounded `LOCAL_PASS`; the P05 active-DWG modal switch is `HOST_REFUSAL / NO PRODUCT VERDICT`, and Auto Host/reference plus the remaining Ribbon/UI and broader drift matrix remain `PENDING_LOCAL`.
- Related docs: `docs/DIRECT-DRAW-WORKFLOW.md`; `docs/DIRECT-DRAW-ACTIVE-FAMILY.md`; `docs/agent-work-claims/2026-08-23-interactive-issue3612-repeated-directdraw-v25-v26.md`; `docs/DIRECT-DRAW-PREVIEW-PROJECT-FRESHNESS.md`; `docs/DIRECT-DRAW-QUICK-WINDOW-2026-08-11.md`; `docs/DIRECT-DRAW-CANCEL-PROJECT-LIFECYCLE.md`; `scripts/preflight-direct-draw-repeated-mode.py`; `scripts/preflight-direct-draw-project-preview-freshness.py`; `scripts/preflight-direct-draw-p0-opening-prompt-freshness.py`; `scripts/preflight-direct-draw-scoped-regeneration.py`; `scripts/preflight-quick-window-authoring.py`; `scripts/preflight-plan-to-3d-finish-workflow.py`
- Updated: 2026-08-23

## LOCAL-009 — clean-machine install/sign/update qualification

- Priority: P1
- Status: OPEN
- Area: Packaging / Release / Trust
- 2026-08-21 operational preview-upgrade evidence: release `v0.1.0-preview.10192` at source commit `afff082096998fa404f08a5e29bcfd9fbc3830dd` was downloaded to the local desktop, with ZIP/sidecar SHA-256 `ED1FF224A7121770B1A99288E5DDE8F3B1C5F662452ECB710F94D32B2642BC92` / `2055DBFDF8DD8D69D2D3086F884BF6D1BE5C4F5E0809FBAF5B1C3D2DF7FC439B`. The 17-file manifest verified, Mark-of-the-Web was removed from the 18 extracted package files without weakening PowerShell/BricsCAD policy, and the transactional installer replaced the fixed per-user V25 DemandLoad path, registered `LoadCtrls=4` plus 462 commands, and installed adapter SHA-256 `6B090154648CBA7378CD09C2396A620130BD8EFECC80DA0B542FA98A6FBCC7A7`. A cold DemandLoad-only run loaded that exact fixed-path binary and passed with zero process residue, resolving the reboot fallback to `0.1.0-preview.2`. The preview is intentionally Authenticode `NotSigned`; no signer/timestamp/commercial trust claim is made. Rollback, uninstall, signed update, SECURELOAD and a genuinely clean-machine matrix remain `PENDING_LOCAL`, so LOCAL-009 stays `OPEN`.
- Why local: Production signing certificate, Windows trust chain, timestamp, clean-machine install/update/rollback/uninstall, and BricsCAD SECURELOAD behavior cannot be proven remotely.
- Scenario: On an authorized customer-like Windows/V25 machine, sign approved binaries, verify signer/timestamp, finalize hashes/manifest/ZIP only after verification, then test clean install, upgrade, rollback, uninstall, DemandLoad, and trust behavior without weakening SECURELOAD.
- Evidence required: Exact SHA/tag; signer/timestamp verification summary; package hashes; install/upgrade/rollback/uninstall result; DemandLoad/SECURELOAD result.
- Evidence: PENDING_LOCAL
- Related docs: `docs/MANUAL-BUILD-RELEASE.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-11

## LOCAL-010 — large-model performance and UI matrix

- Priority: P2
- Status: OPEN
- Area: Performance / UI / HiDPI
- Source/runner status: `SOURCE_READY / PENDING_LOCAL / DO_NOT_RETRY_REMOTE`.
- Runner: `scripts/run-local-v25-local-010.ps1`
- Runbook: `docs/LOCAL-010-PERFORMANCE-UI-QUALIFICATION.md`
- Remote disposition: `REMOTE_DONE / PENDING_LOCAL / DO_NOT_RETRY_REMOTE`
- Local handoff: From one clean exact intended `main` SHA containing this runner/runbook, with licensed interactive BricsCAD V25 x64 and zero pre-existing BricsCAD processes, **pull/sync only and run one command**: `powershell -ExecutionPolicy Bypass -File .\scripts\run-local-v25-local-010.ps1`. Do not patch production source in this local lane.
- Exact-source rule: Evidence is valid only for the runner-reported exact checkout SHA and matching V25/plugin identity. A newer intended `main` candidate requires a clean restart from the beginning; hosted/source CI never manufactures `LOCAL_PASS`.
- Result semantics: `artifacts/local-v25-local-010/qualification.json` records exact SHA, Windows/V25 identity, canonical baseline and per-row `PASS`/`FAIL`/`BLOCKED` evidence; runner exit 0/1/2/3 means complete PASS/FAIL/BLOCKED/NO_RESULT and `localPassClaimedByRunner=false`.
- Cleanup/host-major boundary: Use sanitized/disposable projects only, publish no private paths/raw Handles/ProjectIds/proprietary files, preserve user/DemandLoad state, never kill unrelated BricsCAD sessions, and finish with zero test-owned BricsCAD process residue. This runner qualifies V25 only; V26 requires its matching assembly/runtime and must never reuse or relabel the V25 binary.
- Why local: Representative timings, native palette responsiveness, GPU/driver effects, and DPI/layout behavior need real hardware and V25.
- Scenario: Measure DependencyGraph/regeneration, rooms, wall junctions, Auto Host, Curtain, BQ/BBS/ED2/Interchange, ownership/Health, rebar limits, plus 100/125/150/200% DPI and narrow/normal/wide palettes on representative large projects.
- Evidence required: Exact SHA; hardware/OS/V25 build; project sizes; timings; DPI/layout results; sanitized bottleneck notes.
- Evidence: PENDING_LOCAL
- Related docs: `docs/LOCAL-AGENT-OPEN-WORK-ADDENDUM-2026-08-10.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-11

## LOCAL-011 — staged native rollback and post-commit UI isolation

- Priority: P1
- Status: PASS
- Area: Fault injection / rollback / modeless UI / generated replacement atomicity
- Why local: Core can prove semantic `ProjectStateSnapshot` restoration remotely, but native transaction abort/compensation, DocumentLock and multi-DWG behavior, generated-object cleanup, stale/erased ObjectId behavior, native XData ownership and post-commit WPF/modeless failures require interactive licensed BricsCAD V25.
- Source-side status: REMOTE_DONE for Grid annotation exact-set replacement (`761b9b92f5dd3638b18d281c273a406e41069511`), Curtain LINE/PATH exact-set replacement (`ffd26294f3f27d03de1050643aa0aeb894dcb0f2`), the shared seven-family Rebar exact-set ownership guard (`1850f02382c8ccf71f04e3ea9daa28455aaae08f`), and the dedicated Column Tie exact-set guard follow-up (`b22eacd681230f231e0f970fb670e8f89769c35e`). The complete licensed runtime matrix is `LOCAL_PASS` only for exact tested SHA `fbf1e7923cbde9037637e5b6b1339b31f491c87a`; remote/non-local agents must not re-run or broaden this proof unless the source scenario materially changes.
- Runtime finding (`#3935`): the canonical runner recorded all 21 required rows as `PASS` on BricsCAD V25.2.10 with exact product DLL SHA-256 `2072421697D8C33FE29A857822EB67AB9EBD6C634C2CFA38C7AA12DD19B42B03`. The same-SHA baseline passed all 1042 aggregate gates, Core smoke, V25 `Release|x64` build, offline WPF checks and licensed NETLOAD/Ribbon/Palette smoke. Exact-set refusal/replacement, malformed/duplicate ownership, foreign-object protection, pre/during/post-commit failures, Door/Room/BBS/BQ/RebarMesh cache/reload safety, stale Recognition Apply, palette teardown/rebind, Undo/save/cold-reopen and multi-DWG isolation all passed. Source defects `#3955` and `#3959` remain resolved; no production source was changed by the local worker.
- Runner: `scripts/run-local-v25-local-011.ps1`
- Remote disposition: `REMOTE_DONE / LOCAL_PASS / DO_NOT_RETRY_REMOTE`
- Local handoff: Licensed V25 qualification is complete for this bounded scenario at exact SHA `fbf1e7923cbde9037637e5b6b1339b31f491c87a`. Keep the runner as a regression reference; do not rerun unless a material source change explicitly reopens the lane. Later source descendants are not implicitly qualified.
- Closed-handoff history: the superseded `REMOTE_DONE / PENDING_LOCAL / DO_NOT_RETRY_REMOTE` state instructed a compatible local agent to pull/sync and run one command, `scripts/run-local-v25-local-011.ps1`. The exact-SHA `LOCAL_PASS` above closes that instruction.
- Scenario: Starting from `docs/PROJECT-ROLLBACK-FAILURE-MATRIX.md`, inject failures before/during/after native commit and during post-commit UI refresh. Verify stale Recognition Apply never creates a replacement project and true modeless writes either bind current canonical same-ProjectId state or fail closed. Keep Door/Opening, Room Finish, BBS and BQ windows open across cache forget/reload and active-DWG switches: Door/Room/BBS read-only regeneration/export must operate on detached snapshots without changing live project state; BQ preference writes must rebind canonical state or refuse. Keep `RebarMeshSetupWindow` open across `QS3DRELOAD`/cache replacement and then click Save: it must fail closed before any property/dirty/change-version/audit/native mutation, must not create/cache a replacement project, and must require reopening the window against the current canonical project. With Workspace/Right palettes visible, also activate a drawing whose QS3D project cannot be loaded (for example a sanitized drawing/sidecar identity-mismatch fixture): verify the old project's palette callbacks/content are torn down, palette visibility/layout is preserved, no click can mutate the prior drawing/project, and a later valid drawing activation rebinds cleanly. For generated replacement atomicity, on an exact tested SHA that includes the four source-side commits above, create controlled generated owner sets where metadata stores N handles but exactly one expected handle is stale/missing/erased while the remaining old generated entities are still live. Exercise Grid `GeneratedGridAnnotationHandles`; Curtain `GeneratedCurtainFrameHandles` for both LINE and PATH builders; and representative Rebar owner slots covering longitudinal bars, stirrups/ties/shape bars and slab/wall/foundation mesh families. Each replacement attempt must refuse before committing any destructive erase for that owner set, create no partial replacement objects, preserve surviving old entities plus semantic handle metadata/ownership, and leave another DWG untouched. Also inject malformed and duplicate-canonical Rebar handle metadata and verify fail-closed behavior. Then restore a complete live set and prove exact replacement succeeds, old owned entities are removed exactly once, new handles/native ownership are complete, foreign/unmarked objects are never deleted, Undo is coherent, and save/reopen retains the final ownership state. Another DWG must never be mutated.
- Evidence required: Exact tested SHA including `761b9b92f5dd3638b18d281c273a406e41069511`, `ffd26294f3f27d03de1050643aa0aeb894dcb0f2`, `1850f02382c8ccf71f04e3ea9daa28455aaae08f`, and `b22eacd681230f231e0f970fb670e8f89769c35e`; Windows/BricsCAD V25 build; failure stage/exception; before/after semantic snapshot summary; for each generated owner-slot case record stored expected handles, live CAD handles/counts, native owner/XData identity and object counts before/after; proof stale/missing/malformed/duplicate-canonical cases leave surviving objects and metadata unchanged with no partial new objects; full-live replacement success; foreign-object protection; transaction/Undo result; active-DWG identity and no cross-DWG mutation; Recognition no-project-creation result; Door/Room/BBS detached live-state invariants; BQ canonical preference result; Rebar Mesh Setup reload/Save refusal with proof of no replacement project and no semantic/native mutation; unavailable-project palette before/after state and proof that stale callbacks cannot mutate the prior project; post-commit UI result; save/reopen result; sanitized evidence where useful.
- Evidence: `LOCAL_PASS` on exact SHA `fbf1e7923cbde9037637e5b6b1339b31f491c87a`; see `docs/agent-work-claims/2026-08-25-codex-issue3935-local011-runtime-pass.md` and carrier `#3935`. Canonical runner 21/21, same-SHA baseline/runtime smoke, generated/modeless harness, Curtain P06/P08/P09/P11/P12 and final process/DemandLoad/private-state cleanup all passed. Issue `#3989` remains a separate Recognition-window display defect and is not claimed fixed by this lane.
- Related source/docs: `src/QS3D.BricsCAD.V25/Cad/GridAnnotationBuilder.cs`; `src/QS3D.BricsCAD.V25/Cad/CurtainWallFrameSolidBuilder.cs`; `src/QS3D.BricsCAD.V25/Cad/CurtainWallPathFrameSolidBuilder.cs`; `src/QS3D.BricsCAD.V25/Cad/GeneratedRebarOwnershipGuard.cs`; `src/QS3D.BricsCAD.V25/Cad/GeneratedTieRebarOwnershipGuard.cs`; `scripts/preflight-generated-geometry.py`; `scripts/preflight-curtain-frame-native-ownership.py`; `scripts/preflight-rebar-native-ownership.py`; `docs/PROJECT-ROLLBACK-FAILURE-MATRIX.md`; `docs/EXISTING-PROJECT-MUTATION-CONTEXT.md`; `docs/LOCAL-V25-QUALIFICATION.md`; `docs/LOCAL-AGENT-CONTINUE-ALL-2026-08-10.md`
- Updated: 2026-08-25

## LOCAL-012 — Project Browser native workspace and CAD selection bridge

- Priority: P1
- Status: IN_PROGRESS
- Current source/artifact status: `SOURCE_FIX_4998_MERGED / WAITING_POST_MERGE_PUBLISHED_DESCENDANT / LICENSED_RUNTIME_PENDING`
- Current source boundary: PR #4998 final head `70043ca774b54f4f2fb6dc3b5710299ea1ec6dfc`; protected Shared CI `33374724885` passed `preflight` + `core`; merged to protected main as `80f609057bb95b58f08f3ea88ea22411b88cb558`. This supersedes `.10233` and every pre-merge Workspace crash candidate.
- Current release truth: latest published preview `.10270` targets pre-merge `f9cf4d2e8c9943e729a1034276920e015a95bc53`, so it is not admissible for this Workspace crash qualification. Wait for a published V25 descendant containing `80f609057b...`; publication/source CI still is not `LOCAL_PASS`. Release coordination: #5004.
- Current runtime gate: preserve all already accepted bounded PASS rows and do not rerun them; execute only affected/unfinished Workspace rows after an exact post-merge published descendant is pinned and the licensed host is exclusively allocated.
- Area: Project Browser / Workspace / modeless selection
- Why local: Core query/grouping/virtualization/selection/workspace-state coordination is source-safe, but final integration depends on real BricsCAD Editor implied selection, live ObjectId/handle resolution, modeless WPF palette lifecycle, document switching, focus/zoom behavior and Unicode/HiDPI rendering.
- Source-side status: REMOTE_DONE for exclusive Workspace Instance-selection boundaries (`8e89f4cf8aba98cec4c2b92116fbe4ab9ea9474e`, `5e67c17bddb47a7875befafafe6ee3245f327135`, guarded by `c61cf302ba3b1fe67050a2ec35f4cdbfed38f8b2`) and live Family-value Instance Reset (`5e08dde3b6eeb00d6a24fd377f22302c3937470f`, guarded by `e1b39133ed604921bfde67f204a3e71beaae5f42`). Runtime/modeless proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`; remote agents should only revisit if these source paths materially change.
- Source-side status (#1760): REMOTE_DONE at source/guard commit `020ee574a4c57dcb32bf0b375239a043722c5ba2` for subtype-aware Foundation Family filtering, Add `Tham số`/`Solid3D`, pre-mutation native-capability refusal, `QS3DBUILD3D` delegation, and same-family CAD-selection Family-row highlighting. Exact licensed V25 interaction remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`.
- Source-side status (#2399): REMOTE_DONE at task head `3d5dd48151e74380f1ed7e763f9bb9014c4a37ac`, merged by PR `#2524` at `main@1c79aa2e05270b4fb69af4ea0bb9e2b5decda245`, for the distinct plugin-owned `QS3D — Thuộc tính` palette, single-authoritative-editor reparenting, deterministic four-palette BIM docking/visibility restore, and persisted/fallback Properties palette sizing. Licensed V25 visual/modeless proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`.
- Scenario: Starting from the exact SHA containing `ProjectBrowserWorkspaceCoordinator`, wire/qualify the native Workspace/Project Browser adapter without persisting CAD ObjectIds/handles in Core state. CAD selection must re-resolve to stable semantic IDs, reveal/expand the correct Browser paths and update the multi-selection inspector. Browser node/element selection must re-resolve the current canonical project and live CAD handles at action time, select/zoom only the bound active DWG, and fail closed for stale/deleted/ambiguous IDs. For Workspace property scope, prove exactly one live semantic CAD selection may enter Instance scope; empty selection, one non-semantic CAD entity, one semantic plus one non-semantic entity, multiple semantic entities, ambiguous semantic resolution, or a semantic element whose Family was deleted/missing must all drop back to Family scope and must not mutate the previously selected Instance. Keep an Instance override row open, change the same Family property through another valid modeless writer, and exercise the case where post-commit Workspace UI refresh is unavailable/fails: Reset must use the Family value that is live at click time, never the value captured when the row was opened. If the Family property itself has disappeared, Reset must fail closed and require Refresh rather than writing an empty/stale override. After cache replacement/`QS3DRELOAD`, any stale Workspace Instance row must refuse mutation. Also activate a drawing whose project cannot be loaded while Workspace is visible: stale callbacks/content from the prior project must become non-actionable, and later activation/recovery of a valid project must rebind cleanly. Keep the modeless UI open while switching DWGs, forgetting/reloading project cache, deleting selected semantics, changing grouping/filter/query, paging large nodes, saving/reopening, and cancelling operations. Verify presentation-only browser state never increments semantic `ChangeVersion` or invalidates quantity/regeneration previews.
- Additional #1760 scenario: On the exact candidate SHA, choose `Móng > Móng Bè` and verify only `Móng Bè*` families/properties are shown; Add exposes `Tham số` / `Solid3D`; parameter creation produces and selects/scrolls `Móng Bè-N`; supported Solid3D dispatch reaches native `QS3DBUILD3D`; save/reopen introduces no duplicate subtype Family; an unsupported non-native Solid3D category refuses before project bootstrap, Family create/duplicate, activation, audit, or `ChangeVersion`; window-selecting multiple same-family raft objects keeps the matching Family row highlighted/scrolled into view; document/cache switching and 100/125/150/200% DPI must not permit stale or cross-DWG Workspace actions.
- Additional #2399 scenario: On a clean exact `main` descendant containing `1c79aa2e05270b4fb69af4ea0bb9e2b5decda245`, activate QS3D/BIM and verify `QS3D — Thuộc tính` is a distinct plugin-owned visible region; native BricsCAD Properties must not be counted as that region. Verify Workspace and dedicated Properties are the same authoritative editor/model state rather than two competing mutable editors. Exercise BIM activation/reassert, palette recreation, ordinary Workspace/Management/Quantity isolation, document switching, cache reload/unavailable-project transitions, and saved/fallback dock/size restore. No stale callbacks, cross-DWG edits, duplicate editors, or unintended collapse back to the old one-left-palette state are allowed.
- Evidence required: Exact QS3D SHA including the Workspace source-side commits above; Windows and BricsCAD V25 build; CAD→Browser and Browser→CAD selection matrix; before/after Workspace property scope for single semantic, empty, non-semantic, mixed semantic+non-semantic, multi-semantic, ambiguous, and missing-Family cases; selected CAD handles plus stable semantic IDs; proof that the previously selected semantic element is unchanged after every scope-dropping transition; live-Family Reset before/after values showing a same-project Family change is honored despite stale UI, removed-property Reset refusal, and post-reload stale-row refusal; unavailable-project palette reset/recovery evidence; stale/deleted/ambiguous ID refusal; active-DWG/document-affinity result; cache reload result; paging/filter/grouping state result; before/after semantic `ChangeVersion`; save/reopen; 100/125/150/200% DPI screenshots or sanitized notes; no cross-DWG mutation.
- Additional #1760 evidence required: Exact tested SHA plus matching plugin/V25 identity; subtype family names/counts before and after create/save/reopen; native `QS3DBUILD3D` dispatch evidence; before/after project existence, Family count, active Family, audit count and `ChangeVersion` for unsupported Solid3D refusal; same-family CAD/window-selection highlight evidence; document/cache/DPI and no-cross-DWG results.
- Additional #2399 evidence required: Exact tested SHA and matching plugin/V25 identity; sanitized BIM layout screenshots at representative 100/125/150/200% DPI and narrow/normal/wide host widths; proof plugin-owned Properties is distinct from native BricsCAD Properties; same-editor state continuity before/after reparenting; palette visibility/dock/size persistence and fallback/recreation results; document/unavailable-project transition outcomes; no stale callback, duplicate editor, or cross-DWG mutation.
- Evidence: Bounded CAD -> Excel -> CAD -> Workspace bridge `LOCAL_PASS` at clean source candidate SHA `e5725e96eed6dcebb46370c33e6f8a88e2cc2b68` on BricsCAD V25.2.10 with exact plugin SHA-256 `E8CB451012802098D38ECED664E936998045E36376A28A6E2E3FEF0EB772C0EB`. Production B4D/ED2 created one semantic Beam/detail row/summary row from a disposable BricsCAD-authored 5 m LINE; production Excel Locate set exactly one live PICKFIRST object. The visible Workspace then inspected exactly one matching selection, displayed `1 chọn`, entered Instance scope and resolved its Family. Wrong fingerprint, unknown element, stale Handle and partial resolution all refused while preserving the prior selection and semantic state; fixed V25 DemandLoad state was restored to `LoadCtrls=2`. This qualifies only the eligible single-selection bridge; the wider Browser/modeless/DPI/document/cache/property matrix remains `PENDING_LOCAL`.
- 2026-08-25 LOCAL-012 campaign attempt (`#3936`): clean exact source SHA `dffb7e334f997a981a9d918c23b67592e232d61f` passed exact-SHA/clean-tree, manual-CI-policy and generic source prechecks, then the canonical pinned qualification stopped at `Aggregate feature preflights`. A clean focused rerun reproduced `scripts/preflight-preflight-all-discovery.py` failing with `AssertionError: timeout reason must remain visible`. No plugin DLL was produced, BricsCAD was not launched, and none of the Workspace/Properties/#1760 runtime rows received a product verdict. The relevant two scripts are unchanged through then-current `main@ff6c298d33da2aec5e3e0f38503571899e185686`; source defect `#3946` owns the deterministic Windows timeout-regression repair. This is `BLOCKED_SOURCE_FIX / NO_RESULT`, not a Workspace PASS or FAIL. Do not retry either unchanged SHA; resume by running the canonical pinned qualification on a new exact clean merged SHA containing the `#3946` fix, then execute the full matrix above without local source patches.
- 2026-08-26 LOCAL-012 runtime checkpoint (`#3936`): exact source SHA `fc979ff465873ad3c32507064926292d9f10b3cb` and V25 adapter SHA-256 `DA5D2A92F0D65C02449787576D03D4AE512282A5E3A17A78E95EDBDD7DDC3733` passed the source/build/WPF prerequisites plus clean exact-DLL `NETLOAD` and temporary exact-DLL DemandLoad identity on BricsCAD V25.2.10 x64; installed Loader/`LoadCtrls=2` were restored. Real PICKFIRST selection passed single-semantic Instance scope and empty/nonsemantic/mixed Family fallback, but two same-Family semantic objects incorrectly retained Instance scope on that candidate. Source fix `#4027` later merged through PR `#4033` as `dd510bad3e56cae241d176764064db6b1c5d8fe6`; an exact descendant still needs the licensed rerun. Bounded exact-candidate/current-main inspection plus the real loaded Workspace also found that the Core Project Browser coordinator/planners had no production V25/V26 adapter reference or query/group/page surface; active source issue `#4032` owns that missing hosted integration, so those browser rows are `NOT RUN / SOURCE_FIX_REQUIRED` on this candidate. The bounded Foundation cell selected `Móng > Móng Bè`, exposed Add `Tham số`/`Solid3D`, created `Móng Bè-1`, dispatched native `QS3DBUILD3D`, materialized two `AcDb3dSolid` outputs, and preserved one new Solid3D Family across same-host close-reopen. A later live-Family Reset R1 attempt stopped after its exact runtime marker and before every mutation/assertion, with byte-identical disposable inputs; it is `NO_RESULT / INTERRUPTED_BEFORE_ASSERTION`, not PASS/FAIL. Cleanup then confirmed zero process and restored Loader/`LoadCtrls=2`. Earlier cells did not snapshot pre-launch `CurProfile`, so profile-pointer continuity is unproven; future cells must snapshot/restore it and use/remove only an owned nonce profile. Exact subtype naming, stale/deleted/ambiguous/missing-Family, live-Family Reset, cache/project/DWG lifecycle, dedicated Properties, Unicode and HiDPI remain `PENDING_LOCAL`. This is `PARTIAL_LOCAL_EVIDENCE / SOURCE_FIX_REQUIRED / IN_PROGRESS`, not full `LOCAL_PASS`.
- Related source/docs: `src/QS3D.Core/Navigation/ProjectBrowserWorkspaceCoordinator.cs`; `src/QS3D.Core/Navigation/ProjectBrowserSelectionPlanner.cs`; `src/QS3D.Core/Navigation/ProjectBrowserWorkspaceStateStore.cs`; `src/QS3D.BricsCAD.V25/UI/WorkspacePanel.xaml.cs`; `src/QS3D.BricsCAD.V25/UI/WorkspacePanel.SelectionInspection.cs`; `src/QS3D.BricsCAD.V25/UI/ViewModels/WorkspaceViewModel.cs`; `scripts/preflight-workspace-property-safety.py`; `docs/REMOTE-AGENT-SCOPE.md`; `docs/LOCAL-V25-QUALIFICATION.md`
- Additional #1760 source/docs: `src/QS3D.BricsCAD.V25/UI/WorkspacePanel.FamilySubtype.cs`; `src/QS3D.BricsCAD.V25/UI/WorkspacePanel.QuickDraw.cs`; `scripts/preflight-workspace-family-subtype-add.py`; issue `#1760`; PR `#1772`.
- Additional #2399 source/docs: `src/QS3D.BricsCAD.V25/PaletteCoordinator.cs`; `src/QS3D.BricsCAD.V25/UI/WorkspacePanel.DedicatedPropertiesPalette.cs`; `scripts/preflight-dedicated-qs3d-properties-palette.py`; `scripts/preflight-qs3d-properties-single-editor-authority.py`; issue `#2399`; PR `#2524`.
- Updated: 2026-08-31

## LOCAL-013 — clean-room BRC public capability and eligible CAD quantity round-trip

- Priority: P0
- Status: IN_PROGRESS
- Area: BRC public capability / Recognition / eligible CAD B4D / ED2 / Excel Locate
- Why local: Requires licensed BricsCAD V25, authorized BRC-containing drawing content and native public proxy/entity behavior. All source drawings and workbooks are reference-only; qualification must create disposable drawing copies and a new workbook/output before any command runs, and must never inspect BLT binaries or proprietary/internal BLT APIs.
- Scenario: On disposable copies only, run the automation-only `QS3DBRCPROBE` clean-room diagnostic and record only sanitized public-API capability/count evidence. Determine whether supported public BricsCAD APIs provide finite positive category-appropriate Length, Area or Volume for BRC/proxy entities. Then exercise `QS3DB4D` → a newly created `CHI_TIET`/`TONG_HOP` workbook → Excel Handle locate for an eligible CAD semantic row, verifying quantity/provenance and Handle ↔ active CAD object round-trip without modifying either reference original. If a proxy remains opaque or lacks a finite positive primary metric, keep it review-only and fail closed: do not auto-accept/capture it and do not claim a proxy quantity round-trip.
- Source/runner status: `QS3DBRCPROBE`, `QS3DBRCROUNDTRIPPROBE`, `scripts/test-bricscad-v25-brc-probe.ps1`, `scripts/test-bricscad-v25-brc-quantity-roundtrip.ps1` and their static preflights are implemented. Exact source commit `33744fb13c35d071f171793ff8aaaee945915fc2` additionally makes both runners dismiss only an exact visible `Proxy Information` `#32770` dialog owned by the BricsCAD PID that runner launched. Both runners remain automation-only, require an explicit disposable `*.reference-copy.dwg`, isolate runtime artifacts, restore process environment variables and record before/after DWG hashes. The round-trip runner refuses a pre-existing sidecar, produces a new workbook and validates a positive Excel-to-CAD selection path.
- 2026-08-15 TAS recognition atomicity source-side update: `REMOTE_DONE` for PR `#1681` through exact source/guard commit `b3b5533f2a312b0ed2162f17fab7154ab64365d7`. `RecognitionWindow` now submits one batch callback, `RecognitionApplyBatchService` performs live handle/category/readiness/ownership/project-version preflight before one outer `ProjectStateSnapshot`, manual selected rows remain QS-reviewable, while the `Áp dụng chắc chắn` button and AUTO/B4D revalidate the live `0.92` confidence / `0.15` margin gate before mutation. Mutation/audit failure restores the complete pre-batch semantic/audit state. Licensed V25 proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`; local execution must use the final merged `main` SHA containing this source commit, not the branch-only SHA.
- Additional atomicity scenario: On a disposable drawing with at least two capture-ready source rows, exercise `QS3DRECOGNIZE` manual selected batch, `Áp dụng chắc chắn`, `QS3DRECOGNIZEAUTO`, and `QS3DB4D`. Inject a late mutation/audit-stage failure after an earlier row could have succeeded and prove the complete semantic/audit batch rolls back: no partial semantic owner, no partial `recognition.apply`/`recognition.skip` history, no leaked `ChangeVersion`/timestamp delta attributable to the failed batch. For `Áp dụng chắc chắn` and AUTO/B4D, alter the live source layer/text so recomputed confidence or margin falls below `0.92 / 0.15` while category remains otherwise stable; confident apply must refuse the whole reviewed batch before mutation, and AUTO/B4D must classify that row as a preflight skip before the accepted mutation set commits atomically. Manual selected apply may still proceed only after explicit QS choice if category/readiness/ownership remain valid. Also switch active DWG and replace/reload the project between review and apply; every stale/cross-DWG path must refuse with no mutation.
- Additional atomicity evidence required: Exact final merged SHA containing `b3b5533f2a312b0ed2162f17fab7154ab64365d7`; matching plugin/Core ProductVersion and V25 build; before/after ProjectId, `ChangeVersion`, semantic owner counts and audit-event counts/actions; injected late-failure rollback proof; live confidence/margin drift results for confident/AUTO/B4D versus manual selected apply; active-DWG/project-replacement refusal; zero cross-DWG mutation. Source-safe guard: `scripts/preflight-recognition-atomic-batch.py`. Source files: `src/QS3D.BricsCAD.V25/Services/RecognitionApplyBatchService.cs`, `src/QS3D.BricsCAD.V25/ReviewCommands.cs`, `src/QS3D.BricsCAD.V25/UI/RecognitionWindow.xaml.cs`; design note: `docs/CUBICOST-TAS-RECOGNITION-ATOMICITY-2026-08-15.md`.
- Evidence required: Exact QS3D SHA; hashes or other non-sensitive identity for each disposable reference copy and confirmation that originals were not modified; sanitized aggregate `QS3DBRCPROBE` marker; public measurement/count results; B4D recognition/capture decision; ED2 new-workbook row/provenance summary; Excel Locate success/refusal result; and explicit opaque-proxy fail-closed evidence where applicable. `PASS` is allowed only with sanitized evidence tied to the exact tested SHA.
- Evidence: the historical positive lane passed at exact tested SHA `cd96507f942471d2030b3dbe0acc61f5fabfd5a7` on BricsCAD V25.2.10 x64. The disposable reference copy retained SHA-256 `7B5D54E620500564ADA20B8DDDE0FCA129E6A687644CE07D0BFB2B4A0D8B4B66` before and after both runs; the reference originals were not modified. The public-API probe opened all 352/352 current-space entities with zero read failures and found 25 proxy entities. No proxy exposed a finite positive direct Length, plan Area, surface Area or Volume; 24 proxies produced non-empty public `Explode` results with surface-only evidence across 304 positive Face parts, while exploded plan Area and Volume remained zero. Therefore all 25 proxies stayed review-only and no geometry or quantity was inferred. The B4D/ED2 probe produced 31 project elements, 31 `CHI_TIET` rows, one `TONG_HOP` row and 31 live export Handle references; the modern two-sheet 25-column workbook preserved drawing fingerprint plus Element ID/Handle provenance, and Excel Locate resolved and selected exactly one active CAD object. Its 25 proxy snapshots yielded zero capture-ready, zero auto-accepted and zero captured proxy owners. Artifact-tool inspection found no spreadsheet formula-error tokens and the rendered `CHI_TIET`/`TONG_HOP` sheets were legible.
- Prior current-candidate evidence: the same positive lane passed at exact source commit `33744fb13c35d071f171793ff8aaaee945915fc2` on BricsCAD V25.2.10 x64 with plugin SHA-256 `B1F4C1C32CB1D6515E6240E1165E0599BA9AD27FFF5051F8504A26238C164467`. Each runner dismissed exactly one matching proxy-information dialog owned by its launched PID; public probe completed in about 9 seconds and round-trip in about 7 seconds. The disposable copies and reference original retained SHA-256 `7B5D54E620500564ADA20B8DDDE0FCA129E6A687644CE07D0BFB2B4A0D8B4B66`; the reference workbook retained SHA-256 `EA11B81DA82D3C8235EAB25BA26578AC9B00CE3A6B057100ECA1C913E560FEB2`. Public counts remained 352/352 opened, zero read failures and 25 opaque proxies; B4D/ED2 remained 31 project elements, 31 detail rows, one summary row, 31 live export handles and one positively located/selected CAD object, with zero proxy capture-ready, auto-accepted or captured owners. No `.qsdb` sidecar or BricsCAD process remained. This evidence proves only an eligible CAD semantic quantity/Handle path in a BRC-containing drawing; it does not prove BRC proxy quantity parity. At that SHA the wrong-fingerprint, stale-handle, unknown-element and partial-resolution refusal cases were `NOT_RUN`.
- Exact negative-matrix evidence: exact clean source SHA `813c51ffe6b357c86a1e2a3c93d7f2d515c057b2` passed the automated source/build/offline-WPF plus licensed V25 NETLOAD/Ribbon/Palette baseline and the LOCAL-013 public/B4D/ED2 runtime on BricsCAD V25.2.10 x64. Plugin SHA-256 was `D57605AAAE9CC02A7B88564A44E823FF628F7EFFB5FAD5BE7B66F5DDA9137E11`. The public probe opened 352/352 entities with zero read failures and kept all 25 opaque proxies review-only; 24 proxies exposed public surface-only exploded evidence across 304 positive Face parts but no finite direct or exploded plan Area/Volume suitable for quantity capture. The V2 round-trip produced 31 semantic elements, 31 `CHI_TIET` rows, one `TONG_HOP` row and 31 live export Handle references, then positively resolved/selected exactly one eligible CAD target. Wrong fingerprint, unknown Element ID, stale Handle `0/1` and partial resolution `1/2` were each refused with the exact prior PICKFIRST selection preserved `4/4` and authoritative semantic state unchanged `4/4`. Proxy capture-ready, auto-accepted, captured-owner and proxy-locate-attempt counts were all zero. The reference original and both disposable copies retained SHA-256 `7B5D54E620500564ADA20B8DDDE0FCA129E6A687644CE07D0BFB2B4A0D8B4B66`; no `.qsdb` sidecar or BricsCAD process remained. Exact-current workbook rendering stayed `NOT_RUN` because the repository-approved spreadsheet dependency loader was unavailable, so LOCAL-013 remains `IN_PROGRESS`; package/parser/runtime validation passed, but no visual PASS is inferred. Evidence committed here is sanitized aggregate data only; no private path, raw Handle list, drawing or workbook is committed.
- 2026-08-15 exact-current visual follow-up: the approved spreadsheet dependency is now available, but the previously qualified drawing hash `7B5D54E620500564ADA20B8DDDE0FCA129E6A687644CE07D0BFB2B4A0D8B4B66` and its generated workbook are no longer present locally. Exact pushed task SHA `7e9c7886e1801226ade7dda34a501d71479bd761` passed the focused round-trip preflight and installed-reference V25 build with `0 warnings / 0 errors`; plugin SHA-256 was `0C9DEFF8ECD1D80ABB1088988F739BBD7C6591D542A4C5F293BBFE62655A6390` and Core SHA-256 was `4DAB56C92DCDA46157278564AE2705561BCA25E1FD3D75A3A7E267719141005E`, both ProductVersion-bound to that SHA. The only two exact-name local references had changed hashes `B013A4B1F87E69A5249EB8C23422DDF7A6A0AC042A18CEB3D0D1186A11DA2372` and `B2B080D14E6AB0296BA72DDF0B3400BBF23B353D85F4EF235D0440DE3720334C`; fresh disposable copies of both failed closed with sanitized `ROUNDTRIP_FAILED` before workbook creation. A detached historical control at previously passing SHA `813c51ffe6b357c86a1e2a3c93d7f2d515c057b2` also built `0 warnings / 0 errors` and rejected the older changed reference with the same marker, so no current-source regression is inferred. Artifact-tool structural/formula inspection and both-sheet rendering therefore remain `NOT_RUN`. All copies retained their hashes; no sidecar, workbook, metadata, BricsCAD process or probe environment remained, and private runtime roots were removed. Resume only with the restored `7B5D54...` input or another explicitly authorized BRC-containing reference known to produce a current B4D/ED2 workbook; then rebuild the newest exact pushed SHA, rerun once on a fresh disposable copy, and render every generated sheet. Issue `#1494` / PR `#1496` retain the blocked evidence; no private path, raw Handle, drawing or workbook is committed.
- 2026-08-23 exact eligible-CAD continuation: clean source candidate SHA `e5725e96eed6dcebb46370c33e6f8a88e2cc2b68` and exact V25 x64 plugin SHA-256 `E8CB451012802098D38ECED664E936998045E36376A28A6E2E3FEF0EB772C0EB` passed the production B4D -> ED2 -> Excel Locate positive and refusal matrix on a disposable DWG authored by BricsCAD with one recognized 5 m `beam` LINE. The drawing remained SHA-256 `6D6B7E7EBE11CB23DDD6021C4040F72D7AD30CC1AA4F93A2D34A1F585B4866DC`; the guarded standalone workbook SHA-256 was `2C51C6838B305BE52D8D2CFE5304F92C75DEAE596AA22C0CB08849C710FE5390`. One detail row, one summary row and one live selected CAD object passed; wrong fingerprint, unknown element, stale Handle `0/1` and partial resolution `1/2` all refused with prior PICKFIRST/semantic state preserved. No sidecar or process remained. The missing historical BRC input was not reconstructed, so this is eligible CAD quantity/selection evidence only and does not claim current BRC proxy parity; LOCAL-013 stays `IN_PROGRESS`.
- Related docs: `src/QS3D.BricsCAD.V25/BrcPublicProbeCommands.cs`; `src/QS3D.BricsCAD.V25/BrcQuantityRoundTripProbeCommands.cs`; `scripts/test-bricscad-v25-brc-probe.ps1`; `scripts/test-bricscad-v25-brc-quantity-roundtrip.ps1`; `docs/PRODUCT-BOUNDARY.md`; `docs/COMMANDS.md`; `docs/LOCAL-V25-QUALIFICATION.md`; `src/QS3D.BricsCAD.V25/Services/RecognitionApplyBatchService.cs`; `scripts/preflight-recognition-atomic-batch.py`; `docs/CUBICOST-TAS-RECOGNITION-ATOMICITY-2026-08-15.md`
- Updated: 2026-08-15

## LOCAL-014 — Plan-to-3D preview-to-commit and batch compensation

- Priority: P1
- Status: OPEN
- Area: `QS3DCONVERT2D` / `QS3DPLAN2WALLS` quick path, `QS3DCONVERT2DADV`, immediate native wall creation
- Source-side status: REMOTE_DONE in the current working source for the quick/no-prompt and advanced-prompt split, canonical project identity, drawing-unit and source eligibility/identity checks, deterministic same-ObjectId LINE/open-POLYLINE geometry fingerprints, per-created-element semantic regeneration and ownership-scoped batch compensation. Interactive proof remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`; static source review is not `LOCAL_PASS`.
- Why local: Exact Model Space/UCS state, live ObjectId/source geometry changes while advanced prompts are open, native Solid3d ownership, editor selection, unrelated dirty semantic state and ownership-scoped rollback require licensed BricsCAD V25.
- Scenario: On disposable drawings, run quick `QS3DCONVERT2D` and `QS3DPLAN2WALLS` with LINE/open-POLYLINE sources. Cancel selection before project creation; prove both quick commands show no Thickness/Height/BottomOffset prompt and use the active/preferred `ArchitecturalWall` Family values, or fallback `0.2 m / 3.0 m / 0 m` when projectless. Run `QS3DCONVERT2DADV` and cancel each of its three numeric prompts before project mutation/bootstrap. While an advanced prompt is open, switch DWG, Model Space/non-planar UCS or drawing-unit policy; delete/retype or geometrically edit a selected source; and make a project appear or replace the reviewed project. Every stale/cancel case must fail closed before snapshot, semantic mutation or native output. Then seed unrelated dirty semantic elements, run successful mixed LINE/open-POLYLINE quick and advanced batches, and prove only each new wall is regenerated while unrelated dirty flags remain unchanged. Inject a mid-batch native failure: source CAD must remain unchanged, only this batch's owned solids may be compensated, the semantic snapshot must restore, and another DWG must remain untouched.
- Evidence required: Exact SHA and V25 version; command/prompt count; default Family identity and effective values; before/after DWG/project identity, Model Space/UCS, drawing unit and deterministic source-geometry fingerprint; unrelated dirty element IDs/flags before and after; successful semantic/native ownership counts; injected-failure compensation/rollback; save/reopen and Undo/Redo. Evidence must be sanitized and must not include private drawing paths or raw Handle lists.
- P01 exact evidence: exact clean source SHA `21ca2d08427013f3ef8154708fef85fb2454ff8f` passed the strict BricsCAD V25 `Release|x64` build with zero warnings/errors and the automation-only `QS3DPLAN2DPROBE` quick-positive run on BricsCAD V25.2.10 x64. Plugin SHA-256 was `3CF3F79C22A8659743E4CC72F777CD30596ED794775714C6F0A342C477E2200C`. Two seeded LINE sources produced two semantic `ArchitecturalWall` owners and two live owned Solid3d outputs using fallback `0.2 m / 3.0 m / 0 m`; source geometry remained unchanged, ownership sets were disjoint, native length/thickness/min-Z/max-Z bounds passed and both Core/runtime blocking Health counts were zero. The disposable synthetic copy retained SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; no `.qsdb`/backup/lock, generated script or BricsCAD process remained. The run first exposed and then verified the one-line `WallSolidBuilder` centered-box Z-placement correction integrated by PR `#744`. P01 does not cover the alias, open-POLYLINE, preferred Family, advanced prompts, cancellation/drift, unrelated-dirty preservation, injected rollback, Undo/Redo or save/reopen, so it does not promote LOCAL-014 and the remaining matrix stays `PENDING_LOCAL`.
- P02 exact evidence: exact clean source SHA `7f57130470d4440f25dd27ea0bc3207cbb777a07` passed the strict BricsCAD V25 `Release|x64` build with zero warnings/errors and the guarded `QS3D_PLAN_TO_3D_P02_RUNTIME_V1` run on BricsCAD V25.2.10 x64. Automation-only `QS3DPLAN2DP02PREPARE` / `QS3DPLAN2DP02VERIFY` and `scripts/test-bricscad-v25-plan-to-3d-p02.ps1` drove the real production quick aliases. Plugin SHA-256 was `EE7FA1C5F1A28127622C76F9E246B2E1388E77ED1C2029E1167B7457EE336C80`. Production `QS3DCONVERT2D` and `QS3DPLAN2WALLS` converted one LINE and one open straight POLYLINE into two semantic `ArchitecturalWall` owners and two owned Solid3d outputs using the active preferred Family values `0.31 m / 4.2 m / 0.45 m`. The unrelated dirty semantic state and source geometry were preserved; ownership sets were disjoint; native bounds passed; both wall-scoped Core/runtime blocking Health counts were zero. The disposable copy retained SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; launched process, private script, `.qsdb`, `.bak` and lock state were absent after cleanup. P02 is `LOCAL_PASS` only for `P02_QUICK_ALIAS_POLYLINE_FAMILY_DIRTY_ONLY`; advanced prompt drift/cancel, rollback injection, Undo/Redo, save/reopen and the rest of LOCAL-014 remain `PENDING_LOCAL`.
- 2026-08-25 #3820 P03 advanced prompt-cancellation PASS: exact clean pushed source `1736ae8db0086041f0b1e8ce4b79839469b10061` passed licensed BricsCAD V25.2.10. Three fresh exact-PID processes cancelled `QS3DCONVERT2DADV` independently at Thickness, Height and BottomOffset with physical ESC; every case returned to idle with the same source LINE, zero Solid3d, no project/cache/sidecar/semantic/audit mutation, graceful exit code 0 and full DemandLoad/private-state/fixture cleanup. P03 is complete only for these advanced physical-ESC prompts; do not rerun without a material source change.
- 2026-08-25 #3825 P04 same-DWG advanced prompt-drift PASS: exact clean pushed source `f70c694d94f26bb1cc1be8025931d9a1a6559bb0` passed five fresh-process licensed V25.2.10 cases. While the first Thickness prompt was provably active, the harness independently changed the selected LINE endpoint, switched to a different planar UCS, changed `INSUNITS 4 -> 6`, made a project/cache appear after projectless preview, or advanced the same project's `ChangeVersion +1`; all five then completed the three numeric inputs and production failed closed before native/semantic/audit/sidecar mutation. Only the intentionally armed drift remained until cleanup, which restored source/UCS/unit/project state and left zero process/residue. P04 closes only this same-DWG drift cell; document/space/source delete-retype/project replacement and broader rollback/lifecycle rows remain pending.
- Evidence: P01-P04 are bounded `LOCAL_PASS`; overall LOCAL-014 remains `PENDING_LOCAL` for document/space/source delete-retype/project replacement drift, ownership-scoped batch compensation and rollback, Undo/Redo, save/cold-reopen and multi-DWG isolation.
- Related docs: `docs/PLAN-TO-3D-WORKFLOW.md`; `src/QS3D.BricsCAD.V25/PlanTo3DCommands.cs`; `src/QS3D.BricsCAD.V25/PlanTo3DRuntimeProbeCommands.cs`; `scripts/test-bricscad-v25-plan-to-3d.ps1`; `scripts/preflight-plan-to-3d-runtime-probe.py`; `scripts/preflight-plan-to-3d-project-lifecycle.py`; `scripts/preflight-plan-to-3d-source-geometry-freshness.py`; `scripts/preflight-plan-to-3d-quick-authoring.py`; `scripts/preflight-plan-to-3d-scoped-regeneration.py`; `docs/LOCAL-V25-QUALIFICATION.md`
- Updated: 2026-08-14

## LOCAL-015 — Construction Reference Search browser/modeless runtime

- Priority: P2
- Status: OPEN
- Area: `QS3DREFSEARCH` / modeless browser launcher
- Source-side status: REMOTE_DONE for the document-bound command/window, fixed HTTPS category mapping, URL encoding, bounded query input, SafeSearch parameters, Windows shell-browser launch, no-scrape boundary and source-safe preflight. Exact Windows/BricsCAD/browser behavior remains `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`.
- Why local: Requires licensed BricsCAD V25 on Windows, a real default browser association, modeless WPF/document lifecycle, and active-DWG switching.
- Scenario: From a clean exact candidate SHA, run `QS3DREFSEARCH` and verify the modeless window renders with the QS3D theme. For `Ván khuôn móng` and at least one Unicode variant, exercise Hình ảnh, Web, Video, Mua sắm, Video ngắn and Tin tức; each action must open the intended fixed HTTPS provider URL in the Windows default browser with the query URL-encoded and SafeSearch enabled. Enter in the query box must map to Hình ảnh. Each quick-query button must only fill the query until a result category is chosen. With technical context enabled, verify the construction suffix is added before encoding. Empty input and input longer than 512 characters must refuse without launching a browser. Switch to another DWG while the old modeless window remains open and verify every launch attempt fails closed; reactivate the source DWG and verify launch resumes. Close the source drawing and verify `DocumentBoundWindowLifetime` tears down or disables the bound window without affecting another DWG. Confirm no `.qsdb`, semantic project state, CAD object, selection, audit state or drawing bytes are mutated by opening/using the launcher.
- Evidence required: Exact tested SHA; Windows build; BricsCAD V25 build; default-browser identity; sanitized results for all six categories; Unicode/encoding and SafeSearch proof; Enter/quick-query/technical-context behavior; empty/>512 refusal; active-DWG switch refusal and source-DWG reactivation; source-drawing-close lifecycle; before/after proof of no project/CAD/audit mutation. Do not record private search terms or browsing history beyond sanitized test inputs.
- Evidence: PENDING_LOCAL
- Related source/docs: `src/QS3D.BricsCAD.V25/ReferenceSearchCommands.cs`; `src/QS3D.BricsCAD.V25/UI/ReferenceSearchWindow.xaml`; `src/QS3D.BricsCAD.V25/UI/ReferenceSearchWindow.xaml.cs`; `scripts/preflight-construction-reference-search.py`; `docs/CONSTRUCTION-REFERENCE-SEARCH.md`
- Updated: 2026-08-11

## LOCAL-016 — BricsCAD V26 native authoring and dependent-output qualification

- Priority: P0
- Status: IN_PROGRESS
- Area: issue `#1462`; V26 `.NET 8` native authoring/semantic/generated-geometry lifecycle
- Why local: Requires licensed BricsCAD V26 x64, an interactive Windows desktop, the exact `net8.0-windows` plugin, real native editor commands and fresh-process save/cold-reopen behavior. V25 evidence is not transferable.
- Source/runner status: issue `#3573` owns bounded P01. `scripts/test-bricscad-v26-native-beam-dependent.ps1` reuses the guarded production Beam/native-MOVE/reconcile/dependent-rebuild/cold-reopen probe through a host-major-aware shared runner. Exact-source binding accepts the declared stable ProductVersion only when the colocated portable PDB SourceLink record names the exact Git SHA. P01 is `LOCAL_PASS` at the exact SHA recorded below; the broader V26 native/private-DWG matrix remains `PENDING_LOCAL`.
- Scenario: P01 runs on a disposable copy of `samples/generated/QS3D-Sample.dwg`: NETLOAD the exact V26 DLL; create a 5 m production Beam; build host, four longitudinal bars and six stirrups; native-MOVE only the authoritative LINE by +1 m WCS Y; prove old generated output is unchanged before sync; run `QS3DSYNCSOURCE`; rebuild all dependent families; verify scoped Health; save drawing/sidecar; close; cold reopen in a new V26 process; verify source/semantic/generated continuity; restore the fixture copy and leave zero BricsCAD processes.
- Evidence required: exact pushed SHA, V26 host/CLR/x64/native-runtime identity, declared ProductVersion, plugin SHA-256, repository fixture SHA-256, production marker booleans, drawing/sidecar persistence, fresh-process cold reopen, script/private-state/drawing restoration and zero process residue. No raw paths, ProjectIds, handles, proprietary DLLs or customer drawings may be published.
- Evidence: P01 `LOCAL_PASS` on clean, committed and pushed exact SHA `05bfc038a0b64d611a45d9eea40b239353b56a70`. Focused V26/P01 preflights and PowerShell AST passed; the V26 `Release|x64` rebuild completed with `0 warnings / 0 errors`. The exact stable ProductVersion was `0.1.0-preview.10081`, portable PDB SourceLink named the tested SHA for both plugin and Core, and the plugin SHA-256 was `AF7BFE61AF9E6CE3DFDA7DC2E8B18DA7D88139336872363DE8C6AE96913DBCB7`. Licensed generic NETLOAD/runtime control passed on BricsCAD V26.2.07, CLR 8.0.29, x64 with native runtime major/label/match, Ribbon, Workspace and Right Panel true and Quantity Insight intentionally false. The two-process production matrix then created the 5 m Beam and its host/four longitudinal bars/six stirrups, native-MOVED only the source LINE by +1 m WCS Y, proved pre-sync output isolation, reconciled the source, invalidated and rebuilt all three dependent families, passed scoped Health, persisted DWG plus sidecar and passed fresh-process cold reopen; every final marker boolean was true with `error_code=NONE`. Repository fixture SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; drawing restoration and process/script/private-state cleanup all passed with zero BricsCAD processes. The same runner pair also rejected a deliberately nonexistent `DOTNET_ROOT` before artifact creation or host launch, preventing the prior opaque NETLOAD timeout class. This closes only LOCAL-016/P01 and does not qualify the remaining V26 native/private-DWG matrix or customer release.
- 2026-08-23 exact-current V26 continuation: source candidate SHA `9a77d329e90809a2006d8e4dc1bafc995c0a8ca2` built and loaded exact net8.0-windows x64 plugin SHA-256 `F063EC199DD50A25FBF619D54D9F8492B94065E3C7E5E4AF0454B54252BD3AFC` in licensed BricsCAD V26.2.07. Production repeated Direct Draw passed independent two-segment Wall and Beam sequences, eight hosted `WorldDraw` callbacks per Family, Enter, physical ESC, planar UCS, document-switch isolation, native/semantic Undo/Redo, save/sidecar and fresh-process Wall/Beam cold reopen. On predecessor candidate `e5725e96eed6dcebb46370c33e6f8a88e2cc2b68` and plugin SHA-256 `83AE13A31EFE191DAC8A4AC3727325919F07DE59FD9E66BE8A0A04F828E8E779`, CLR 8.0.29 generic native host identity, Ribbon, Workspace and Right Panel passed, and the enhanced LINE lifecycle passed Direct Draw, native MOVE/ROTATE/STRETCH, source reconcile/rebuild, Undo/Redo, two-DWG refusal/isolation/reactivation/close and cold reopen. Every run ended with zero BricsCAD processes. This qualifies those bounded native workflows only; private-DWG, clean-machine packaging/signing and release remain pending under #1462.
- 2026-08-25 V26 package install/uninstall lifecycle closeout (#3878 / #3879 / #3916 / #3922): the source fix landed at exact merged source `5da966686826a350d8babc8f22a390ab29ec824b`; the unchanged production lifecycle runner subsequently passed licensed Windows/V26 qualification at exact clean runtime-tested source `e90c6aba7ef7bf903042d42dd991f9e7112fe659` on BricsCAD V26.2.07 x64 with canonical profile `V26x64/en_US`, ProductVersion `0.1.0-preview.10081`, generated package SHA-256 `60F5239611B13F424BAE49922E5D34ADF3FC12C3064BF7506FE06CD27B8B3F7C`, and V26 `Release|x64` build `0 warnings / 0 errors`. This is `LOCAL_PASS / BOUNDED` only for the package install/uninstall lifecycle; current/newer `main` is not relabeled runtime-tested. DemandLoad/NETLOAD behavior, signing, SECURELOAD/trust, update/rollback/cancel, private/customer-DWG, UI/DPI, publication and the broader V26 matrix remain pending under #1462. Do not rerun this unchanged bounded cell unless a material package/install source change explicitly reopens qualification.
- Package lifecycle evidence: `LOCAL_PASS / BOUNDED` at exact runtime source `e90c6aba7ef7bf903042d42dd991f9e7112fe659`; sanitized evidence PR #3916 merged as `d67119defb31d8649ea7099e2c45bc53996331b5`. Canonical status closeout: #3922.
- Related source/docs: `docs/LOCAL-V26-QUALIFICATION.md`; `scripts/test-bricscad-v26-native-beam-dependent.ps1`; `scripts/preflight-v26-native-beam-dependent.py`; `src/QS3D.BricsCAD.V25/SourceReconcileNativeBeamDependentRuntimeProbeCommands.cs`; `scripts/test-v26-package-install-lifecycle.ps1`; `scripts/preflight-local016-v26-package-source-ready-handoff.py`; `docs/LOCAL-V26-PACKAGE-INSTALL-LIFECYCLE.md`; issue `#3573`; issue `#3878`; PR `#3879`; PR `#3916`; issue `#3922`.
- Updated: 2026-08-25

## LOCAL-017 — BricsCAD V26 native Slab POLYLINE qualification

- Priority: P0
- Status: PASS
- Area: issues `#80`, `#1462`, and bounded carrier `#3576`; V26 `.NET 8` native source-edit lifecycle
- Why local: Requires licensed BricsCAD V26 x64, the exact `net8.0-windows` plugin, a real top-level native crossing-window `STRETCH`, production reconcile/rebuild and a fresh-process save/cold-reopen sequence. Existing V25 evidence is not transferable.
- Source/runner status: the V25 P02 runner is host-major-aware while preserving its default V25 contract; `scripts/test-bricscad-v26-native-polyline-edit.ps1` is the thin V26 entrypoint. Exact-source binding accepts the declared stable ProductVersion only when the colocated portable PDB SourceLink record names the exact Git SHA. The runner also fails closed on an invalid configured V26 `DOTNET_ROOT` before artifact creation or host launch.
- Scenario: On a disposable copy of `samples/generated/QS3D-Sample.dwg`, NETLOAD the exact V26 DLL; use production Direct Draw to create and build a closed 4 m by 3 m Slab; run one real top-level native crossing-window `STRETCH` that changes only the top-right source vertex from `(4,3)` to `(5,3)` m while excluding the overlapping generated solid; prove the old solid is unchanged before sync; run `QS3DSYNCSOURCE`; verify area/perimeter/quantity refresh and generated invalidation; run `QS3DBUILD3D`; verify distinct expected native output and scoped Health; save drawing/sidecar; close; cold reopen in a new V26 process; restore the fixture copy and leave zero BricsCAD process residue.
- Evidence required: exact clean pushed SHA, V26 host/CLR/x64 identity, declared ProductVersion, plugin SHA-256, repository fixture SHA-256, production marker booleans, drawing/sidecar persistence, fresh-process cold reopen, script/private-state/drawing restoration and zero process residue. No raw paths, ProjectIds, handles, proprietary DLLs or customer drawings may be published.
- Evidence: P02 `LOCAL_PASS` on clean, committed and pushed exact SHA `54b7fce6127208085817f20dd0781b580a18e4bd`. Focused P02/V26 guards, PowerShell AST and all 988 discovered aggregate feature gates passed; the V26 `Release|x64` build completed with `0 warnings / 0 errors`. Exact-source binding accepted ProductVersion `0.1.0-preview.10081` only after both plugin and Core portable PDB SourceLink records named the tested SHA. Plugin SHA-256 was `BA0A0758BD2440D50455FDD9A36D992EE14869800B1700396CE5580F53E1882D`. Licensed BricsCAD V26.2.07 / CLR 8.0.29 passed the generic x64/native-runtime identity gate. The two-process production matrix then created and built the 4 m by 3 m Slab, real native-STRETCHed only the selected closed-POLYLINE source vertex, proved the old solid unchanged before sync, reconciled area/perimeter/quantity state to the 13.5 m2 quadrilateral, invalidated/rebuilt the owned solid, passed scoped Health, persisted DWG plus sidecar and passed fresh-process cold reopen; every final marker boolean was true with `error_code=NONE`. Repository fixture SHA-256 remained `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; drawing restoration and process/script/private-state cleanup all passed with zero BricsCAD processes. A deliberately invalid `DOTNET_ROOT` was also rejected before artifact creation or host launch. This closes only the bounded LOCAL-017/P02 cell and does not qualify parent #80, the remaining #1462 V26 native/private-DWG matrix or customer release.
- Related source/docs: `docs/LOCAL-V26-QUALIFICATION.md`; `scripts/test-bricscad-v26-native-polyline-edit.ps1`; `scripts/preflight-v26-native-polyline-edit.py`; `src/QS3D.BricsCAD.V25/SourceReconcileNativePolylineEditRuntimeProbeCommands.cs`; issue `#3576`.
- Updated: 2026-08-22

## LOCAL-018 — exact V26 LINE and repeated Direct Draw lifecycle

- Priority: P0
- Status: PASS
- Area: issues `#80`, `#1462`, completed `#3578`, and carrier `#3612`; V26 native editor/document/repeated lifecycle
- Why local: Requires licensed BricsCAD V26 x64, the exact net8.0-windows plugin, real editor commands, exact-process physical ESC, live document switching and fresh-process reopen.
- Scenario: On the exact candidate SHA, load the V26 `net8.0-windows` plugin in licensed BricsCAD V26 x64; qualify production repeated Wall/Beam Direct Draw through DrawJig preview, accepted segments, Enter, exact-process physical ESC, planar UCS, document switching and whole-command Undo/Redo; then qualify production LINE Direct Draw through native MOVE/ROTATE/STRETCH, reconcile/rebuild, two-DWG refusal/isolation/reactivation/close, save and fresh-process cold reopen.
- Evidence required: Exact candidate SHA; V26 host, CLR, x64, plugin and portable-PDB identity; DrawJig, ESC, UCS, document-switch and Undo/Redo booleans; native LINE edit/reconcile, multi-DWG, save and cold-reopen booleans; fixture/script/private-state restoration and zero BricsCAD process residue. Publish no raw paths, private identifiers, handles, proprietary DLLs or customer drawings.
- Evidence: `LOCAL_PASS` for repeated mode at source candidate SHA `9a77d329e90809a2006d8e4dc1bafc995c0a8ca2`, BricsCAD V26.2.07 and plugin SHA-256 `F063EC199DD50A25FBF619D54D9F8492B94065E3C7E5E4AF0454B54252BD3AFC`. Its Wall/Beam matrix passed database-free DrawJig preview with eight hosted `WorldDraw` callbacks per Family, four accepted segments total, Enter, exact-process physical ESC, planar UCS, document-switch isolation, whole-command Undo/Redo, save and fresh-process Wall/Beam cold reopen. On predecessor candidate `e5725e96eed6dcebb46370c33e6f8a88e2cc2b68`, BricsCAD V26.2.07 / CLR 8.0.29 and plugin SHA-256 `83AE13A31EFE191DAC8A4AC3727325919F07DE59FD9E66BE8A0A04F828E8E779`, the generic identity gate proved the correct host major/runtime and native Ribbon/Workspace/Right Panel, and the enhanced LINE matrix passed production Direct Draw, native MOVE/ROTATE/STRETCH, pre-sync isolation, reconcile/rebuild, native Undo/Redo, two-DWG wrong-document refusal/isolation/reactivation/close, save/sidecar and cold reopen. Exact source/PDB identity, fixture restoration and zero-process cleanup passed in every run.
- Scope: This confirms the bounded #3578/#3612 native lifecycle only. Parent #80 remains open for its broader topology/category/dependent/failure matrix; parent #1462 remains open for private-DWG, clean-machine package/signing and release qualification.
- Related source/docs: `docs/agent-work-claims/2026-08-23-interactive-issue3612-repeated-directdraw-v25-v26.md`; `docs/LOCAL-V26-QUALIFICATION.md`; `scripts/test-bricscad-v26-runtime.ps1`; `scripts/test-bricscad-v25-source-reconcile-native-line-edit.ps1`; `scripts/test-bricscad-direct-draw-repeated-mode.ps1`
- Updated: 2026-08-23

## LOCAL-019 — six-sheet QS Review export and Excel-to-Model Locate

- Priority: P0
- Status: PASS
- Area: issue `#3536`; `QS3DREVIEWEXPORT` / `QS3DREVIEWLOCATE`; BricsCAD V25 + V26 host bridge
- Why local: Final acceptance requires licensed BricsCAD hosts to `NETLOAD` the matching `net48` V25 and `net8.0-windows` V26 assemblies, create native entities, export a real XLSX package, establish native PICKFIRST selections, and prove refusal/selection preservation against stale trace data. Source/build evidence alone is not `LOCAL_PASS`.
- Source/runner status: The V25 source defines the production six-sheet export/locate service and automation-only `QS3DREVIEWROUNDTRIPPROBE`; V26 consumes the same source through its existing linked-source boundary. `scripts/test-bricscad-review-workbook-roundtrip.ps1` requires a clean exact pushed SHA, matching portable-PDB SourceLink identity, the repository Release output for the selected host major, a disposable `*.review-probe-copy.dwg`, and an empty ignored artifact directory.
- Scenario: On separate disposable drawing copies, run the exact pushed candidate first in licensed V25 and then in licensed V26. Each host must export the exact ordered `01_TONG_HOP`, `02_CHI_TIET_QTO`, `03_CLASHES`, `04_DUPLICATES`, `05_RULES`, `06_MODEL_INFO` workbook from three semantic/native elements and persisted coordination state without re-running detectors. Read all three traceable sheets through the production reader/resolver and verify PICKFIRST counts `1/2/2`. Then refuse wrong drawing fingerprint, wrong model revision, stale Handle and partial pair resolution while preserving the prior PICKFIRST set and authoritative semantic stamp in all four cases.
- Evidence required: Exact clean pushed SHA; V25/V26 host version and x64 identity; matching assembly name and plugin SHA-256 per host; exact six-sheet order; QTO/clash/duplicate row and locate counts; four negative refusals with PICKFIRST and semantic state preserved; source drawing hash unchanged; no QSDB sidecar; zero matching BricsCAD process residue. Publish only sanitized aggregate evidence and retain raw workbook/metadata under ignored `artifacts/`.
- Evidence: `LOCAL_PASS` on the same exact clean pushed source SHA `9cfff87262d7a7117c5ef1f03b486271a0723fa3`. Licensed BricsCAD V25.2.10 x64 loaded `QS3D.BricsCAD.V25` (SHA-256 `0CDEB3FD5545EFBFA4B63A07856857A41A2D313115B5A940842898F2D92A34AD`) and licensed BricsCAD V26.2.07 x64 loaded `QS3D.BricsCAD.V26` (SHA-256 `B5B548E9BF99E009B14B4D188CA47366707C00254A2C87D57215177186045F1B`); both resolved the same candidate `QS3D.Core` (SHA-256 `97EDEC75303642D4568D3707E973CAC4E8FD0CE9F71E60E22036B6DDE3E93494`). Each host exported the exact ordered six-sheet workbook with QTO summary/detail, clash and duplicate counts `3/3/1/1`; production Excel-to-Model Locate established PICKFIRST counts `1/2/2`; and all four wrong-fingerprint, wrong-revision, stale-handle and partial-resolution cases refused without changing PICKFIRST or authoritative semantic state. Both source drawing hashes were unchanged, no QSDB sidecar or matching BricsCAD process remained, and the isolated V25 startup DemandLoad state was restored. Read-only workbook inspection rendered all six sheets from both hosts and found zero formula-error tokens. Raw workbooks, markers and metadata remain under ignored `artifacts/`.
- Related source/docs: `docs/LOCAL-V25-QUALIFICATION.md`; `docs/LOCAL-V26-QUALIFICATION.md`; `src/QS3D.BricsCAD.V25/ReviewWorkbookCommands.cs`; `src/QS3D.BricsCAD.V25/ReviewWorkbookRuntimeProbeCommands.cs`; `src/QS3D.BricsCAD.V25/Services/ExcelLocateResolutionService.cs`; `src/QS3D.Core/Export/Qs3dReviewWorkbook.IssueProjection.cs`; `scripts/test-bricscad-review-workbook-roundtrip.ps1`; issue `#3536`.
- Updated: 2026-08-24

## LOCAL-021 — Móng Bè workflow and Quantity Insight viewport highlight

- Priority: P0
- Status: IN_PROGRESS
- Area: issue `#4041`; BricsCAD V25 Móng Bè Add/Edit/native 3D/Quantity Insight viewport highlight
- Why local: Acceptance requires the licensed interactive BricsCAD V25 host, real modeless Workspace/Properties rendering, native `Solid3d` geometry, direct viewport highlighting, and fresh-process DWG reopen; hosted/static/source/package evidence cannot establish these behaviors or be promoted to `LOCAL_PASS`.
- Source/artifact status: `10230_TERMINAL_NO_RESULT_DO_NOT_RETRY / SOURCE_FIX_4998_MERGED / WAITING_POST_MERGE_PUBLISHED_DESCENDANT / LICENSED_RUNTIME_PENDING`
- Remote disposition: `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`
- Consumed runtime artifact: `v0.1.0-preview.10230`; source `9c13be8d8f34a0b3b0a66ceba431a019f3c695d0`; exactly one licensed V25 invocation ended `NO_RESULT / STARTUP_WPF_VIRTUALIZATION_EXCEPTION_AFTER_NETLOAD_BEFORE_BASELINE`. Product/probe NETLOAD completed, then the `VirtualizingStackPanel.SetVirtualizationState` exception family stopped execution before Workspace baseline; `stages=[]`, so none of the preregistered Móng Bè/formwork acceptance matrix ran. Guarded pre-state restoration and literal `HOST_RELEASED -> #72` cleanup were accepted. `.10230` is consumed and must not be retried or repointed.
- Successor source fix: the earlier #4268 ModelTree containment was necessary but insufficient. Issue #4173 is now closed after PR #4998 merged the remaining grouped `WorkspacePanel.PropertyList` first-layout containment: final PR head `70043ca774b54f4f2fb6dc3b5710299ea1ec6dfc`, protected Shared CI `33374724885` with `preflight` + `core` SUCCESS, merge `80f609057bb95b58f08f3ea88ea22411b88cb558`. Source/build evidence is not `LOCAL_PASS`.
- Current release truth: `.10233` is superseded by the later PropertyList repro/fix. The newest published preview `.10270` targets `f9cf4d2e8c9943e729a1034276920e015a95bc53`, the parent before merge `80f609...`; therefore `.10233`, `.10270` and every earlier preview are not admissible. Wait for an exact published V25 descendant containing `80f609057b...`; coordination issue #5004 owns only publication readiness, not runtime PASS.
- Runtime gate: do not touch the shared licensed host until a published V25 descendant containing `80f609057b...` is pinned. Then require one fresh LOCAL-021 task identity, exact tag/source/package/adapter/Core/probe/runner hashes, literal #72 exclusive-host admission, zero pre-existing BricsCAD processes, one bounded invocation, and exact cleanup. The consumed `.10230` task/allocation is never reusable.
- Scenario: prove Workspace baseline without the historical startup crash; `Móng > Móng Bè > Add` creates and auto-selects `Móng Bè-N`; prove the dedicated schema (`Tên Family`, `Loại=Móng Bè`, `Tầng`, `Dày`, `Cách đặt`, `Cao độ đầu` with strict `bottom_level`/`top_level`, Display, Mark/Comment/WBS and material fields) without generic Foundation/`Bề dày`/`Offset đáy`; create a 4 m × 6 m × 0.8 m native raft with `19.2 m³`, correct bottom/top Z, edit/regenerate thickness `800 -> 900 -> 800 mm`, Quantity Insight row/highlight behavior, save/QSAVE plus fresh-process cold reopen, and complete cleanup on licensed V25.
- Evidence: historical `.10222`, `.10225` and `.10228` attempts remain `NO_RESULT`; `.10230@9c13be8d8f34a0b3b0a66ceba431a019f3c695d0` is terminal `NO_RESULT / DO_NOT_RETRY` after NETLOAD and before baseline. No LOCAL-021 `LOCAL_PASS` exists yet. `.10233` and `.10270` are pre-#4998 artifacts and are not eligible for the next licensed run.
- Evidence required: exact artifact/source/host identity; exact visible property labels and auto-selected Family; semantic/native ownership and 4×6×0.8 m geometry; `19.2 m³`; bottom/top Z and thickness-regeneration values; Quantity Insight/highlight observations; save/fresh-process cold reopen; sanitized Loader/DemandLoad/profile/UI restoration and stable zero owned-host residue.
- Related docs: `docs/LOCAL-V25-QUALIFICATION.md`; `docs/LOCAL-QUANTITY-REVIEW-CUBICOST-ACCEPTANCE.md`; issues #4041, closed #4173, #4282 and #5004; source fix PR #4998.
- Updated: 2026-08-31

## LOCAL-022 — Móng đơn placement/edit/save-reopen on V25/V26

- Priority: P0
- Status: IN_PROGRESS
- Area: issue `#4034`; BricsCAD V25/V26 Móng đơn Add/placement/edit/regenerate/save-reopen
- Why local: Final acceptance requires licensed interactive BricsCAD V25/V26 placement, native Foundation geometry, editing/regeneration, persistence and cold-reopen behavior that offline/static gates cannot prove.
- Source/artifact status: `NATIVE_API_V25_V26_LOCAL_PASS_BOUNDED / PHYSICAL_UI_V25_LOCAL_PASS_BOUNDED / PHYSICAL_UI_V26_FAIL / EXTENDED_IDENTITY_PENDING`
- New admitted viewport successor (#5840 / #5841): pushed source `87aff7fec452f9a8dd9f641ef84d143edc73514d`; V25 ZIP `6da38fcb3bc5fdb1989e9397fad45da712bf4af6b7690298a2fe83657bcb10ac`; V26 ZIP `59498948341f36d408f8bf838e177170c19d99ffc64acaa2070b0524e6b99a81`. This supersedes older pins for new UI allocations. Actual extracted WPF layout regression RED→GREEN and both exact-source host builds pass; these do not establish licensed acceptance. Preserve prior consumed allocations. Fresh V25 UI/save/cold-reopen plus cleanup is required before V26 on the same source; no MCP or installed-package change.
- Latest UI disposition (2026-09-05): V25 allocation `ui-property-fit-v25-29`, RunId `53fafd79a2b5456fb9226a43405aa47b`, exact product `87aff7fec452f9a8dd9f641ef84d143edc73514d`, harness `6976ea8f599d5a3147123ac874b873116a0de8f0`, passed all three physical UI/save/cold-reopen phases on BricsCAD25.2.10, including direct Family-scope H2 edit and native regeneration. Matching V26 allocation30 failed before placement with no available mapped drawing viewport. Allocation `ui-native-panels-v26-31`, RunId `ea4d2ef85fae4f9abe7cee19c1b1d3f0`, harness `8242ccbd307393c38abecd585bea411fa0b8737a`, hid native panels but failed `ui_selecttree / UI_TIMEOUT_SELECTTREE`: actual mouse ancestry was the adjacent hint, not the requested tree row. Zero V26 UI phases qualified. All three allocations verify full private/profile/protected-state cleanup and exact autostart restoration. No consumed allocation may be relabelled or replayed as PASS; no coordinate offset workaround is admitted without evidence.
- Source correction landed: #5840 / #5841 merged at `34e2609a0fd125fd1bf020c1d4e4787dd75134a7`, exact PR head `1002e18b6f25e348aa8c097eeed4ed4869aecaab`; fresh PR run `33954888075` and push run `33954886020` passed protected preflight/core. Issue #5840 is closed. Current main `8f86ff3676c0c6cff2697795f444d88836918d5d` contains that merge. Runtime evidence remains bound to the frozen `87aff7fec` packages, not the newer merge/head. Older checkpoints below are historical only.
- Current admitted UI successor (#5829 / PR #5834): exact pushed source `f0146aacef0b398bc71e8e278e6f7675432c1f17`, V25 ZIP `ee1f820baaa93d1b5e636432e317ca76c632b856480450467449af249c2636c0`, V26 ZIP `4410e6b00caf15c99f7a974254debaffa255987e566f4ca54d1717e5355291a8`. This supersedes the older package pins below for new UI allocations. #4586 / #4594 is merged (`c41837ccca9594ffffe4991999912f235a9e1cd3`), fixing Add routing and numeric 0/1 coercion. New #5829 retains the specialized validated/native-regenerating Family editor on direct scope switch. Both builds/host-free guards pass; direct scope edit/save/cold-reopen licensed evidence remains pending. Old-source V25 physical Add/Cancel/six values/two centres/Enter passed individual assertions, but consumed allocations 15–24 produced no complete UI PASS. Full cleanup/restoration is verified. All historical native bounded PASS receipts remain unchanged.
- Latest admitted successor: pushed source `0fc1ced48a089267246e78fe4ceeadc36cd5a2e7`, after physical Add exposed and #4586 corrected 0/1 dimension-to-boolean coercion during Family display. V25 ZIP `0d2032d4be962ab3b321abf1292bd9fd67e59ae09172cef674421bed430c2f05`; V26 ZIP `5fa8eeb26ead7719b2f27f363f22ea4ae85af91addd1c6d7500d8b8e5887c05d`. Earlier 0db6e6595 UI allocations 15/16 remain FAIL with full cleanup. Latest pair is a local PR candidate, not official release; fresh UI acceptance still pending.
- Remote disposition: `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`
- Consumed exact runtime source: `28e48e6dbd4e8976b31ab16f344ce0f76d7a7bf5`
- Consumed runtime verdicts: V25 = `NO_RESULT / STARTUP_WPF_VIRTUALIZATION_EXCEPTION_BEFORE_BASELINE`; V26 = `NO_RESULT / STARTUP_WPF_VIRTUALIZATION_EXCEPTION_BEFORE_BASELINE`. Both reached product/probe `NETLOAD` and `QS3D WILL_START`, but the product baseline never arrived and the Móng đơn acceptance matrix was not run. Both cells completed guarded cleanup and ended `HOST_RELEASED`; these historical attempts remain non-results and are not superseded/relabelled by the later bounded passes below.
- Current bounded evidence (2026-09-05, #5718 / PR #5738): licensed V25.2.10 and V26.2.07 independently passed run/saved/fresh-process reopen on the same product source `43130a49f49676299b865f094a9a6ded482f67ad`. The local packages retain committed version `0.1.0-preview.10307` and are explicitly `LOCAL_PR_CANDIDATE`, not the official release of that version. V25 harness `e32fba7b808bbe2a286c5c8b625e061de48d8ea0`; V26 harness `628a920b5b943ea1273d3b7132eb758e6f38b709`. H2=0/H2>0, repeated centres, native volume/extents/XData ownership, Family regeneration/former-output erase, generic Foundation refusal, QS3DSAVE/QSAVE and cold dimensional/cardinality checks passed. Profile/current-pointer/private-drawing cleanup and original tunnel-autostart restoration passed with zero process residue. Exact hashes and consumed V26 probe-load failures are in `docs/validation/issue-5718-local022-native.md`.
- Remaining acceptance: V26 physical UI placement/edit/save/cold-reopen; cross-DPI/Quantity/private-DWG UI; complete topology/taper-section coverage beyond the bounded dimensional/cardinality/identity checks. V25's allocation29 now proves its bounded visible Add/Cancel/physical picks/Enter/Esc/edit/save/cold-reopen path. Aggregate status stays `IN_PROGRESS`; preserve exact native/API and physical-UI evidence separately.
- Owner-authorized #4586 continuation (2026-09-05): the owner explicitly reassigned the existing source carrier to this session. PR #4594 is reopened; exact pushed candidate `0db6e659510809a6781221204a32409605c851ba` fixes final Grid detachment, pre-Room Grid dispatch and Family/finish button isolation. Matching V25/V26 local PR packages built with zero warnings/errors and retain committed version `0.1.0-preview.10307` (not official-release evidence). ZIP SHA-256: V25 `6b6d00de4d391e772b58780be96afab9e4b31c0d8e0246dee3d7b79a8c1c5f70`; V26 `4259a2c9850e2e18dd82a8496a8b70c4c68d80abe290c51fb660e1e9d12e946d`. New UI allocations must use this pair, V25 first with full cleanup before V26. No licensed UI PASS yet. The historical ownership block below applied only before this explicit authorization.
- Actual V25 physical UI result (2026-09-05): harness `91b7810168d65bcf57ad29cf8248deac4e5d84dc` on the same frozen product source physically selected `Foundation.SingleFooting` and clicked the Family Add button. The surviving `OnGridAwareFamilyAddModeClick` ran before `OnBlt3dRoomAwareAddClick`, opened `Tham số` / `Solid3D`, and prevented the six-field dialog. Result `FAIL_OR_NO_RESULT / UI_TIMEOUT_OPENCANCELDIALOG`, zero verified phases; complete host/profile/private cleanup and exact autostart restoration passed. This reproduces existing source carrier #4586 (another session; closed-unmerged PR #4594, reservation still open), not a new duplicate defect. No source mutation/takeover was performed. Stop unchanged-source UI retries; require an authorized source correction, fresh matching package pair, then V25 UI PASS/cleanup before V26. V26 physical UI remains NOT RUN. Full consumed allocation history is in `docs/validation/issue-5718-local022-native.md`.
- Historical startup containment: #4179/#4268 ModelTree containment remained insufficient. The successor source fix is now merged: closed issue #4173 / PR #4998, final head `70043ca774b54f4f2fb6dc3b5710299ea1ec6dfc`, protected Shared CI `33374724885` with `preflight` + `core` SUCCESS, merge `80f609057bb95b58f08f3ea88ea22411b88cb558`. Every earlier preview is superseded for this crash qualification.
- Consumed post-#4179 descendant: `v0.1.0-preview.10229`; exact source/tag `a7b7dfb456654509c9eccf33f854350a61d9e59d`; ProductVersion `0.1.0-preview.10229`; official V25 ZIP SHA-256 `B7FE681B7958908B6E0E84C76CC6B62E8DFE97B42EAD3F80A4E01864D0F670F1`. Licensed V25 on sibling Foundation lane #4041 reached product/probe `NETLOAD` / `QS3D WILL_START` and again stopped before Workspace baseline with the same `VirtualizingStackPanel` virtualization-state exception class. No Foundation acceptance matrix ran. Therefore `.10229` is `NO_RESULT / DO_NOT_RETRY` for startup-dependent Foundation qualification and must not be used as a LOCAL-022 runtime candidate; this does not claim that #4034 itself executed the Móng đơn matrix on `.10229`.
- Source basis: #4019 / merged PR #4021, restored/finalized by #4058. No production source fix belongs in this runtime-only lane.
- Scenario: independently qualify V25 and V26 Cancel, `H2=0`, repeated placement, edit/regenerate, `H2>0`, semantic/native geometry, a generic Foundation control, `QS3DSAVE`/`QSAVE`, and cold reopen on one explicitly authorized exact successor source identity after the reopened startup defect is fixed.
- Runtime gate: the consumed `28e48e6d...` cells and `.10229` request are non-retryable. Do not launch or attach BricsCAD until an exact published descendant containing `80f609057b...` is pinned. Then use a fresh LOCAL-022 V25 task/allocation first, clean up completely, and only afterward run V26 on the same exact source identity with a separate fresh task/allocation. `.10233`, `.10270` and all earlier previews are inadmissible; release coordination is #5004.
- Evidence required: exact source/ProductVersion/hash identities for matching V25/V26 adapters and dependencies; per-host matrix verdicts; ownership/geometry/persistence checks; sanitized cleanup/no-residue evidence.
- Related docs: `docs/agent-work-claims/2026-08-26-codex-issue4046-restore-single-footing-regression.md`; `docs/validation/issue-5718-local022-native.md`; issues #4034, closed #4173, #4189, #5004 and #5718; PRs #4058, #4179, #4998 and #5738.
- Updated: 2026-09-05

## LOCAL-020 — Grid pair-owned intersection marker native lifecycle

- Priority: P1
- Status: OPEN
- Area: Grid / pair-owned native intersection markers (#3771)
- Source/runner status: `SOURCE_READY / MERGED_MAIN`; minimum source/guard ancestor `707ba4f2991e6ab47a81d9de80a32c19e55fca79`. PR #4109 passed fresh protected `preflight` + `core` on final task head `75810a0154cdb345395469bd19e17b6e009e6119` and merged as `3a0e9e06f70b11bffb323df2d205bd94c05ed71d`.
- Remote disposition: `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`
- Runbook: `docs/LOCAL-GRID-INTERSECTION-MARKER-QUALIFICATION.md`
- Why local: Final acceptance requires licensed BricsCAD V25/V26 native Circle/XData/object ownership, Undo/Redo, save/cold-reopen and multi-DWG/document/owner-space behavior; hosted/static CI is not `LOCAL_PASS`.
- Scenario: Run P01-P06 from the runbook on one clean exact intended source-containing SHA. Prove deterministic canonical pair/occurrence ownership, selected/all refresh and cancellation/freshness boundaries, malformed/missing/duplicate/foreign ownership fail-closed replacement, geometry refresh with Undo/Redo and save/cold-reopen, document/owner-space/multi-DWG isolation, and V26 parity where the linked source applies.
- Evidence required: exact tested QS3D SHA and matching V25/V26 plugin/host identity; pair/owner/XData/native entity assertions; before/after replacement and Health results; Undo/Redo/save/reopen/multi-DWG results; sanitized cleanup evidence. Record only sanitized evidence; no private paths, raw Handles/ProjectIds, proprietary DLLs or customer drawings.
- Evidence: `PENDING_LOCAL`
- Related docs: `docs/LOCAL-GRID-INTERSECTION-MARKER-QUALIFICATION.md`; `scripts/preflight-grid-intersection-marker-lifecycle.py`; issue #3771; PR #4109.
- Updated: 2026-08-27

## LOCAL-023 — Beam formwork behavior matrix on preview.10228

- Priority: P1
- Status: PASS
- Area: issue `#4093`; BricsCAD V25 Beam formwork M1–M8 behavior matrix
- Source/harness status: `COMPLETE / 100% / LOCAL_PASS / BEAM_BEHAVIOR_MATRIX`
- Remote disposition: `TERMINAL / DO_NOT_RETRY_REMOTE`
- Exact runtime artifact: `v0.1.0-preview.10228`; source `7dacdce17a6403d19681732ca7bad22cdb6f1499`; licensed host boundary BricsCAD V25.2.10 x64.
- Artifact hashes: ZIP `EC7385FC6085A838B94F84FC20B77E61E728952CC3A580FEC695031280FBC39E`; packaged V25 DLL `010F729470B0644CD0ECBFF7395F4DCFAE39E81AA1B230C7219AC18C11C1340A`.
- Canonical runtime carrier: draft PR #4094 / branch `agent/gpt56sol/issue-4093-beam-preview10223-matrix`. The existing branch name remains historical; the owner's 2026-08-27 direct runtime assignment retargeted its immutable qualification pin to `.10228` without creating a duplicate carrier.
- Why local: The final gate required NETLOAD of the immutable released adapter plus live native BREP face classification, directed deductions and aggregate/detail projection inside licensed interactive BricsCAD V25; source/static evidence cannot establish this result.
- Scenario: M1–M8 from `docs/LOCAL-V25-BEAM-FORMWORK-MATRIX.md` against the exact packaged DLL: Side/Bottom rule toggles, Top and End/Other exclusion, directed Side/Bottom deductions, aggregate/detail `FormworkM2` parity, and diagonal-axis face classification.
- Runtime gate: licensed interactive V25 only. Do not rebuild, substitute, instrument, or relabel the pinned preview. Verify sanitized evidence with `scripts/test-local-v25-beam-formwork-matrix-evidence.ps1` and restore profile/Loader/DemandLoad state with zero test-owned process residue.
- Evidence: `100% / LOCAL_PASS / BEAM_BEHAVIOR_MATRIX`; gross `7.07106781186549 / 9.19238815542513 m²`; Top/End/Other `0 / 0 / 0 m²`; net `6.77106781186549 / 8.80238815542513 m²`; Aggregate equals Detail `8.80238815542513 m²`; M8 `Side=2`, `End=2`, `Top=1`, `Bottom=1`, `Other=0`.
- Evidence required: Exact pinned preview/source and packaged V25 DLL identity; BricsCAD V25 host identity; sanitized M1–M8 cell results; gross/net Side/Bottom values; Top/End/Other exclusions; aggregate/detail parity; verifier result; blocker and cleanup state.
- Cleanup: protected profile and exact DemandLoad tree restored; nonce removed; ten stable zero-process samples; verifier `cells=8`; blockers empty.
- Related docs: `docs/LOCAL-V25-BEAM-FORMWORK-MATRIX.md`; `docs/evidence/2026-08-27-issue4093-preview10228-beam-matrix.json`; issue #4093; PR #4094.
- Updated: 2026-08-27

## P1 — #3480 Quantity Review exact native BREP face highlight

- Priority: P1
- Status: OPEN
- Area: Quantity Review / formwork exact native BREP subentity highlight
- Source-side status: `REMOTE_DONE / MERGED_MAIN`. PR #3487 merged the exact-face implementation at `main@9303fb34109e0b5859d8fc2ff1122afdc3cefa83`; final feature head `977066e1becbf3719a7c6324308288499cc7c57e` passed shared CI run `32557652786` / #12461 (`preflight` + `core`, including V25 compile).
- Exact source identity: `src/QS3D.BricsCAD.V25/UI/QuantityInsightPanel.DetailExplainer.ExactFace.cs` blob `a4e60d3ded1f649bed21ba589c21d855af37ef82`. The 2026-08-26 audit confirmed intended descendant `main@4bf7de082b8a1e6366612b10c69742fd24f5c969` still carries that exact blob.
- Remote disposition: `PENDING_LOCAL_AGENT / DO_NOT_RETRY_REMOTE`.
- Runbook: `docs/FEATURE-RUNBOOKS/issue-3480-quantity-exact-face.md`.
- Local handoff: fetch/sync Git, use a clean checkout/worktree of the exact intended source-containing SHA, then run the licensed BricsCAD V25 matrix in the runbook. At this audit the intended checkout is `4bf7de082b8a1e6366612b10c69742fd24f5c969`. If `main` has advanced, a local agent may qualify the newer exact `main` SHA only after confirming the ExactFace source blob above is unchanged; otherwise refresh this handoff before running.
- Scenario: prove clicking `SOLID-01/FACE-03` highlights only native BREP face #3 rather than whole-solid implied selection; `S gộp` / `S còn` route to the same exact face; switching faces removes the old face highlight; deduction target+cause/transient-region navigation remains intact; stale topology/fingerprint fails closed; tree/detail/document/panel transitions clear the highlight; save/reopen persists no face color/material/presentation mutation.
- Evidence required: exact tested QS3D SHA, BricsCAD V25 version, PASS/FAIL for every runbook row, proof no whole Solid3d is selected for exact-face actions, sanitized screenshots/log summary where useful, and no private/customer DWG, raw handle, private path, ProjectId, license data or proprietary binary in committed evidence.
- Evidence: `PENDING_LOCAL_AGENT`.
- Related source/docs: `src/QS3D.BricsCAD.V25/UI/QuantityInsightPanel.DetailExplainer.ExactFace.cs`; `docs/FEATURE-RUNBOOKS/issue-3480-quantity-exact-face.md`; `docs/agent-work-claims/issue-3480-gpt56sol.md`; issue #3480; PR #3487.
- Updated: 2026-08-26

## Close-out rule

Closing all `OPEN` P0/P1 items does not automatically mean the product is commercially released. Release publication still follows `CI_POLICY.md` and requires the owner's separate explicit release authorization. This inbox only records local engineering qualification truth.

## LOCAL-024 — #4352 ChatGPT MCP full-agent qualification

- Priority: P0
- Status: OPEN
- Area: issue #4352; ChatGPT MCP embedded full-CAD agent / Cloudflare onboarding
- Source/runner status: `SOURCE_READY / PENDING_LOCAL`
- Remote disposition: `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`
- Exact-source rule: Run only from a clean checkout of the exact candidate SHA that is intended for merge/release qualification; hosted/static CI is source evidence only.
- Why local: Final acceptance requires licensed BricsCAD V25/V26, Windows desktop/UI input, a real Cloudflare account/tunnel/browser login, and ChatGPT MCP connectivity.
- Scenario: Follow `docs/agent-work-claims/issue-4352-chatgpt-mcp-session-handoff.md` and `docs/MCP-FULL-CAD-AGENT.md`; prove embedded loopback MCP, authenticated protocol/read-only probe, Cloudflare Named Tunnel + Quick fallback, ChatGPT discovery, direct CAD API and bounded command/UI fallback, timeout uncertainty/no-auto-retry, emergency stop/resume/cancel, save/reopen, shutdown and cleanup on the exact candidate SHA.
- Evidence required: exact candidate SHA; licensed BricsCAD V25/V26 host/plugin identity; sanitized MCP/Cloudflare/ChatGPT results; no bearer token, Cloudflare credential, private path, customer DWG, proprietary binary, raw Handle/ProjectId or unsanitized screenshot.
- Evidence: `PENDING_LOCAL`
- Related docs: `docs/agent-work-claims/issue-4352-chatgpt-mcp-session-handoff.md`; `docs/MCP-FULL-CAD-AGENT.md`; issue #4352.
- Updated: 2026-08-29

## LOCAL-025 — #5719 background semantic UI mutation hardening qualification

- Priority: P0
- Status: OPEN
- Area: issue #5719; BricsCAD V25 background semantic UI discovery/invoke hardening
- Source/runner status: `SOURCE_READY / PENDING_LOCAL`
- Remote disposition: `PENDING_LOCAL / DO_NOT_RETRY_REMOTE`
- Exact-source rule: Run only from a clean checkout of the final merged `main` SHA containing PR #5741. Hosted/static source guards, V25 compilation, and Shared CI are REMOTE_SAFE source/build evidence only and must not be reported as licensed runtime PASS.
- Why local: Final acceptance requires licensed interactive BricsCAD V25 and live Windows UI Automation providers plus real BricsCAD document/UI-thread behavior.
- Scenario: Follow `docs/FEATURE-RUNBOOKS/mcp-background-semantic-ui.md`. Obtain semantic discovery with `confirmSensitiveRead=true`, record `discoveryGeneration`, then invoke one harmless allowlisted Ribbon/WPF control with exact same-process window, `elementPath`, expected control metadata, matching `expectedDiscoveryGeneration`, explicit `actionId`, and `confirmMutation=true`. Verify `provider-completed` keeps `cadStateVerified=false`, `retryAllowed=false`, and `requiresRediscovery=true`; stale generation/path/metadata, active-document change, and same-target-UI-thread cases fail closed before provider invocation. If a safe deterministic provider/postcondition failure can be induced, verify `uncertain` is redacted, the Accepted ACK replay does not invoke the provider again, and recovery requires inspection plus fresh discovery. Verify no focus/cursor/keyboard takeover, no desktop fallback, and Pause/Emergency Stop blocks mutation.
- Evidence required: exact merged SHA; BricsCAD V25/plugin identity; sanitized per-case outcomes and actionId ACK status; proof of no automatic retry/provider reinvocation for the same actionId; active-document/thread affinity results; no raw provider exception/path/screenshot/private identifiers; clean host/process state.
- Evidence: `PENDING_LOCAL`
- Related docs: `docs/FEATURE-RUNBOOKS/mcp-background-semantic-ui.md`; issue #5719; PR #5741.
- Updated: 2026-09-05
