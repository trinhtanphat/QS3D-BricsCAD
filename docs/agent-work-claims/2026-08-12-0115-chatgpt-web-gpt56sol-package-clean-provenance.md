# Work claim — V25 package clean-source provenance

- Status: `ACTIVE`
- Agent: `chatgpt-web-gpt56sol-package-clean-provenance`
- Registered: `2026-08-12T01:15:00+07:00`
- Baseline main SHA: `CLAIM_COMMIT_PARENT` (replace from exact claim commit before implementation)
- Priority: owner-requested continue-all review; prevent package metadata from asserting a Git SHA for bytes produced from an uncommitted source state.

## Verified defect

`scripts/package-v25.ps1` writes `gitCommit` from `git rev-parse HEAD`, then rebuilds package output, but has no package-boundary clean-worktree check. Direct/local invocation can therefore produce a package whose source-generated bytes include tracked/untracked repository edits while `PACKAGE-METADATA.json` names only the clean `HEAD` SHA.

## Reserved scope

Require a valid Git repository and clean tracked/untracked worktree before package mutation/output creation. Resolve and retain the exact HEAD SHA once, use that value for metadata, and fail before `dist` cleanup if status is dirty or Git inspection fails. Extend the existing customer-release/package regression and document clean-source package provenance.

## Expected surfaces

- `scripts/package-v25.ps1`
- `scripts/preflight-customer-release.py`
- `docs/MANUAL-BUILD-RELEASE.md`
- this claim file for baseline/completion updates

## Excluded scope

- V26 package/release lane; release tag semantics; finalizer/manifest/signing/updater/installer; build output contents; `src/**`; `tests/**`; GitHub Actions dispatch/re-run and licensed V25 runtime.

## Validation plan

- Git repository/HEAD/status checks occur before `Remove-Item $DistRoot` or any package output mutation.
- `git status --porcelain=v1 --untracked-files=all` must return no records; Git command failure is fatal.
- Metadata `gitCommit` uses the already-resolved exact clean HEAD rather than a second late Git read.
- Regression pins dirty-source refusal and ordering; deterministic clean/dirty model where practical.
- No GitHub Actions dispatch/re-run.

## Coordination

The active V26 lane is V26-only and must not weaken V25 behavior. No current V25 package provenance claim was found.

## Completion condition

V25 package creation cannot claim a Git SHA for a dirty source tree, regression/docs are on `main`, and this claim is `COMPLETED`.
