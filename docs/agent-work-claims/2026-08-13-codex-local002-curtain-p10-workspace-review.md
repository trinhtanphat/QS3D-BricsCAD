# Work claim — LOCAL-002 P10 generated Curtain Workspace review

- Status: `ACTIVE`
- Agent: `codex-local002-p10-20260813` (`/root`, local Windows + licensed BricsCAD V25 worker)
- Registered: `2026-08-13T17:54:48+07:00`
- Baseline main SHA: `68bbfc36f3ce1f178db8b716454c1435a9027fe5`
- Priority: `LOCAL-002 / P0 / P10` — the remaining source-ready generated-panel → Workspace/Family/Health/Release cell

## Reserved scope

Prepare and execute a guarded, repository-synthetic BricsCAD V25 qualification proving that selecting one production-generated Curtain panel resolves the canonical GlassWall owner through the real Workspace inspection path, presents the owner/Family review state, and participates in both Health All and Release Check without mutating the wrong owner or project.

This is LOCAL_ONLY qualification and minimum local-dependent automation. Production Workspace/Curtain ownership behavior remains read-only in this lane; any product defect found by the licensed run is handed to a non-local source agent instead of fixed here.

## Expected surfaces

- one additive automation-only P10 command/probe under `src/QS3D.BricsCAD.V25/`, limited to driving and observing real Workspace/Family/Health/Release behavior without changing their production contracts;
- one guarded PowerShell runner under `scripts/` using only a fresh ordinary copy of `samples/generated/QS3D-Sample.dwg` outside the repository;
- one focused static preflight under `scripts/` locking the exact P10 boundary, sanitized marker schema, cleanup and no-production-bypass rules;
- `docs/CURTAIN-NATIVE-PANELS.md`, `docs/LOCAL-AGENT-INBOX.md`, and this claim for exact-SHA evidence/status only after the guarded run;
- exact installed-reference V25 `Release|x64` build, focused/aggregate local gates and licensed runtime evidence.

## Excluded scope

- No production changes to `WorkspacePanel`, semantic ownership resolvers, Curtain builders/planners/Health/Release services, Family mutation, project lifecycle or generated-handle policy.
- No P11 native Undo/Redo work or issue `#987`; its active source claim remains independent.
- No LOCAL-004 Source Reconcile work or issue `#1005`.
- No Level placement (`LOCAL-003`), P01-P09/P12 reruns, broad modeless-window qualification, customer/private DWG, V26, installer, release, signing or GitHub Actions.
- No promotion of overall LOCAL-002 from one bounded P10 result.

## Validation plan

- Re-fetch current `main` and re-audit ACTIVE/BLOCKED claims before implementation and again before every integration commit.
- Build the exact candidate against installed BricsCAD V25 references with zero warnings/errors and verify plugin/Core ProductVersion ends in the exact Git SHA.
- Run focused Workspace/Curtain ownership, runtime-health, Family/project-affinity and new P10 static gates; run Core smoke and the aggregate local preflight where current-main prerequisites allow.
- Run the guarded V25 probe with zero pre-existing BricsCAD processes, an initialized profile, fresh synthetic copy and empty outside-repository artifact directory.
- Require a sanitized PASS proving selected generated panel → exactly one canonical GlassWall owner, expected Family review identity, Health All/Release participation with zero blocking Curtain issues, preserved source/native sentinel/project affinity, and verified process/script/sidecar/lock/DWG-hash cleanup. A sanitized FAIL remains `PENDING_LOCAL` and is handed to the correct remote source owner.

## Coordination

The completed `2026-08-13-codex-issue982-workspace-curtain-owner-selection.md` source claim made P10 `SOURCE_READY` and explicitly left this exact licensed proof pending. The ACTIVE `2026-08-13-codex-issue987-curtain-native-undo.md` claim excludes P10. The BLOCKED LOCAL-003 Level claim excludes Curtain P01-P12 topology/qualification and this probe will use legacy/no-Level geometry.

## Completion condition

The additive probe/runner/gate is integrated on current `main`, one clean exact-SHA V25 run records truthful bounded P10 evidence with all cleanup postconditions, docs mark only P10 `LOCAL_PASS` or retain `PENDING_LOCAL` with a source-blocking handoff, this claim is `COMPLETED`, and no GitHub Actions or private/customer data is used.

## Licensed attempt — 2026-08-14 NO_RESULT

The first clean licensed attempt used exact merged `main` SHA `337e97d32b6642b3a3d013596ffa1545df168999`, BricsCAD `25.2.10` and x64 Release adapter SHA-256 `17AE9378E46ACD18B50611A268C939B19F0EDAF51DCE59EA4F1D744DDB063229`. BricsCAD did not emit the P10 marker within the guarded 900-second startup window and no launcher-to-host handoff occurred, so this is `NO_RESULT`, not a product PASS or FAIL.

The runner nevertheless proved its failure cleanup contract: the launched process exited, private script and sidecar state were removed, the UI layout was restored, and the disposable DWG returned byte-for-byte to SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`. No private path, profile value, drawing content or exception payload is retained here. P10 remains `SOURCE_READY / PENDING_LOCAL`, and this claim remains `ACTIVE` pending a fresh initialized-profile run that reaches the sanitized production marker.

A second clean attempt on exact SHA `4de61a97362003072e0918b7a35e12f53671f2e6` first proved the initialized V25 profile and exact adapter independently: `QS3DRUNTIMEPROBE` passed in about five seconds on BricsCAD `25.2.10`, with adapter SHA-256 `BC8C4F9B513DCE615832E687FE4294A0716C76A1C7A8BCFA6E62D8401A897A1E`. The P10 runner then opened the correct disposable DWG in a responsive host but produced no product marker before its 600-second guard. A same-SHA P01 diagnostic using the shared Direct Draw GlassWall -> source prepare -> Curtain3D prefix also produced no marker before its 300-second guard. Both disposable DWGs retained repository SHA-256 `CEC1350FB2207542AEECD96A790A198A6C9CC9E99A9F875871F367554B3D967E`; P10 process/script/private-state/UI-layout cleanup passed.

Because the existing final marker cannot distinguish which pre-review command retained control, this ACTIVE claim now adds an automation-only allowlisted progress checkpoint after each production command boundary. The progress artifact publishes only one coarse phase token such as `plugin_loaded`, `direct_draw_complete`, `source_selection_prepared` or `curtain_build_complete`; it never publishes Handles, IDs, paths, profile values, drawing content or exception text. This diagnostic refinement does not change production behavior and does not promote P10.
