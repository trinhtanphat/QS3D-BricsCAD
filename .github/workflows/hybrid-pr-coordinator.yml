name: QS3D Hybrid PR Coordinator

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  coordinator-disabled:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: Coordinator disabled
        run: echo "QS3D Hybrid PR Coordinator automatic runs are disabled."
