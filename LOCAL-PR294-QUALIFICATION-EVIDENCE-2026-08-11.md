# Local qualification evidence recovered from PR #294

Updated: 2026-08-11 (UTC+7)

This file preserves sanitized exact-SHA evidence produced on the local BricsCAD V25 branch that became draft PR #294. It is **evidence/history only**, not a second live LOCAL_ONLY queue. `docs/LOCAL-AGENT-INBOX.md` remains authoritative for current priority and current-main qualification status.

## Exact-SHA evidence preserved

- `3a8ae9fc5165fda588ac1377545ad9b31c85982e`: automated baseline passed clean-tree/manual-CI/source preflights, 365 aggregate feature gates, Core Release build and deterministic smoke, exact-V25 adapter Release build with zero warnings/errors, offline WPF theme/Workspace/RightPanel smoke, and licensed BricsCAD V25 NETLOAD/Ribbon/Palette runtime probe. Sanitized runtime identity recorded BricsCAD `25.2.10`, x64 CLR `4.0.30319.42000`. The same adapter binary SHA-256 `A78FDC7F9E5300EEBA3E553D9C571F15E38075D8570FB00AE70C3679DCA3A991` was packaged locally and passed clean BricsCAD start plus `QS3DRUNTIMEPROBE` through DemandLoad. This was source-build/runtime-smoke evidence only, not full customer-release qualification.
- `604982a506aa07ba3eae047d282847333d529314`: `scripts/test-bricscad-v25-project-lifecycle.ps1` passed on BricsCAD `25.2.10`, covering disposable-DWG SaveComplete sidecar persistence, cold-cache identity continuity, canonical existing-project binding, detached-snapshot immutability, per-DWG isolation, persisted cold reload, absent-sidecar non-creation and corrupt-sidecar fail-closed behavior. Evidence was sanitized and temporary runtime state/process residue was removed.
- `f627e9667e58c036df0be5b094a6f9cc494abaeb`: the expanded lifecycle runner passed real `QS3DREGEN`, `QS3DREFRESH` and `QS3DFINISH` existing-project plus absent-sidecar phases on BricsCAD `25.2.10`. The local pass also exposed and drove lifecycle fixes around passive Workspace project creation and detached modeless state before the final rerun passed.
- `1dfbf35fbc9745b880133f7ac0cdfb50887391b1`: the nine-phase lifecycle runner passed legacy automatic unit binding through real `QS3DBQ`, no-project supported-INSUNITS inspection without project creation/cache, and explicit `QS3DUNITS` project bootstrap/persistence. It also exposed and drove fixes for unit inspection using `GetOrCreate` and BQ XAML checkbox events mutating live preferences during initialization.
- `cd96507f942471d2030b3dbe0acc61f5fabfd5a7`: clean-room public BRC proxy and B4D/ED2 round-trip qualification passed on BricsCAD V25.2.10 x64 using disposable reference copies only. The reference-copy SHA-256 `7B5D54E620500564ADA20B8DDDE0FCA129E6A687644CE07D0BFB2B4A0D8B4B66` was unchanged before/after. Public probing opened 352/352 current-space entities with zero read failures and found 25 proxy entities. No proxy exposed a finite positive direct Length, plan Area, surface Area or Volume; 24 proxies had non-empty public `Explode` results with surface-only evidence across 304 positive Face parts, so all 25 proxies remained review-only and no proxy geometry/quantity was inferred. The B4D/ED2 probe produced 31 project elements, 31 `CHI_TIET` rows, one `TONG_HOP` row and 31 live export Handle references; the 25-column workbook preserved drawing fingerprint plus Element ID/Handle provenance and Excel Locate selected exactly one active CAD object. No private source path, raw Handle list, customer drawing or workbook is recorded here.

## Current-main boundary

These PASS statements belong only to the exact historical SHAs above. Integrating the reusable source, runners, tests and documentation from PR #294 into a newer `main` does **not** transfer `LOCAL_PASS` to the new squash SHA. Current-main exact-SHA qualification remains governed by `docs/LOCAL-AGENT-INBOX.md`; remote/non-local agents must not rerun or claim the missing V25/private-DWG/UI gates.
